{
  "name": "health-expert",
  "version": "1.0.0",
  "description": "The Wallach Codex — root package. Exists only to hold puppeteer for the headless render probes (tools/render_probe*.js); the app + its build toolchain live in dashboard/.",
  "private": true,
  "type": "commonjs",
  "devDependencies": {
    "puppeteer": "^25.1.0"
  }
}
