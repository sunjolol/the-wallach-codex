# Brain v3.14 — Cura's findings executed (Round 107)

_Round 107. 2026-06-17. The user-approved execution of Cura session #1's three findings (Survivors A + B + addendum) from 2026-06-17 03:48 AM + 03:55 AM addendum. The first end-to-end proof of the Round 100 cross-mode collaboration loop working as intended._

## What changed

This is a tool-surface + protocol change. `brain/current.md` text is unchanged. The brain version bumps because the disciplines codified govern future round closes and catch-up behavior.

### 1. Operating-protocols §1 extended with downstream-sweep discipline (Survivor A)

New bullet point #6 under §1 governs "Round-close residue in long-lived markdown files." When a round close touches `memory/open-threads.md` or any other multi-section markdown file, the closing-move includes an explicit sweep of all semantically-distinct sections (Active / Deferred / Standing / For-next-session) for items the close just changed. Reconcile version refs in forward-looking sections against the top status line. Move completed Deferred items to Recently shipped or remove. Dedup items filed across mutually-exclusive sections. Cost ~3 min per close.

Cura session #1's audit lean was right: file-hygiene invariants are too brittle for this surface (historical "Recently shipped Round N" sections legitimately reference past versions). The discipline-only path is preferred; a `check_open_threads_status_consistency` invariant is filed as a candidate but not promoted.

### 2. Operating-protocols §23 — markdown embed auto-restore at catch-up (Cura addendum)

New section. When a catch-up trigger or morning briefing finds `audit-sentinel.json`'s `last_lapse_reason` matches the `Markdown content: cl-data-* size mismatch` pattern, the agent's routine first action is `python tools/dashboard_integrity.py restore`. Surface the action in the briefing per always-surface-all-logs (Round 105).

The reasoning (audited before shipping): NOT a patch job. The audit FAIL between Tacitus's autonomous nightly write and the user's interactive morning session is INFORMATIVE — it tells the user "the dashboard has new content to read." The restore is the canonical resolution workflow per the Round 105 canonical-vs-projection distinction. Alternatives (post-Tacitus sync hook breaking Tacitus's boundary; excluding cl-data-notebook from strict check training the user to ignore signals; runtime dashboard fetching blocked by offline-first) are all structurally worse.

### 3. New invariant `check_tacitus_v1_task_no_resurrection` (Survivor B)

In `tools/invariants.py`. Severity warning, daily cadence. Scans today's notebook for session headers; verifies every header's mode name is in the canonical {Cura, Vision, Aegis} allowlist. Foreign headers on operational days = warning. Catches v1 task accidental re-enable, deletion undo, Windows scheduler restoration, or unforeseen new mode drift. The deletion's truth claim, previously prose-only, is now pinned to file structure. Now 25 daily invariants total.

## What the agent does differently

When closing any round that touches `memory/open-threads.md` (and almost every substantive round does):

1. Append the saga / lessons / decisions entries (§1 unchanged).
2. Run the downstream-sweep (§1 new bullet 6): inspect Active / Deferred / Standing / For-next-session for items just changed; reconcile version refs; move completed Deferreds; dedup duplicates.
3. Then proceed with version_bump, integrity restore, audit, catchup seal.

When running catch-up or morning briefing (every fresh session):

1. Read sentinels per existing protocol.
2. If `audit-sentinel.json` shows `last_lapse_reason` matching `Markdown content: cl-data-* size mismatch`, run `dashboard_integrity.py restore` as the first concrete action (§23 new).
3. Surface the restore action in the briefing per always-surface-all-logs.
4. Then proceed with the rest of catch-up.

The new invariant runs automatically in the daily audit; nothing in the agent's discipline changes for it directly.

## What's the same as v3.13

- `brain/current.md` text — agent instructions unchanged.
- §1 closing-move-atomic principle and points 1-5 — unchanged. Point 6 is additive.
- §17 / §20 / §21 / §22 — all unchanged. §23 is a new sibling section.
- Tacitus subsystem state — unchanged. The new invariant pins to its file structure; doesn't modify any Tacitus surfaces.
- Daily audit / weekly audit tasks — unchanged.

## The principle underneath, named

Rounds 45 / 104 / 105 / 106 / 107 form a progression of cross-system drift defenses. Each round names a specific instantiation of "shared mutable state across system boundaries drifts; the cure is one canonical source per surface plus a verifier or eliminator of the redundant state."

- **Round 45:** versions.json as single source of truth + propagator.
- **Round 104:** saga.md as canonical source of round numbers + saga↔versions invariant.
- **Round 105:** vitality-findings.jsonl as canonical persistent log + in-session re-check + invariant.
- **Round 106:** stdin payloads as filesystem-state-free write primitive + SKILL anti-pattern §22.
- **Round 107:** downstream-sweep at round close (§1 ext) + markdown embed auto-restore at catch-up (§23) + v1 task no-resurrection invariant.

Each round took a real failure (often surfaced by the user or by defense in depth or by Cura's autonomous reflection) and closed the upstream structural surface. Defense in depth caught the failures; structural fix removed the need for the safety net on those specific surfaces.

## The cross-mode collaboration loop, demonstrated

Round 107 IS the first end-to-end proof of the Round 100 cross-mode architecture working as intended:

1. Cura session #1 (2026-06-17 03:48 AM) raised three findings she could not act on herself (§4 boundary forbids her from editing invariants, protocols, prompts).
2. Cura session #1 addendum (03:55 AM) surfaced an architectural tension she'd discovered through her own legitimate write.
3. Vision session #1 (04:42 AM) picked up the Phase 0 self-correction note from Cura's addendum and shaped it into the user-actionable Phase 0 pre-flight proposal — cross-mode collaboration in action.
4. Aegis session #1 (05:15 AM) scored both runs, naming the cross-mode pattern explicitly in the meta-observation.
5. The user reviewed in morning catch-up, approved with audit-then-execute discipline.
6. Round 107 (this round, 2026-06-17 2:25 PM) executed all three Cura findings.

The architecture earned its night-one + day-one cost. The loop of autonomous-finding → autonomous-scoring → user-review-and-approve → co-work-execution is the safe shape. The agent doesn't ship autonomous findings without user approval; the user is always the gate; the audit-then-execute discipline catches over-trust.

## Filed for future watch

- Cura's Vision Survivor B (Phase 0 pre-flight audit in Tacitus prompts) is still on the Active queue — the user flagged it as "more serious; independent audit-only first before any execution." Round 107 did NOT execute it; left for the audit pass when the user is ready.
- Cura's NEAR-MISSes (nightly coverage snapshot, quarterly themes digest, session-index header) remain filed for revisit after specific preconditions change (Pass F lands, 2-3 weeks of operational rhythm, etc.).
- The cross-night memory question (do Cura/Vision/Aegis read prior notebook entries?) — user opted to observe for 2-3 days before deciding whether to add structural memory. No action this round.

The discipline progression continues — the next surface that needs defense will surface in time and Round N+1 will close it.
