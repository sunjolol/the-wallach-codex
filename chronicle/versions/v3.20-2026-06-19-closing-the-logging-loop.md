# Brain v3.20 — Closing the logging loop (Round 148)

**Shipped:** 2026-06-19 at 4:36 PM EDT
**Round:** 148
**Trigger:** Round 120 logging-discipline failure family recurred; user direction: *"close the loop... no more failure by design"* + *"if there's another system/structure/whatever within this project that could ALSO benefit from this system, we need to conceptually tie it in."*

## What changed

v3.20 ships the **Closing-move record + paired-write integrity** discipline (operating-protocols §30) project-wide. The marker pattern from §27 (verified-pattern-search) generalizes to four logging surfaces — implementations.jsonl / lessons.md / decisions.md / memory-change-log.md — each with a per-surface truthfulness invariant. The new `memory/paired-write-catalog.md` enumerates every paired-write surface in the project (14 total — 13 covered, 1 partial) with its trigger, marker, and paired invariant. A new meta-invariant `check_paired_write_catalog_coverage` verifies every `check_*` citation in the catalog resolves to a registered invariant.

The Tacitus dashboard gains a loud `unknown_unlogged` synthetic-status badge for survivors past 1 day with no implementations.jsonl entry, and the build itself fails (exit 1) for survivors past 3 days unless `IMPL_LOG_UNGATED=1`. Single-source-of-truth invariant `check_dashboard_impl_status_source_purity` pins the renderer to `implementation_log.latest_status()` only.

## Why this matters

Round 120 noted the §24 logging-discipline failure and spot-fixed it with trigger-phrase recognition + dual-surface logging. The candidate paired invariant was filed Deferred because its dependency (task-list on-disk export) never materialized. The discipline lapsed within 24 hours. Round 148's user observation: *"we dealt with this same issue before and I thought we fixed it"* — naming the failure family-level rather than instance-level.

Per lessons.md §18 ("Known failure modes have detectors, not memorials"), a pitfall without a paired detector is a recurring tax. v3.20 closes the tax at FOUR logging surfaces simultaneously rather than the one (implementations.jsonl) that triggered the round.

Compounding bug discovered + fixed in the same round: `latest_status()` in `tools/implementation_log.py` was crashing on `int(None)` whenever it iterated past the user-mode entries (source_session=null) appended Rounds 131/132. The build script swallowed the exception via `except Exception: impl = None`, silently dropping ALL implementation badges across ALL sessions since 2026-06-18 17:15 EDT. Two surfaces in agreement, neither anchored to truth — exactly the Round 74 "agreement is not truth when surfaces share a cache" pattern at the dashboard-render layer.

## What's the same as v3.18

Operating principles unchanged. Source-rule unchanged. Engineering doctrine principles 1–11 unchanged. Substance discipline unchanged. Tool surface (v3.4) unchanged. Catch-up trigger + briefing-as-proof unchanged. Tacitus three-mode architecture unchanged.

## Surface summary

- **Files added:** `memory/paired-write-catalog.md`, `brain/versions/v3.20-2026-06-19-closing-the-logging-loop.md` (this file)
- **Files modified (sub-system):** `tools/implementation_log.py`, `tools/build_tacitus_dashboard_live.py`, `tools/invariants.py`, `tacitus/dashboard/index.html`
- **Files modified (canonical):** `memory/operating-protocols.md` (§30 added), `memory/essence/saga.md`, `memory/essence/lessons.md`, `memory/essence/decisions.md`, `memory/open-threads.md`, `memory/memory-change-log.md`, `memory/versions.json`, `brain/CHANGELOG.md`
- **Invariant manifest:** 44 → 51
- **Implementations.jsonl:** 5 new entries (3 implemented + 2 deferred), post file-level verification

## Note on v3.19

v3.19 reserved — `version_bump.py minor` jumped 3.18 → 3.20 directly. Convention follows v3.16's "skipped — reserved" precedent. No content drift introduced.
