# Brain v3.23 — Saturday afternoon close (Round 156)

_2026-06-20. Continuation of the Saturday filed-work arc — real Item 3 fix + 10 more items shipped + 2 user-decision items (Pattern D promotion + Vision Survivor B expansion) closed out._

## What changed at the protocol/tool/manifest layer

Round 156 continued the Saturday arc that Round 155 started. Round 155 cleared the 5 originally-filed Items; Round 156 cured the bug surfaced by the user on the first Item 3 ship, then cleared every remaining open thread except the 5 explicitly-deferred items.

**New tool surfaces.** None. (Items 14/18/20 all extend existing tools.)

**New protocol/cursor surfaces.** 
- `tacitus/security-audit-cursor.json` — 8-surface rotation cursor for the new Cura security sub-check (Item 14).

**Invariant manifest changes (net daily -1).**

| Action | Invariant | Cadence | Severity |
|---|---|---|---|
| Added | `check_round_markers_truthful` (umbrella) | daily | warning |
| Removed | `check_round_pattern_consultation_marker` | daily | warning |
| Removed | `check_round_implementations_marker_truthful` | daily | warning |
| Removed | `check_round_lessons_marker_truthful` | daily | warning |
| Removed | `check_round_decisions_marker_truthful` | daily | warning |
| Removed | `check_round_memory_writes_marker_truthful` | daily | warning |
| Added | `check_deferred_candidate_invariant_drift` | daily | warning |
| Added | `check_no_native_dialogs` | daily | warning |
| Added | `check_log_surface_mtimes` | daily | warning |
| Added | `check_tacitus_dashboard_no_real_data_fetches` | daily | critical |

Net: 5 retired + 5 added (1 umbrella + 4 deferred-candidates). The 5-failure-point protection from the 5 marker-truthful checks is preserved — each underlying `check_fn` body still lives in the file and runs verbatim under the umbrella. Manifest noise drops from 5 rows to 1 for that family.

**Verified-patterns catalog promotions.**
- **Pattern D — Chokepoint helper + window-exposed trigger primitive + paired routing invariant** (Round 150 / §31 ship; promoted Round 156 per user-confirmed recurrence expectation). Catalog 12 → 13 entries. Future LS-backed multi-surface state surfaces apply this recipe by default.

**Cura prompt extension.**
- Six sub-checks (was five). New 6th: **Security** — gated on `tacitus/feature-flags.json cura_security_subcheck.enabled` (default true). Cap: 1 LAND/night unless `CRIT_OVERRIDE` asserted. Rotation cursor at `tacitus/security-audit-cursor.json` (8 surfaces, 21-day cadence, sample 2-3 per night).

**Tool atomicity discipline.**
- `tools/version_bump.py` now snapshots `versions.json` BEFORE mutation; on dashboard embed failure, rolls back via `save_versions(snapshot)`. The Round 114 silent-overwrite family is closed at the tool-orchestration layer.

**Architectural commitments codified (decisions.md).**
- **Pattern D as canonical for LS state.** Any new dashboard feature involving multi-surface LS state must apply Pattern D — define one trigger primitive per state family, N chokepoint helpers (one per LS key, each firing the trigger), `window.X = X` exports, catalog row, paired routing invariant. The umbrella principle: "any LS-backed multi-surface UI state belongs in a chokepoint regime."
- **Consolidation-via-umbrella safe-by-design constraints.** When N coherent checks become manifest noise, consolidate via umbrella IFF (a) underlying `check_fn` bodies remain in the file, (b) all N checks run on every umbrella call (no short-circuit), (c) per-surface detail in failure messages. Not applicable when checks are truly independent failure families or when underlying checks are expensive.

**Lessons codified.**
- **Authoring is not verifying for UI fixes.** "Code change passed integrity" is not the test for user-visible changes — the user reload + observe is. UI-affecting saga entries must name the exact reload sequence and expected visual state.
- **Tool-level non-atomic writes are Round 114 family.** Any tool that performs N>1 persistent writes as one logical operation needs snapshot+rollback. Sibling to safe_write's path-level atomicity at the multi-write transaction layer.

## Failure modes this version cures

- **The first-ship-without-browser-test pattern (Item 11).** Round 155 Item 3 fix claimed "done" without verifying the user-visible state. Round 156 Item 11 cures with `dose_text` + `has_nutrient_data` + load-time slot-stats refresh; the lessons entry codifies the discipline going forward.
- **Tool-orchestration non-atomic writes (Item 20).** Today's brain double-bump came from `version_bump.py` writing `versions.json` before the dashboard integrity check. Cured with snapshot+rollback. The Round 114 family is now closed at both the safe_write primitive layer AND the tool-orchestration layer.
- **Editorial drift in the Deferred section (Item 18 first invariant).** `check_deferred_candidate_invariant_drift` was filed Round 125; never shipped until Round 156. Caught 5 stale entries on first fire; all retired in the same patch. Future stale entries surface at the next daily audit, not at a manual cleanup pass weeks later.
- **Pre-existing native dialog tech debt visibility (Item 18 second invariant).** `check_no_native_dialogs` surfaces 8 pre-existing native dialog calls every audit. They're queued for `showLcModal` conversion at a future cleanup; the warning is the structural pressure that the Round 127 "discipline → invariant" doctrine calls for.

## Next session orientation

Catch-up trigger reads this file via the brain catch-up sequence. The brain prompt (`current.md`) is unchanged at v3.23; the version reflects the support-layer additions and the architectural commitments. Next operational fire: **Monday 2026-06-22 at 3:48 AM EDT** (Cura) — exercising the Round 140-147 prompt edits AND the new Round 156 Security sub-check for the first real time on real substrate.

**Saturday work is structurally COMPLETE.** Only the 5 user-confirmed deferred items remain on the watch — V-A (Aegis counter-argument gate, paired with V-B expansion as side-by-side experiment), Living-system-kernel open questions #4-#8, Security hardening (gated on share/export ship), brain folder review (~2 weeks from now). Plus a small queue of intentional ongoing technical debt — the 8 native dialog calls queued for `showLcModal` conversion, and the next-session "audit + shrink old code" pass to re-evaluate the 3.0 MB / 512 KB budgets.

— Round 156, Saturday afternoon wrap
