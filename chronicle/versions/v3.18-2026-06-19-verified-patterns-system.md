# Brain v3.18 — Verified Patterns System (Round 140)

_Round 140. 2026-06-19. Generalizes the verified-formula + parameter-tuning discipline from design (Round 139 v6/v7 — Juxtopposed CodePen neumorphism port) to all project domains: engineering, security, ops, logging, tacitus, brain. The catalog substrate + Cura/Vision integration + user-controlled feature-flag toggle + traceability discipline (saga marker + rollback recipe + inline file labeling) ship as one structurally complete system._

## What's new in v3.18

**1. Verified Patterns catalog** — `memory/verified-patterns.md` holds 9 seed entries spanning all domains:
- Atomic safe_write + byte-verify (engineering, Round 73 §17)
- Cron + sentinel + paired invariant (ops, Round 117)
- Cross-boundary allowlist + critical invariant (engineering, Round 135)
- Sidecar JSON attestation + paired invariant (engineering, Round 137)
- Two-layer enforcement: write-time + audit-time (engineering, Round 136)
- Reference standard + nightly Cura audit (tacitus, Round 136 Cure C)
- Append-only structured log + resolution invariant (logging, Round 105)
- Grainy Neumorphism Toggle (design, Round 139 v6)

Each entry follows uniform shape (Source / Domain / When to use / Recipe / Parameters / Anti-patterns / Instances). Promotion criteria codified: verified + concrete + recurring + user-approved.

**2. Operating-protocols §27 — Verified-pattern-search discipline.** Before any substantive implementation, read `memory/verified-patterns.md` and check for an applicable pattern. If found, the implementation reduces to parameter-tuning. Every substantive round's saga entry includes a `**Patterns consulted:**` marker (saga-marker discipline). Pattern-search becomes a pre-implementation gate.

**3. Operating-protocols §28 — Rollback-recipe-in-saga discipline.** Every major-feature saga entry includes an explicit `**Rollback recipe:**` section listing files added/modified, reversal steps, dependencies. Every file touched by a major-feature round gets an inline comment labeling the round number. This responds directly to user's stated concern about losing track of project complexity as it grows.

**4. Tacitus integration via rubric extensions** (NOT new sub-checks):
- `tacitus/prompts/cura.md` — Architectural sub-check gains a verified-pattern question, gated on `cura_pattern_search.enabled` in `tacitus/feature-flags.json`
- `tacitus/prompts/vision.md` — Phase 1 scan gains a pattern-seed candidate type, gated on `vision_pattern_seed.enabled`. Hard cap: ONE candidate per night. Vision NEVER proposes batch conversion — Vision cannot see rendered output. Every pattern-seed candidate ships with the explicit seed-not-propagate framing.
- Tacitus v2.4 → v2.5 (prompt extensions ship with this brain version)

**5. User-controlled feature flags** — `tacitus/feature-flags.json` holds both flags. User toggles via co-work request ("turn off Cura pattern suggestions" → Claude updates the file). No automated UI write surface. Intentional manual control for safety per user direction.

**6. Three new invariants** (manifest 36 → 39):
- `verified_patterns_catalog_present` (warning) — catalog file exists with ≥1 entry
- `feature_flags_present` (warning) — flags JSON exists with expected keys; surfaces enabled/disabled state
- `round_pattern_consultation_marker` (warning) — last 3 saga rounds (Round 140+) have the `**Patterns consulted:**` marker

## The discipline this version codifies

Round 139 v6/v7 demonstrated verified-pattern + parameter-tuning for design — once a verified pattern exists, future work reduces to parameter changes inside the immutable structural commitment. v3.18 generalizes the operating cadence project-wide: pattern-search before implementation; catalog growth as the project matures; user control via feature flags; traceability via saga markers + rollback recipes + inline labeling.

## What's the same as v3.17

The 11 doctrine principles. Source-rule cornerstone. Closing-move-atomic (§1). All prior operating-protocols sections (§2-§26). All Tacitus mode structure (Phase 0-6, three modes). All catch-up trigger discipline. Verified Patterns System extends the doctrine surface but doesn't modify any existing rule.

## Rollback recipe

Per §28, this version's rollback recipe lives in `memory/essence/saga.md` Round 140 entry. Reversal is mechanical: disable flags first (lowest-risk soft-disable); remove prompts/CSS/files in reverse-dependency order if full rollback needed; restore brain v3.17 + tacitus v2.4.
