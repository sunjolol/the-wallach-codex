# Brain v3.26 — §32 whack-a-mole rebuild trigger + Vision/Aegis design proposal recorded (Round 159)

_2026-06-20 late evening. Saturday close. User-recognized structural pattern lands as doctrine. Vision/Aegis rebuild-proposal mode + Aegis patch-fatigue detector + simulate-before-ship discipline RECORDED verbatim at tacitus/notebook for a dedicated design session — NOT shipped this round per user direction ("ship Part A, log the rest"). Dashboard not touched this round._

## What user recognized

After Round 158 close: "I noticed twice today we ran into a cluster of issues and whack-a-mole moments where we just decided 'let's build a new, better system' ... after we implemented these new systems, it INSTANTLY felt wayyyy better building with you, no more stupid issues and we could finally make progress." Two clusters observed: Rounds 148-152 (LS_SCHEMAS + chokepoint + state-mutation routing) and Round 157 (Eden sealed catalog). Both followed the same arc: 3-5 patches in a row to the same surface, each fixing the reported bug while regressing something else or surfacing a deeper issue. Rebuild ended the bug stream each time within 1-2 rounds.

User-named structural lesson: **when patches stop landing cleanly, the bug is no longer in the code — it's in the architecture.** You can't patch your way out of a shape problem.

## What shipped this round (Part A — lightweight in-session trigger)

Three layers of the whack-a-mole rebuild trigger, codified per user direction "Part A looks pretty light weight to me and super beneficial with no drawbacks":

**1. §32 doctrine in `memory/operating-protocols.md`.** Full trigger conditions: (A) 3+ consecutive bug-fix attempts to same surface in same session; (B) fix reverted/hotfixed/followed-by-still-broken report; (C) 3+ rounds touching same module produce no net forward motion. What Claude MUST do when triggered: STOP, name the cycle count out loud, propose the explicit choice (continue with stated rationale OR rebuild sketch), let user decide. Hard framing: NOT auto-rebuild, NOT a substitute for thinking, NOT scoped to dashboard.

**2. Brain prompt directive #6 in `brain/current.md`.** Inline self-check directive before any code change in a bug-fix context: "how many of the last 5 messages have been fixes to this area? If 3+, invoke §32 before writing code." Points to §32 as the hard-required check.

**3. Cross-session detector `check_whack_a_mole_clusters`.** Daily warning-severity invariant in `tools/invariants.py`. Always-PASS by design — text payload carries the advisory signal. Scans saga.md round entries from last 14 rounds. Detects files appearing in 3+ rounds. Noise filter excludes always-touched artifacts (dashboard.html, invariants.py, doctrine files) so signal:noise stays high. First fire surfaced 2 plausible candidates: `tacitus/prompts/cura.md` (rounds 146, 147, 156) and `tools/tacitus_simulate.py` (rounds 145, 146, 147). Both real iteration clusters.

Registration in `memory/paired-write-catalog.md`: §32 ↔ check_whack_a_mole_clusters row added per closing-move-atomic discipline.

## What did NOT ship — recorded for dedicated design session

Per user direction "for now just record all of this exactly for easy reference later" — the full Vision/Aegis proposal lands at `tacitus/notebook/2026-06.md` (2026-06-20 ~10:00 PM EDT entry, ~330 lines). That entry is the canonical source-of-truth for the dedicated redesign session. Three substantive ideas locked in:

**(I) `vision_rebuild_proposal` operating mode for Vision.** Additive — does NOT touch existing pattern-seed behavior. 5 ALL-must-be-true pre-condition gates (sustained-struggle signal + doctrine violation present + cooldown clear + non-trivial target + no-recent-successful-ship). 0-100 scoring rubric with 5 weighted categories (patch density / doctrine violations / lesson surface / cross-surface entanglement / user-reported regression density). Threshold ≥70 to propose. Required 8-section proposal structure with REQUIRED "Why this might be wrong" counter-argument paragraph (forces "if it ain't broke" consideration). Truth anchors: engineering-doctrine + lessons + claude-best-practices + verified-patterns + saga. Compliance invariant `check_vision_rebuild_proposal_discipline` enforces template compliance + counter-argument substantiveness. Anti-misfire core: "no vibes-based rebuilds" + required counter-argument.

**(II) Aegis patch-fatigue detector.** New Aegis sub-check that watches recent shipping activity for same-component clusters + user-facing "still broken / regressed" patterns in saga's verification notes + declining quality. Surfaces a landing recommending Vision's rebuild-proposal mode be invoked on the affected component. User-flagged correctly: this is Aegis's natural domain, NOT an invariant's. Replaces placeholder text-scan inside the Round 159 cross-session invariant once shipped.

**(III) Vision/Aegis simulate-before-ship discipline.** Cura EXEMPT. Any change to Vision or Aegis prompts/rules MUST be simulated standalone first — proposed change vs historical 14+ days of saga/lessons/decisions, score outputs, check misfires — BEFORE shipping to live nightly schedule. Implementation: `tools/vision_simulate.py` + `tools/aegis_simulate.py`. User's words: "this is absolutely critical when we dive into the MANY Aegis and Vision open tasks we have."

User also flagged performance concern: willing to relax weekly caps on Vision rebuild-mode if Vision can fire daily without "long think times / getting lost in the sauce." Empirical question; needs measurement after rebuild-mode ships.

## Invariant manifest changes

| Action | Invariant | Cadence | Severity |
|---|---|---|---|
| Added | `check_whack_a_mole_clusters` | daily | warning (always-PASS, text payload carries signal) |

Net: 55 → 56 daily. No critical added.

## Lessons + decisions

**Lessons logged** (1 entry at memory/essence/lessons.md):
- Round 159 — when patches stop landing cleanly, the bug is in the architecture, not the code. User-named source of the §32 doctrine. Cost asymmetry: rebuild was 1-2 rounds; not-rebuilding was ongoing whack-a-mole. Anti-doctrine: this is NOT rebuild-every-time — most bugs resolve in 1-2 patches; §32 just forces the conscious pause at the 3rd.

**Decisions logged** (2 entries at memory/essence/decisions.md):
- Round 159 — §32 whack-a-mole rebuild trigger adopted as project protocol (3-layer enforcement: in-conversation directive + cross-session detector + hard doctrine document).
- Round 159 — Vision/Aegis simulate-before-ship discipline (Cura exempt) — any prompt/rule change must be simulated against historical inputs BEFORE shipping to live nightly schedule.

## Truth verification

Eden hash: `8e594a01192a0930d2249163f5feb09a5a80046360a107c09c47bbe58fe086ca` — unchanged since Round 157 seal. All 3 Eden invariants OK. eden_write_protection clean — no agent writes to Eden protected files this round.

## Open thread filed

**Vision/Aegis dedicated redesign session** — high-priority, multi-substantive, source-of-truth at `tacitus/notebook/2026-06.md`. **Pre-conditions:** trace minerals brainstorm wraps up first (per Round 158 user direction); user explicitly invokes the design session. Order of operations preserved.

## Rollback recipe

Trivially reversible. Trim §32 from operating-protocols.md; trim directive #6 from brain/current.md; remove `check_whack_a_mole_clusters` function + manifest entry from invariants.py; trim row from paired-write-catalog.md. The tacitus/notebook entry stays (it's the recorded proposal, not the implementation — preserves design-session source-of-truth). Essence files: trim Round 159 entries. version_bump rollback to brain v3.25.
