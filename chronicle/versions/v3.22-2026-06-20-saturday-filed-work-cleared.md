# Brain v3.22 — Saturday filed work cleared (Round 155)

_2026-06-20. Saturday wrap consolidated round._

## What changed at the protocol/tool layer

This brain bump captures protocol-and-tool-surface changes from Round 155's consolidated Saturday close. The brain prompt itself (`current.md`) is unchanged — the bump reflects new tool surfaces + new verified-patterns entries + new invariants that future sessions must be aware of.

**New tool surface.** `tools/best_practices_refresh.py` — Sunday-conditional fetch of three `docs.claude.com` pages (prompt-engineering / context-windows / tool-use). SHA-256 hash compare against `memory/system/best-practices-snapshot.json`; jsonl-append to `memory/system/best-practices-findings.jsonl` on diff. Invoked by `tools/system_audit.py` weekly Sunday flow ONLY — no new scheduled task per user direction (the existing weekly audit cron at Sun 11 AM EDT is the existing fire surface).

**Invariant manifest changes (net +1 daily, +1 weekly).**

| Action | Invariant | Cadence | Severity |
|---|---|---|---|
| Removed | `open_threads_status_consistency` | daily | warning |
| Added | `cross_iife_bare_refs_reverse_scan` | daily | warning |
| Added | `best_practices_refresh_status` | weekly | warning |

`check_cross_iife_bare_refs_reverse_scan` widens the Round 135 forward check (allowlist-based, critical) with a complementary discovery-scan (walks IIFE private symbols + bare call sites, flags cross-IIFE references without `window.` prefix). The forward check catches regressions on the 11 known symbols; the reverse scan catches NEW instances of the family like Round 149's `esc()` slip.

`check_best_practices_refresh_status` reads the docs.claude.com snapshot freshness + unreviewed changes. Read-only; no fetch in the invariant. Surfaces in weekly audit (Sun 11 AM EDT).

**Verified-patterns catalog promotions (Round 140 / `memory/verified-patterns.md` 9 → 12 entries).**

- **A — Closing-move record + paired-truthfulness invariant** (5 instances in-project)
- **B — Accept-all-shapes alternation parser regex** (3 instances; Tacitus parser)
- **C — Catalog-as-visible-enumeration + closing-move-atomic row-add** (3 instances)

Filed deferred: D — Chokepoint helper + window-exposed trigger primitive + paired routing invariant. Promote when a 2nd chokepoint instance lands OR user confirms recurrence expectation.

**Dashboard runtime change.** `buildGoalDrivenRecommendedItems` in `dashboard/dashboard.html` (Round 141 ship) now wires synthetic items to `getRegimenLabelLookup()` (Round 75 Pass A primitive). All goal-driven recommendations carry full Youngevity-label nutrient profiles. Round 154's `isGoalDriven` Adopt-modal branch retired — all recommendations flow through unified `lcScan` verdict path. The runtime change is the canonical instance of an emerging architectural commitment (logged in `memory/essence/decisions.md` Round 155): any synthetic regimen item MUST look up the product name in `getRegimenLabelLookup()` before returning.

## Why this is a minor bump, not just an editorial round

Per `memory/versions.json` `bump_policy`: *"Brain minor bumps per protocol/discipline/tool-surface change."* Round 155 added 2 new invariants + 1 new tool + 3 new verified-pattern recipes + 1 new architectural commitment (synthetic-items-wire-the-lookup). All four belong to the "discipline/tool-surface" category. Minor bump is correct.

## Failure modes this version cures

- **Allowlist-only blindness to new family instances** — Round 149 `esc()` slipped through Round 135's forward check. Round 155 reverse-scan would have caught it at audit time. Future cross-IIFE bare references against any IIFE-private symbol surface as warnings on the next daily audit run.
- **Reference-standard staleness in `claude-best-practices.md`** — the file is manually maintained; Round 142 C-B installed the freshness floor (mtime + cadence threshold). Round 155 ships the ceiling: weekly docs.claude.com fetch surfaces actual content drift, not just lapsed maintenance. Closes failure mode #6 from `memory/vision-living-system-kernel.md` Section 2.
- **Synthetic regimen items with empty `nutrients[]`** — Round 141 shipped the goal engine with hardcoded `nutrients: []`. Round 154 worked around the empty array with the `isGoalDriven` Adopt-modal bypass. Round 155 cures at the source: the engine wires the lookup so all downstream consumers (computeSlotStats, periodic table, lcScan verdict) see real data.

## Next session orientation

Catch-up trigger reads this file via the brain catch-up sequence. The brain prompt (`current.md`) is unchanged at v3.22; the version reflects the support-layer additions. Next operational fire is Monday 2026-06-22 at 3:48 AM EDT (Cura) — exercising the Round 140-147 prompt edits for the first real time on real substrate.

— Round 155, Saturday wrap
