# QOL / UX final-ship audit — status ledger

_The final pre-ship pass Luneth ordered: "find every last QOL / UX / logic-loop / dead-end failure and
fix it." This is the master ledger. **Round-closed 2026-08-15** with 23 items shipped + verified; the
remaining 30 each carry a LOCKED disposition below, so the next session executes — it does not re-decide._

Found by a 13-finder adversarially-verified audit (60 verified → 50 deduped) + the 3 issues Luneth named.
Every fix went: confirm finding vs live code → `safe_write` → `build.mjs` → `tsc` → eslint-vs-HEAD (no new
errors) → touched-surface probe(s) → invariants board 91/91.

---

## DECISIONS — LOCKED (do not re-ask)

1. **Scanner shelf** → **Two lists**: an auto *Recent captures* (last 5, dedup-fixed) **+** a durable
   *Saved* shelf. (Shipped.)
2. **Data safety** → **Guard all three**: confirm before Import + before Reset-identity; Undo toast on
   single-item removal. (PROF-01, PROF-03, REG-02 — TODO.)
3. **Fake controls** → **Wire the intended, cut the redundant**: make ⌘1/2/3, `/`, the green search
   button, and history rows real; remove the duplicate "Continue as guest". (NAV-03, KNOW-03, ASK-02,
   SCAN-05[done], PROF-08 — rest TODO.)
4. **Goal picker** → **Veil everywhere**: Regimen's "+ Add goal" opens the full-screen veil like
   Coverage; the veil gains Esc / backdrop / X cancel that does **not** write goals. (NAV-06, NAV-05,
   NAV-01 — TODO.)
5. **Scanner scope** → **Full coherent fix** (durable shelf + clickable history + neutral couldn't-read
   + editable name). (Shipped as SCAN-01..07.)

**Proceed-on-default (decided, unless Luneth redirects):** REG-05 subtitle → "ranked by your biggest
gaps"; REG-11 container noun → **"Save"** everywhere; PROF-06 name cap → unify to **24**; REG-04
slot-name cap → **40** + live counter (no silent truncation); REG-10 → inline "not found" hint; REG-06 →
build the Duplicate-slot control; KNOW-01 → hide the per-tab search box while a detail is open; KNOW-04 →
origin-aware back; ASK-04 → add "Back to search"; FOOD-01 → drop "Processed Meat" from the Absorption
curation; PROF-05 → data errors inline near the buttons; PROF-09 → "Backup saved" confirm + error path.

---

## DONE — 23 items, shipped + verified green (board 91/91 throughout)

| ID | What shipped |
|----|-------------|
| **COV-01** | no-goal recommender case-join fixed (coverage.ts + regimen.ts) — empty-regimen user no longer told "everything covered". **New gate**: coverage probe now runs the no-goal branch. |
| **COV-02** | clear stale `body.focusing` atop `render()` — removing a goal no longer freezes the field dimmed. |
| **ASK-01** | search repaint memo key includes the query — no-match state updates as you keep typing. |
| **ASK-03** | best-answer hero gets `data-sr-claim` — in-answer "see also" jumps to it resolve. |
| **PROD-01** | "1 serving" pluralization on grid cards. |
| **PROD-02** | product cards derive "of 90" from `essentialCount()`. |
| **NAV-04** | bare-key S/K no longer toggle a drawer behind the veil/profile modal (knowledge probe updated to dismiss veil first). |
| **KNOW-02** | record keyword-filter shows "— no claim matches X —" instead of a blank gap. |
| **KNOW-05** | tapping a 2nd glossary term switches to it (tracks active element) instead of just dismissing. |
| **KNOW-06** | Explore group headers hide when all their chips are filtered out. |
| **NAV-02** | per-workspace scroll position restored on tab switch. |
| **REG-07** | slot-delete Undo at the slot cap surfaces the refusal instead of a silent no-op. |
| **PROF-02** | typed name commits on close (Esc/backdrop blur), not just blur/Enter. |
| **NAMED-2** | regimen readout squares sorted covered→goal-gap→open + per-element `title` + honest hover pop (regimen-only; Coverage already had tiles). **Luneth confirmed looks right.** |
| **NAMED-3** | profile avatar grid: Upload tile leads, default(initial) follows. **Luneth confirmed looks right.** |
| **SCAN-01** | neutral "Couldn't read this label" state (no reads) instead of a false red REJECT (§00.A). |
| **SCAN-02** | editable product-name field in Confirm + `humanizeName` (never raw `aluminum_can`); name flows to Saved rows + regimen. |
| **SCAN-03** | history dedup no longer collapses distinct low-cardinality-named scans (drops name-filter; unique id keeps each). |
| **SCAN-04** | durable Saved shelf (`lcSavedScans_v1`) — "Save for later" persists; panel always visible incl. idle/refresh. |
| **SCAN-05** | Recent + Saved rows clickable → re-open the verdict (re-scored); saved rows have a × to remove. |
| **SCAN-06** | paste-guard — no hidden background OCR when the Scanner isn't on screen. |
| **SCAN-07** | Confirm surfaces a scoring failure inline instead of a dead button. |
| **(gate)** | `render_probe_profile.js` was STALE (console rebuild renamed `.pf-log-entry`→`.pf-logentry` etc.) — updated; it now validates the Creator's Log renders **843 entries** from the build embed. The log was never broken; my earlier "log doesn't render" was wrong and is corrected here. |

**⚠ Scanner — Luneth flagged SEVERAL issues** on the shipped redesign (layout AND flow — the Confirm
step now shares the left column with the persistent rail, plus more he'll enumerate). **Get his specific
list first next session, then fix** — do NOT assume it's only the cramped Confirm column. Functionally
verified (5 scanner probes PASS); the visual/QOL pass is owed on his #1 feature.

---

## TODO — 30 findings, each with its LOCKED disposition (evidence = file:line)

### Scanner
- **SCAN-08** — Confirm "N mapped · U to check" count goes stale as rows are fixed in place → recompute on nfix. (scanner.ts:288) _Deferred this session as minor._

### Regimen
- **REG-01** _major_ — Undo toast destroyed by the next re-render → host the undo bar OUTSIDE the re-rendered `.ck` subtree (create once in mount). (regimen.ts:685,967)
- **REG-03** _major_ — adding a product already in the slot double-counts coverage → dedup by case-insensitive name, bump dose via saveRgOverride. **Two sites: regimen `addItem` + scanner adopt handler.** (regimen.ts:253; scanner.ts adopt)
- **REG-02** _major_ — single-item × removal has no undo, lands in a trash no screen surfaces → **Undo toast reusing restoreFromTrash** (decided). (regimen.ts:912; coverage.ts:705)
- **REG-08** _polish_ — full innerHTML rebuild after each add drops input focus + closes goal menu → re-focus the add input after add. (regimen.ts:937)
- **REG-09** _minor_ — saveRgManual/Removed/Override swallow the WriteResult → thread it back so a failed save doesn't show false success. **Touches the §31 chokepoint — do carefully, it's the critical gate.** (regimen.ts:353)
- **REG-04** — slot rename silently truncates at 17 vs schema 40 → cap at **40 + live counter** (decided). (regimen.ts:758; schemas/regimen.ts:74)
- **REG-05** — "Best next moves — ranked by your goals" shown with no goals set → **"ranked by your biggest gaps"** in no-goal mode (decided). (regimen.ts:679)
- **REG-06** — `duplicateSlot` fully built + window-bridged, zero UI → **build the Duplicate control** on the filled-slot tile (decided). (state/regimen.ts:436)
- **REG-10** — typing an unmatched product + Enter does nothing silently → **inline "not found" hint** (decided). (regimen.ts:936)
- **REG-11** — one concept named 5 ways (Slot/Save/Stack/Active stack/regimen) → standardize on **"Save"** across copy/aria (decided). (regimen.ts:315)

### Coverage
- **COV-03** _polish_ — goal chip body has pointer cursor + hover glow but only × acts → **drop cursor:pointer** from `.gchip` body (decided; keep on × and +Add). (workspace-coverage.css:890)

### Profile
- **PROF-01** _major_ — Import overwrites saved data (regimen incl.) with no confirm, then reloads → **confirm dialog** before restore()+reload (decided). (profile.ts:415; storage.ts:234)
- **PROF-03** — Reset identity drops name + uploaded photo instantly → **confirm + warn photo is discarded** (decided). (profile.ts:501)
- **PROF-04** — "32 graphics" hardcoded → derive `presetIds().length` (clear-fix). (profile.ts:172)
- **PROF-05** — upload/import errors render in the hero name block, far from the control → **surface inline near the data buttons** (decided). (profile.ts:167)
- **PROF-06** — name cap 18 (veil) vs 24 (profile) → **unify to 24** via a shared const (decided). (profile.ts:56; welcome.ts:49)
- **PROF-07** _polish_ — theme Cream/Charcoal toggle is the only control with no `:hover` → add one (clear-fix). (dashboard.css:274)
- **PROF-08** _polish_ — "Continue as guest" duplicates "Done" for a guest → **remove it** (decided). (profile.ts:208)
- **PROF-09** — Export gives no success/failure feedback → **"Backup saved" confirm + error path** (decided). (profile.ts:383)

### Knowledge / entity
- **KNOW-01** _major_ — stale list-search filter survives a pill drill-through → **hide the per-tab search box while a detail is open** (decided). (knowledge.ts:299)
- **KNOW-03** — the `/` kbd hint in Knowledge search is never wired → **wire `/` to focus it** (decided; fake-controls). (knowledge.ts:304)
- **KNOW-04** — essential/condition back buttons not origin-aware → **make origin-aware** like the product back button (decided). (knowledge.ts:764)

### Ask-Wallach / Search
- **ASK-02** — green search-glyph looks/hovers like submit but is inert → **wire it** (focus/re-run) (decided; fake-controls). (search.ts:478; drawer-search.css:143)
- **ASK-04** _major_ — "Learn More" destroys the search query/results with no way back → **add "Back to search"** return path (decided). (main.ts:267; search.ts:596)

### Navigation / shell / first-run
- **NAV-01** _major_ — re-opening the goal picker stacks click listeners; stale closures rewrite profile + goals → **unmount the veil before remount** (clear-fix; part of the goal-picker batch). (main.ts:467)
- **NAV-03** — rail advertises ⌘1/⌘2/⌘3 wired to nothing → **wire real workspace shortcuts** (bind to keys that exist on the Windows portable build) (decided; fake-controls). (dashboard.html:70; main.ts:247)
- **NAV-05** — re-opened goal picker has no Esc/backdrop/X cancel → **add cancel that leaves prior goals untouched** (decided). (welcome.ts:180)
- **NAV-06** — identical "+ Add goal" opens a veil on Coverage vs an inline dropdown on Regimen → **veil everywhere** (decided). (coverage.ts:702; regimen.ts:857)

### Products / Foods
- **FOOD-01** _major_ — "Processed Meat" food card is a dead-end (opens nothing, bounces to top) → **drop it from foods-curation.json remove[]** (decided). (foods-curation.json:13; knowledge-topic.ts:107)

---

## Suggested next-session order

1. **Regimen correctness/undo batch** — REG-01 (undo bar survives re-render), REG-03 (dedup both sites),
   REG-02 (item undo), REG-08 (focus), then REG-09 (WriteResult — careful, §31 gate). Screenshot review.
2. **Profile batch** — PROF-04, PROF-07 (clear), PROF-01/03 (confirms), PROF-05/06/08/09. Screenshot review.
3. **Goal-picker / veil batch** — NAV-01 + NAV-06 + NAV-05 together (they share the veil system). Screenshot review.
4. **Fake-controls batch** — NAV-03, KNOW-03, ASK-02. Screenshot review.
5. **Copy/nav cleanup** — REG-04/05/06/10/11, COV-03, KNOW-01/04, ASK-04, FOOD-01, SCAN-08.
6. **Final sweep** — full board + all probes + Luneth's visual pass on the scanner layout.

## Notes / caveats carried forward

- **Pre-existing eslint debt** (NOT introduced this pass, verified vs HEAD): main.ts 6, search.ts 2,
  entity-page.ts 18, knowledge.ts 1 — style-rule errors (import-order, blank-lines, prefer-template,
  etc.). `eslint --fix` is banned; hand-fix if/when addressed. The "eslint clean" claim in an older
  handoff was inaccurate.
- **Line endings**: the git `stash`/`pop` during the log investigation normalized several touched files
  CRLF→LF (git `autocrlf=input`). This ALIGNS them to the repo's canonical LF; harmless, functionally
  identical, byte-verified through `safe_write`.
- **eden/ untouched** — no corpus/catalog seal applies or was run.
