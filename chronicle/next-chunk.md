# Next chunk — ★ AUTHORITATIVE HANDOFF (set 2026-07-29, after ZINC's header SHIPPED)

# ⚠ HOW THIS SESSION STARTS
Luneth types `genesis` HIMSELF, in a NEW session. Claude runs `tools/genesis.py` ONLY in response
to that — never as a self-initiated in-session "reboot" ([[reboot-after-boot-file-changes]]).

# ★★★★★ READ FIRST (plain language)
Board **78/78 green · corpus kv432 · 2267 sealed claims · repo clean + pushed**.
**Zinc's element header is LIVE** — element #2 of 90, signed off by Luneth. Two down
(copper, zinc) plus selenium's earlier one. The task now is **the remaining 88**, one at a time.

## ★ BEFORE YOU TOUCH ANYTHING: read `.claude/rules/element-headers.md`
It now carries **8 rules**, not 6. Rules 7 and 8 are new and both came from zinc:
- **Rule 7** — grep a class name before claiming it, and keep the regression pass. Zinc's opener row
  was first named `.kd-ep-fam__hook`, a class that ALREADY existed as the per-beat payoff line;
  same ID specificity, mine later in the file, so it silently restyled selenium's AND copper's beat
  hooks. **Zinc's own page looked perfect** — only the copper/selenium regression checks found it.
  Also: containment must be STRUCTURAL (a `clipPath`, not an inset).
- **Rule 8** — "slightly clearer" is NOT a licence to redesign. Change LABELS before geometry; beat
  prose ceiling is **2 sentences**. Memory [[measured-change-not-extremes]] was updated with both
  instances from this session.

## The workflow (Luneth's, unchanged)
**4 genuinely distinct mockups** in the real container → **Luneth picks** one or a mix → build the
winner live → **STOP for his visual sign-off**. He drives. He also picked the element by EVIDENCE
this time and liked it: every sealed claim for 6 candidates was read before choosing.

## What ZINC added (reuse, don't re-derive)
- **2 new schema blocks**, both OPTIONAL + self-suppressing: `hook` `{figure,text,pivot}` — an
  opener row (small figure + 1 line + 1 italic pivot) that starts the block on something the reader
  can check on their own body; and `coda` (string) — one closing line returning to what the hook
  opened on. Selenium/copper/omega/pdm render byte-identically.
- **2 new figure keys**: `nail_spots` · `metal_fingers`. Live keys are now
  `rancidity · cofactor_fork · decline_rail · reversal_rail · nail_spots · metal_fingers`.
  **Copper owns fork + both rails**, so a new fork- or rail-shaped figure will read as a recolour.
- **CSS**: `__opener*` · `__coda` · `__skin/__nail/__nailline/__ntip/__nlun/__nspot` ·
  `__strand/__znode/__zstem/__gbar/__zdiv` · `__gglyph--sm`.
- **17.6px is a CEILING, not a floor.** Zinc's glyph ships at **14.6px** because its node is smaller
  than copper's; `figure_type_within_standard` only fires above 17.6. Do NOT "restore" it.
  The `--sm` modifier needed ID specificity — the cascade trap applies to font-size, not just width.
- **Probe**: `tools/render_probe_zinc.js` (57 checks) is now the template — copy it per element and
  KEEP its regression pass on the already-shipped elements, asserting they did NOT gain the new
  block (`!cu.hook && cu.coda === ''`), not merely that they still render.

## ⚠ TWO ZINC NUMBERS THAT CANNOT BE USED YET (found + rejected, do not re-reach for them)
- **98%** birth-defect prevention (`WAL-CLM-DDDL-000042`) — in our `claim_text`, in NO verbatim.
- **spina bifida "the other 50%"** (`WAL-CLM-RARE-000326`) — the sealed verbatim STOPS DEAD
  mid-sentence before attributing that half to zinc. The severed-quote signature
  ([[severed-quote-signature]]). Both need RE-MINING before either figure can appear anywhere.

## OPEN DEFERRAL (carry forward)
**`entity-copy.json` holds 4 of 91** (calcium · selenium · copper · zinc). The other **87 have
neither** a `lede` nor a why-this-number line. `element_header_complete` binds only on elements that
HAVE a header, so this remainder is a labelled WISH, not a covered gap. Luneth wants both on EVERY
element ([[daily-target-provenance-always]]); each `why` derives from the documented transform chain
in `essentials-targets-data.json`. Rides along per element, or wants a dedicated pass — his call.

## LOAD FIRST next session
- `.claude/rules/element-headers.md` (8 rules) + `.claude/rules/visual-verification.md` (the STOP gate).
- The live gold standards in `views/entity-page.ts`: `renderMechanism` (composed) ·
  `nailSpotsFigure` · `metalFingersFigure` · `cofactorForkFigure`, and their CSS in `drawer-knowledge.css`.
- Mockup shells to COPY, both preserved in `temporary/`: `zinc-header-B3.html` (the approved zinc
  build) · `copper-header-combined.html`. Also kept for reference: `zinc-header-mockups.html`
  (the 4 options) and `zinc-header-B2.html` (the REJECTED overhaul — useful as a negative example).
- Memories: [[element-header-playbook]] · [[measured-change-not-extremes]] ·
  [[header-mockups-in-real-container]] · [[illustration-type-scale]] · [[category-color-coding]] ·
  [[element-intro-what-is-claim]] · [[daily-target-provenance-always]] · [[element-sources-at-bottom]] ·
  [[claim-text-numbers-unguarded]] · [[severed-quote-signature]] · [[outside-knowledge-injected-as-wallach]].

## STANDING DOCTRINES (unchanged)
1. Every claim lives in ONE of three homes (essentials/conditions/Explore); search is a retrieval layer.
2. Diet not food; nutrients from the DIET (food OR supplements). 3. NEVER fabricate — verbatim ⊆ sealed book, or GAP.
4. `corpus_seal` + `catalog_seal` are USER-ONLY (per-invocation authorization). 5. Memory index: consolidate only past ~200 lines ([[memory-consolidation-threshold]]).

**Corpus kv432 · 2267 claims · board 78/78 green · repo clean + pushed. Next: pick the THIRD element, read the 8-rule playbook, run the 4-mockup process.**
