# Next chunk — operating system CLEANED → continue **Phase 2** surfaces

## LATEST (2026-06-24) — Wallach Knowledge Revamp **Phase α SHIPPED** (commit 01669ee): Eden two-tier / three-wing · sealed corpus + graphics + the 90-canon

- **The big pivot is live.** Eden is now the single sealed source of ALL Tier-1 Wallach truth, in **three wings**: Wing 1 `eden/eden-catalog.json` (YGY catalog, untouched), **Wing 2 `eden/corpus/`** (Wallach books + the extracted claim graph — NEW), **Wing 3 `eden/graphics/`** (Luneth's 5 hand-made Wallach-derived graphics — NEW, Tier-1 by his source-owner authority). **Tier-2 `knowledge/`** is now only `transcripts-clean/` (the Sunday YouTube-scan feed) + `design-wisdom/` — unsealed, advisory, kept OUT of Eden so it can't poison the canon.
- **Proposal of record:** `chronicle/proposals/wallach-knowledge-revamp.md` (approved). **Schema spec:** `eden/corpus/SCHEMA.md`. **Architecture:** `eden/corpus/README.md`. Read these before Phase β.
- **What's sealed (knowledge_version=2, board 29/29):** 6 books in-housed at `eden/corpus/books/` (LF-normalized, hash-anchored via `books-meta.json`); `essentials-canon.json` = the immutable 90 (60 min / 16 vit / 12 amino / 2 fatty; Omega-9 excluded); 5 graphics + `graphics-manifest.json`. Three NEW invariants: `corpus_integrity` (delegates to `corpus_verify.py` = the SINGLE impl of 10 checks), `corpus_runtime_purity` (0 LLM/network markers in `dist/main.js`), `graphics_integrity`.
- **Tools (`eden/tools/`):** `corpus_verify.py` (read-only, 10 checks, the truth gate), `corpus_seal.py` + `graphics_seal.py` (write goldens, bump version — Luneth waived the user-only ceremony, agent may run them), `corpus_extract.py` (deterministic DRY pre-scan now; draft emission is Phase β), `corpus_derive.py` (stub; full per-axis derivation is Phase δ). Golden siblings are `<name>.json.golden.sha256` (this exact form = free `pre_write_guard` auto-block — do NOT use the stem form).
- **TWO open canon adjudications are now formally tracked** in `essentials-canon.json` `pending_adjudications` (resolve in Phase δ): (1) **Cysteine vs Taurine** (layout has Cysteine — flagged `provisional`; Wallach's graphic has Taurine); (2) **Fluoride re-adjudication** (removed on a suspected hallucinated-citation basis; re-confirm against the actual books). The corpus audit is the place these get ruled.
- **Locked engineering decisions** (don't re-litigate): claims **sharded per book**; **verbatim is the load-bearing anchor** (PDF books lack reliable page markers — `content_sha256` over LF-normalized text, `.gitattributes eol=lf`, clone-stable); **agent-in-the-loop extraction** (NO LLM subsystem — determinism from seal+hash); `core/schemas/corpus.ts` **deferred to Phase ε** (no runtime consumer yet; SCHEMA.md + corpus_verify are the single schema authority until the dashboard loads claims).
- **NEXT ORDER — continue Phase β (DDDL at volume).** **β.1 SHIPPED:** the extraction pipeline is live + the first **14 DDDL claims are SEALED** (knowledge_version=5, board 29/29; voice LOCKED = neutral-declarative, Luneth-approved). Pipeline CLI is now subcommands: `python eden/tools/corpus_extract.py prescan|finalize --book <id> [--raw <raw.json>]`. Flow: the agent authors claim SEMANTICS (kind / slugs / claim_text / verbatim) into a raw-batch JSON → `finalize` SNAPS each verbatim to EXACT book bytes (whitespace-collapse + quote/dash fold + norm→orig index map, so mid-sentence line-wraps + curly quotes are fine), assigns `WAL-CLM-<SHORT>-NNNNNN` ids + char_offset, validates essentials slugs against the 90-canon, emits the draft + `drafts/reports/<book>.report.md` → `corpus_seal.py` promotes to `claims/` and `corpus_verify` proves every verbatim is a real book substring (check #2). `finalize` MERGES the already-sealed shard so multi-batch extraction never drops earlier claims. **Luneth is reviewing the first batch's claim format/quality** (`eden/corpus/drafts/reports/dddl-3e-2011.report.md`) — continue DDDL at volume in review-sized batches (dose kind now in use; review can be lighter now the format's locked), then **Phase γ** the other 5 books (one at a time, review+seal each), then **Phase δ** derive the indices (essentials/conditions/symptoms/consistency — `corpus_derive.py` is still a stub). Condition/symptom slugs in claims are free strings now; they become the Phase-δ index keyspace. The two canon adjudications (Cysteine↔Taurine, Fluoride) get ruled during the audit pass.
- **Method (locked):** §17 all project writes via `safe_write` (never Edit/Write); stage payloads with template backticks to AppData via Write tool; LF payloads for `replace`. Round-close = build → invariants (29/29) → build-log → Creator's Log → commit + push. Cleanup of the legacy `knowledge/` layers (→ `C:\Users\Light\Desktop\claude\legacy-wallach-knowledge\`) is **Phase η**, gated on migration — do NOT delete anything in `knowledge/` until its last consumer is migrated.
- **Coverage/dashboard surfaces are UNCHANGED** by Phase α (foundation only). The earlier surface-polish next-orders below still stand as parallel tracks if Luneth redirects.

## EARLIER (2026-06-24) — 90-essentials correctness SHIPPED: Germanium replaces Fluoride · count unified at 90 · all names full · vitamin/amino tiles → mineral format

- **The Germanium/Fluoride conflict is RESOLVED (Luneth-ruled).** Wallach's enumerated "90 For Life" graphic (4×-confirmed: Hell's Kitchen, Rare Earths, DDDL 2011 EPUB, Dirobi/Harada) lists exactly **60 minerals — Germanium IN (slot 20), Fluoride NOT.** Both were individually Wallach-essential, but Luneth ruled: **Germanium takes Fluoride's slot; Fluoride DELETED from the live essentials system entirely** (suspected to originate from an earlier-Claude hallucinated conversation about a fluoride transcript — to be re-adjudicated against the actual books later). Full record + open items: **`chronicle/contradictions/2026-06-24-germanium-replaces-fluoride.md`**.
- **Fluoride scrubbed from 5 live data files**; Germanium tile added to the layout. Canonical now 60 minerals; embed 91 entries; `wallach_stance_embed_sync` = 91 byte-equal. `knowledge/_wallach_stance_candidates.json` LEFT intact (raw corpus-search audit material for the re-eval). Saga/eval/books = history, untouched.
- **The 91/92→90 sweep is DONE.** Single source: **`essentialCount()` in `state/coverage.ts`** (layout tiles where `essential !== false` = 90). recompute() now excludes non-essential (Omega-9) from totalCount/coveredCount. Hero (`/90` + kicker), section sub-counts, `scanner.ts`, `regimen.ts` mock slots all read the helper. JSDoc "92 essentials" comments corrected. No `92`/`91` literals remain.
- **Full names everywhere (Luneth mandate — NO abbreviations ANYWHERE).** All 38 abbreviated layout display names expanded (`MAGNES.`→`MAGNESIUM`, `SILICON`→`SILICA`, `PANTO.`→`PANTOTHENIC ACID`, `PHENYLAL.`→`PHENYLALANINE`, …). Tile CSS widened (`auto-fill minmax(82px)`, `aspect-ratio:auto`+`min-height`, `overflow-wrap`) so full names fit; 0 clipped (objective puppeteer check).
- **Vitamin + Amino tiles unified to the MINERAL format** (Luneth request): `renderTile` now emits `code` before the glyph → code top-left (like the atomic number), letter/abbr centered at 17px (like `.tile__sym`), 9.5px name strip. This ALSO fixed the vitamin name-strip left/right **gap** (strip now bleeds to tile edges via `margin: auto -5px 0`, matching minerals). All three grids identical (minmax 82px). Verified via 2× section screenshots.
- **Board 26/26, build OK, all 5 probes green** (coverage/seeded/knowledge/scan/adopt, 0 page errors). One stale probe locator fixed (`render_probe_seeded.js` matched old `ASCORBIC`/`ALUMIN.` tile text → updated to full names).
- **Luneth verified the count/Germanium visuals ("Looks great") + directed the full-name & tile-format work; authorized round-close after this last adjustment and wants a FRESH `genesis` next session.**

- **NEXT ORDER (pick one, build in visual-verified phases → STOP for Luneth → round-close):**
  1. **Dedicated Wallach essentials/books corpus audit** (Luneth's big one — flagged in the contradiction record): re-adjudicate **Fluoride** vs actual Wallach book statements (+ audit the suspected hallucinated-citation provenance), resolve **Cysteine vs Taurine** (the layout aminos list **Cysteine**; Wallach's graphic has **Taurine** — same class of issue as Fluoride, spotted but NOT acted on), then definitively log every essential's source statements and attach to the targets table.
  2. **Coverage hero/region pixel-match** — continue the rest of Coverage vs `dashboard/components/workspace-coverage-v3.2-PROPOSAL.html` via `tools/style_diff.js`. ⚠ The periodic-table tiles now DIVERGE from the v3.2 demo BY DESIGN (full names + wider tiles + Germanium-not-Fluoride); the demo is stale there — do NOT "correct" the tiles back to it.
  3. **CODEX v1.0 version stamp** (3.27→1.0 from `versions-data` + footer + the legacy CL-panel ref).
  4. **Command palette (⌘K)** · 5. **Alien-glyph cipher** (find Luneth's glyph system first).
- **Method reminder (locked):** visual surfaces build in coherent phases → functional gates (build · 26/26 invariants · probe) → **STOP, Luneth visually verifies** → round-close (build-log → Creator's Log → commit + push). New CSS roots every rule under its surface host id (Sever-Safety). §17: ALL project writes via `safe_write` (never Edit/Write). Stage payloads containing template-literal backticks/`${}` via the Write tool to AppData (NOT a bash heredoc — it interpolates). Render-probe locators that match tile display TEXT must track name changes.

## EARLIER (2026-06-24) — Knowledge drawer SHIPPED to gold-standard (full kd-* styling + Essentials deep-dive + 90-essentials/Omega-9 reframe + drawer +100px) [Germanium conflict now RESOLVED above]

- **Knowledge drawer is now gold-standard.** Three sub-chunks, all Luneth-verified:
  1. **Shared-chrome refactor + kd-* styling.** New **`dashboard/assets/styles/drawer-shared.css`** holds the chrome (head/tabs/search/body/footer/close/action/eyebrow/empty) shared by BOTH drawers — every rule dual-selectors `#drawer-journey-mount .jd-*` + `#drawer-knowledge-mount .kd-*` (single source; both rooted at their mount id = no leak). `drawer-journey.css` slimmed to journey-only; new **`drawer-knowledge.css`** = kd-* content. The view's tab-content classes were renamed generic→`kd-*` (`.essential-tile` etc. COLLIDE with legacy-dashboard.css `.essential-tile` + workspace-coverage `.essentials-grid`; kd-* makes it leak-proof, mirroring jd-*). Load order: …workspace-coverage → drawer-shared → drawer-journey → drawer-knowledge.
  2. **Essentials tab rebuilt + deep-dive.** Layout-driven (walks `coverage-layout-data.json`, the SAME source as the Coverage periodic table) → all essentials shown across 4 section heads (no pagination; the old `slice(0,60)` showed minerals only), real symbols, coverage-state colors joined from the **CoverageSnapshot** (`getOrCompute()`, same classifier as Coverage — covered/trace=green, partial/gap=amber, pending=neutral). Click a tile → in-place **deep-dive**: Wallach `wallach_stance.quote`+citation (§00.A educational layer; schema gained `quote`/`context`), status pill, "FOUND IN YGY VAULT" chips resolved via the canonical `matchEssential` (no matcher drift). Probe `render_probe_knowledge.js` now asserts all-shown + deep-dive.
  3. **90-essentials + Omega-9 (§00.A — CONFIRMED, see `chronicle/contradictions/2026-06-24-source-rule-90-essentials-omega9.md`).** Always **90** (Wallach's own "The 90 Essential Nutrients" lectures + Eden "90 essentials"; our own Omega-9 `wallach_stance.quote` literally says "technically non-essential since the body can synthesize them"). **Single source:** `coverage-layout-data.json` Omega-9 tile `"essential": false` (+ `LayoutTileSchema.essential`). Count derives from `essential !== false` → 90. Badge "90 ESSENTIAL", featured-citation "2 essential fatty acids — 90 essentials total", fatty-acids head **"· 2 + 1"**, Omega-9 a teal `--bonus` tile + on-click non-essential flag (coverage math retained). Drawer widened **420→520px** (kills tile-meta wrapping).
- **Board 26/26**, build OK, knowledge+journey+coverage+seeded probes green. NOTHING about Coverage's visuals changed yet.

- **⚠⚠ TOP PRIORITY THIS SESSION — the "91/92 → 90" math + the Germanium conflict (Luneth's explicit ask).** Investigated: the targets-DB **embed** (`essentials-targets-data` in dashboard.html) has **61 minerals → 92 entries**, while `coverage-layout-data.json` + the canonical `knowledge/essentials-targets.json` `totals` both say **60 → 91**. The single extra is **GERMANIUM** (in the embed, absent from the layout periodic table; no duplicates). Germanium is a real Wallach DDDL trace element, so this is a genuine "is it one of the 60, or a shown-but-not-counted trace like Omega-9?" decision — **needs the Wallach 60-minerals-list confirmed, then Luneth ratifies the data fix** (numbers-corrected-at-end is his domain). Two clean resolutions: (a) Germanium is NOT in Wallach's core 60 → mark it `essential: false`-style "shown bonus trace" (like Omega-9) so 90 holds; or (b) it IS one of the 60 and the layout is missing it → add the tile AND drop whatever pushed the count over (still must net to 60). Confirm against corpus FIRST.
- **Every live 91/92 instance to correct to 90 (drive ALL from the layout essential count = 90, never the 92-entry embed):**
  - `views/coverage.ts:131` `const total = snapshot?.totalCount ?? 92` (hero denominator) + `:138` kicker "92 minerals + vitamins…". Hero must read covered/total over the **essential layout set** (numerator AND denominator over the 90; exclude Omega-9 via the flag + exclude Germanium since it's not in the layout). Also the Coverage **Fatty Acids section → "2" (or "2 + 1")** + Omega-9 a distinct non-essential color + its on-click detail (mirror the Knowledge treatment — apply to ALL card surfaces).
  - `views/scanner.ts:340` `+${added}/92`; `views/regimen.ts:61-65` mock slots `total: 92` + `:239` `/ 92` (mock/demo data — fix once the count source is one helper).
  - Comments saying "92 essentials": `state/coverage.ts:5,13`, `core/schemas/knowledge.ts:6`, `core/schemas/coverage-layout.ts:10` ("91 hardcoded tile specs"). Correct WITH the real fix, not before (a comment flipped to 90 while the embed holds 92 would lie the other way).
  - Recommended: add ONE exported helper (e.g. `coverage.essentialCount()` / `essentialTiles()`) so 90 has a single definition every surface reads — no scattered literals to drift again.

- **NEXT ORDER (pick one, build in visual-verified phases → STOP for Luneth → round-close):**
  1. **Coverage 90/Omega-9 + the 91/92 sweep** (above) — the natural continuation; resolves Luneth's correctness ask. Confirm Germanium first.
  2. **Coverage hero region** to pixel-exact vs `dashboard/components/workspace-coverage-v3.2-PROPOSAL.html` via `tools/style_diff.js`, then periodic-table grid + Daily-Protocol sidebar.
  3. **Full CODEX v1.0 stamp** (bump 3.27→1.0 from `versions-data` + footer + legacy CL-panel ref).
  4. **Command palette (⌘K)** · 5. **Alien-glyph cipher**.
- **Method reminder (locked):** visual surfaces build in coherent phases → functional gates (build · invariants · probe) → **STOP, Luneth visually verifies** → only THEN round-close. New CSS roots every rule under its surface host id (Sever-Safety). §17: all project writes via `safe_write` (never Edit/Write). `safe_write` CLI exits clean; calling `safe_write.safe_rewrite` from an imported script lingers ~2 min after the write lands (write still OK — use the CLI).

## LATEST (2026-06-23 night) — Journey drawer SHIPPED to gold-standard (J3 mount + J4 styling) + topbar CODEX rename (commit 7b1e5b8)
- **The LAST legacy teal is dead.** Journey was the only surface still falling back to the legacy `#tab-journey` tab (teal). **J3** generalized the Knowledge-only drawer wiring in `main.ts` into a SHARED K+J registry (`DRAWER_SPECS` → `mountDrawers` / `toggleDrawer` / `wireDrawerKeys` / `closeAllDrawers`); the J rail item + Esc + bare-J now open the new jd-* drawer. Knowledge still flows through the same registry (probe green). **Auto-derive** wired: `scanner:scan-complete` / `regimen` add·remove·restore / `goals:updated` → `journey.logEvent` (deliberately NOT `coverage:recomputed` or regimen `dose-edit` — too high-frequency, would flood the timeline; J1's "fills from real activity, no fake seed" intent).
- **Drawer fully styled to the v3 mockup.** New **`dashboard/assets/styles/drawer-journey.css`** (wired in dashboard.html after workspace-coverage.css) ports the mockup's generic `.drawer__*`/`.tl-*`/`.goal-card`/`.checkin`/`.milestone` vocabulary onto the view's self-namespaced `jd-*` classes. **EVERY rule is rooted at `#drawer-journey-mount`** so it cannot leak (the Sever-Safety containment lesson applied PREEMPTIVELY to new CSS — keep doing this). Covers panel + chrome (head/tabs/search/body/footer) + all 4 tabs (Timeline/Goals/Check-ins/Milestones + the quick-checkin entry button) + inline forms. Built in 2 visual-verified phases (B.1 chrome+timeline, B.2 the rest); Luneth verified each.
- **J4 probe:** `tools/render_probe_journey.js` (13 checks: rail J opens the STYLED drawer, panel 600px, paper-darker head, logged event → styled timeline entry, **legacy host not shown = the teal-kill proof**, tabs/Esc/bare-J, 0 errors).
- **CODEX:** the two visible topbar BRAIN refs renamed (rail sub `v3.27 · CODEX`, telemetry `CODEX v3.27`). **v3.27 KEPT** — consistent with the footer (`WALLACH·SYS v3.27`) + `versions-data` source, NO drift. The legacy CL-panel BRAIN (dashboard.html ~line 258, inside `#legacy-workspace-host`) left untouched (dies at R5).
- **Board: 26/26.** Coverage + Knowledge probes still green (the shared-registry generalization didn't regress K).
- **NEXT ORDER (pick one, build in visual-verified phases → STOP for Luneth → round-close):**
  1. **Knowledge drawer styling** — its shell is STRUCTURALLY IDENTICAL to Journey's (`kd-*` parallel to `jd-*`). It is currently mounted-but-UNSTYLED (the other half of the drawer drift). Lift the shared chrome (head/tabs/search/body/footer/close/action/eyebrow) to target BOTH `kd-*` + `jd-*` (refactor the common rules out of drawer-journey.css), then style the kd- tab content (corpus/essentials/products/doctrine). **`drawer-journey.css` is the template.** Mockup: `dashboard/components/drawer-knowledge-v3-PROPOSAL.html`.
  2. **Coverage hero region** — `THE WHOLE PICTURE` + the NN/92 kill-shot, diffed vs `dashboard/components/workspace-coverage-v3.2-PROPOSAL.html` via `tools/style_diff.js`. Then periodic-table grid + Daily-Protocol sidebar.
  3. **Full CODEX v1.0 stamp** — bump 3.27→1.0 from ONE canonical source (`versions-data` JSON + `tools/version_bump.py`), update the footer signature + the legacy CL-panel ref, no duplicate system.
  4. **Command palette (⌘K)** · 5. **Alien-glyph cipher** (swap `.ds-cipher` human chars for glowing alien glyphs — Luneth said he "developed a glyph system"; find it first).
- **Method reminder (locked):** build a surface in coherent visual-verified phases → functional gates (build · invariants · probe) → **STOP, Luneth visually verifies** → only THEN round-close (build-log → Creator's Log → commit + push). Never chain past the STOP. `tools/style_diff.js` finds visual deltas objectively (don't eyeball). New CSS roots every rule under its surface's host id so it can never leak (Sever-Safety).

## LATEST (2026-06-23 evening) — Sever-Safety: legacy CSS leak CONTAINED + ENFORCED (commit 5220c96)
- **The legacy→v3 sever is now leak-proof.** Audit (Luneth's "total clean cut from the old system" ask) confirmed the sever is clean at the MARKUP level (the whole old dashboard is one `#legacy-workspace-host` div, documented R2→R5 deletion schedule) but was LEAKY at the CSS level with NO enforcement. `legacy-dashboard.css` (loaded AFTER the v3 design system) held **24 bare element/universal selectors** (teal h2/h3/table/th/td, an `@media header`, `*`, html/body) that bleed into the `.app-*` shell the instant a surface uses a bare element — only the 4 that already bit were hand-patched last session. Fixed: scoped the 22 descendant rules under **`:where(#legacy-workspace-host)`** (ZERO added specificity → legacy cascade preserved byte-for-byte; a plain descendant combinator would raise them to ID specificity and flip legacy winners) + collapsed html,body/body onto the host element.
- **CRITICAL catch:** the pixel-exact shell was SILENTLY riding on legacy's leaked `*{box-sizing}` + html,body font-smoothing (the sealed v3 token sheet scopes those to `.ds-canvas` only, which `.app-shell` doesn't use). Moved box-sizing/smoothing/line-height into `dashboard.css` so the shell OWNS them — required so the R5 legacy deletion can't regress it. **Lesson: before scoping a leaked global away, check what the new shell silently inherits from it.**
- **NEW critical invariant `legacy_css_contained`** (board now **26/26**): deterministic re-parse — no bare element / `*` / non-var `:root` selector may EVER exist in `legacy-dashboard.css` again, at any nesting level. Proven with a negative test (passes clean, fails on planted h2/table/*/`:root{color}`/`@media header`). The leak can never silently recur as the file shrinks to its R5 death. **Luneth's principle, now codified in practice: always stop the leak before you continue building.**
- **Verified:** 24→0 leak vectors; `style_diff` vs coverage v3.2 = 4 live-better residuals only (shell visually unchanged); invariants 26/26; render probes coverage+seeded+knowledge 0 page errors. Luneth visually verified Coverage identical, and found teal now appears ONLY on the un-migrated legacy **Journey** tab (the rail falls back to `#tab-journey` because `journeyView` is imported but not mounted yet — contained, NOT a leak; vanishes at J3/J4).
- **NEXT ORDER:** (1) **Journey J3/J4** — mount the J2 `journeyView` (generalize the K-drawer `mount/toggle/wireKeys` into ONE shared K+J helper in `main.ts`; wire the auto-derive subscriptions from `regimen:changed`/`scanner:scan-complete`/`coverage:recomputed`/`goals:updated` → `journey.logEvent`) → this removes the LAST visible teal; then `tools/render_probe_journey.js`. OR (2) the **Coverage hero region** (`THE WHOLE PICTURE` + the NN/92 kill-shot), diffed vs `dashboard/components/workspace-coverage-v3.2-PROPOSAL.html`. Then CODEX version stamp, alien-glyph cipher, command palette.
- **Audit oddities flagged (not blocking):** (a) retired-system name **"BRAIN v3.27"** still live in the rail sub / topbar telemetry / `versions-data` JSON → the queued CODEX v1.0 swap addresses it; (b) `dashboard.html` is a **348K-token monolith** — 8 inline JSON + 5 markdown data blocks (~740 KB) that the file's own header says migrate to fetched files at R5 (tools can't `Read` it whole); (c) pre-existing legacy **wallpaper-jpg 404** (`url("assets/background-3.jpg")` resolves relative to `styles/`), legacy-zone-only, dies with the zone.

## LATEST (2026-06-23 PM) — Coverage gold-standard Phase 1 (shell -> v3.2, Luneth-verified) + visual-match lesson
- **Coverage Phase 1 SHIPPED — the shell is now ~pixel-exact to the v3.2 mockup** (Luneth: "THAT FIXED IT, almost 100% exact"). Found via `tools/style_diff.js` (NEW — objective computed-style diff, live vs mockup; ~50 diffs -> 0 meaningful). Two systemic root causes: (1) `legacy-dashboard.css html,body{font-size:15px}` -> whole rem UI at 93.75%; removed -> 16px root (re-scaled the ENTIRE coverage page). (2) legacy BARE element selectors (header/footer/html,body) bled into the `.app-*` shell (14px radius, teal shadow/border, a `header::before` veil over `.app-topbar`); scoped to `#legacy-workspace-host` (grep `[v3-contain]`). ALSO in-housed the v3.2 fonts (Chakra Petch + Bruno Ace — wired but never procured -> 404 -> Space Grotesk fallback). Lesson codified: `.claude/rules/visual-verification.md` "Getting to exact".
- **Method now locked (visual-verification gate):** build a surface/chunk -> functional gates -> **STOP, Luneth visually verifies** -> only then commit. Never claim matched without his eyes. He is the test step. `tools/style_diff.js` is how you find the deltas (don't eyeball).
- **Also shipped this session:** Journey J2 (7595a57); Creator's Log timestamps -> machine-local/auto-DST (c2d9c95); the visual-verification gate rule (02fc9c0).
- **NEXT ORDER (Luneth):** CONTINUE Coverage to ~pixel-exact, surface region by region, STOPPING for visual verification each phase. (1) Coverage hero (`THE WHOLE PICTURE` + kill-shot NN/92), then periodic-table grid + tile states, then Daily-Protocol sidebar — diff each vs `dashboard/components/workspace-coverage-v3.2-PROPOSAL.html`. (2) **CODEX version** — replace ALL `BRAIN v3.27` (rail sub, topbar telemetry, footer signature) with `CODEX v1.0` from ONE canonical version source, no duplicate system (find the existing source). (3) **Alien-glyph cipher** — swap the `.ds-cipher` human-char sets (0-9/A-Z) for minimalist glowing alien glyphs (Stargate Atlantis/SG-1 vibe) in theme colors; Luneth said he "developed a glyph system" — find it first. (4) Then Journey J3/J4, palette.
- **Audit done (Opus 4.8):** the Creator's Log works; its sacred guarantees are STRUCTURAL, re-proven live. Board is now **25/25**: `creators_log_well_formed` + `creators_log_append_only` (critical, git-anchored) + `creators_log_digest_synced` + `creators_log_embed_synced` + `creators_log_archive_synced`.
- **Canonical path is `chronicle/creators-log/log.jsonl`** (+ generated `LOG.md` + `README.md`). The old flat `chronicle/creators-log.jsonl` path is GONE — ignore any older mention of it below.
- **Chunk H shipped (commit 5c63212)** — 3 audit fixes: (4a) digest-spoof closed — `validate_entry` rejects newline summaries + `render_digest` escapes a leading #/> and flattens newlines so the human view can't show a forged entry; (5a) delete-guard now covers the whole `chronicle/creators-log/` dir + `rm -rf chronicle` + a dir `mv`; (5b) a COMMITTED deletion is a hard RED ('SACRED LEDGER REMOVED FROM HEAD') + git-unavailable is a loud fail-open ('⚠ UNVERIFIED').
- **README audit (54be426)** — all 16 READMEs verified accurate; 0 retired-system tokens (tacitus/cura/aegis/vision/brain) in any README. **Glossary fix (a07e164)** — CLAUDE.md dropped Cura/Aegis + slimmed the Tacitus guard; also fixed a pre_bash_guard regex that spanned a separator into an unrelated short-flag.
- **L2 SHIPPED (99215fe) — DASHBOARD Creator's Log.** The CLI ledger is inlined at build (`creators_log.py write_embed()` → `dashboard/assets/data/creators-log-embed.json`; esbuild JSON import in `state/log.ts`) and boot-merged with the LS `wallachCreatorsLog_v1` entries (dedup by id) so the Profile panel shows BOTH. Probe: `tools/render_probe_profile.js`; invariant `creators_log_embed_synced`. The req-3 VISUAL truth-verification layer is LIVE.
- **Chunk N SHIPPED (088ab1c) — navigability archive-tree.** `INDEX.md` (month-by-month map: count + kind tally + digest link) + `digests/YYYY-MM.md` (full history per month) generated over `log.jsonl`; `LOG.md` is now a recent-window view (cap `DIGEST_RECENT=200`). New invariant `creators_log_archive_synced`. `log.jsonl` stays the unsharded canonical spine. The Creator's Log system is now COMPLETE: L1 (CLI mirror) + hardening (4a/5a/5b) + L2 (dashboard boot-merge) + N (navigability). Deferred: cap the L2 embed to recent-N once it grows large.
- **Journey J1 SHIPPED (bb08e5f) — state engine.** `core/schemas/journey.ts` (Zod) + a real `state/journey.ts`: `logEvent`/`listEvents`/`logCheckin`/`listCheckins` (§31 chokepoints to `wallachJourneyEvents_v1`/`wallachJourneyCheckins_v1`, emit `journey:changed`, FIFO cap 5000), `crossRefForCheckin` (±7-day walker), all Zod-validated; `journey:event-logged` → `journey:changed`. `state/journey.ts` (J1) and `views/journey.ts` (J2) are both real now. No fake seed (reads empty; fills from real activity). Functionally smoke-tested.
- **Genesis boot system SHIPPED** — type **`genesis`** → `tools/genesis.py` prints the boot report (banner + invariants/build-parity/Creator's-Log/build-log scoreboard + THIS live pass-off) and Claude ends with the action question (never flair-only). `sunjo/` was renamed → `genesis/` (boot-system home + archived original pass-off); the LIVE rolling pass-off stays `chronicle/next-chunk.md`. CLAUDE.md "Genesis" documents it.
- **Journey J2 SHIPPED — `views/journey.ts` drawer view.** Replaced the throwing scaffold with a real 4-tab renderer (Timeline / Goals / Check-ins / Milestones) using self-namespaced `jd-*` classes (parallel to Knowledge's `kd-*`; the v3-proposal's generic `.timeline`/`.goal-card`/`.milestone` collide with `legacy-dashboard.css` — `jd-*` CSS styling is the Round-6 polish pass). Reads ONLY via the state layer; zero inline literals (day-stamp uses `toLocaleDateString`, not a 12-elem month array). Two mutation hooks: the footer LOG EVENT + the Check-ins quick-entry → inline forms calling `journey.logEvent()`/`logCheckin()` (bounded inputs per §00.B #8: maxlength + slice + clampSeverity + `EventKindSchema.safeParse`). ALSO implemented the READ side of the `state/goals.ts` scaffold (new `core/schemas/goals.ts` with GoalSchema/MilestoneSchema using `.optional()` not `.default()` so input==output types; Zod-validated `listGoals`/`listMilestones` → bad LS empties; `evaluateMilestoneTriggers` stays a deferred throw). tsc strict + eslint clean on all 4 files; invariants 25/25; coverage + knowledge probes green. The view COMPILES but is tree-shaken from the runtime bundle until J3 calls `mount()` (its `jd-`/"LOG EVENT" markers are absent from dist BY DESIGN; tsc is the compile gate).
- **NEXT ORDER (Luneth):** (1) **J3** — generalize the K-drawer mount/toggle/keys (`mountKnowledgeDrawer`/`toggleKnowledgeDrawer`/`wireKnowledgeKeys` in `main.ts`) into ONE shared K+J helper; mount `journeyView` into `#drawer-journey-mount` + J rail toggle + Esc + bare-J; AND wire the auto-derive: subscribe journey to the existing `regimen:changed`/`scanner:scan-complete`/`coverage:recomputed`/`goals:updated` events → `journey.logEvent` (no chokepoint surgery needed). (2) **J4** — `tools/render_probe_journey.js` (the journey drawer's first real runtime/visual test). (3) Command palette (⌘K).
- Older notes below are superseded where they conflict ('older loses').

**Status (2026-06-22, HEAD after file-audit cleanup A-C4):** The operating contract was
restructured (Cowork) and the entire OLD discipline system was decommissioned.
There is now **ONE instruction surface**: `CLAUDE.md` + `.claude/rules/` (11
files). `memory/` is gone (70 files). `invariants.py` is pruned to **20 checks
that protect the live app / code / eden / §17** — board **21/21 GREEN** (+creators_log_well_formed), baseline
empty (any red now is a real regression). Phase 1 is done + tagged
`v0.1.0-cleanup-complete`; Phase 2 has started (Coverage/Regimen/Scanner live;
**Knowledge drawer wired in 2A**). The FILE-AUDIT cleanup is now COMPLETE (A: invariants.py dead-helper reachability sweep -151 lines; B: sunjo plan de-staled; C/C2/C3/C4: repo-wide deleted-reference fixes + the 8-file `.claude/rules/` instruction surface now git-TRACKED + orphaned *.payload.tmp / products-db-audit artifacts removed; the stale out-of-repo `C:\Users\Light\Desktop\CLAUDE.md` duplicate deleted by Luneth). Board 21/21. SINCE THEN: the LOGGING MANDATE was codified (`.claude/rules/logging-doctrine.md` — the 3rd "why" doctrine, historical memory) + **Phase-2 L1** shipped — the Creator's-Log file-mirror (`chronicle/creators-log.jsonl` + `tools/creators_log.py`) makes round-close step 5 CLI-fireable. **Do NOT resurrect memory/, brain/, tacitus,
saga/lessons/decisions, or the old invariant paperwork — it was deleted on purpose.**

## First commands (catch-up)
```
PYTHONUTF8=1 python tools/invariants.py | tail -1     # expect: 25/25 passed (0 failed)
PYTHONUTF8=1 python tools/creators_log.py verify      # Creator's Log: N entries, 0 problems
git -C "C:/Users/Light/Desktop/claude/health expert" log --oneline -8
node tools/build.mjs                                  # Build OK (~309 KB raw / ~66 KB gzip)
node tools/render_probe.js                            # coverage 20/92, 0 errors
node tools/render_probe_knowledge.js                  # Knowledge drawer + 201-product vault → PASS
```
Read order: `CLAUDE.md` → the matching `.claude/rules/*.md` for your domain →
`genesis/02-clarifications-and-plan.md` → `REVIEW.md` → `tail -20 chronicle/build-log.md` → this file.

## The new instruction surface (memorize)
- `CLAUDE.md` — orientation + §00 prime directives + Disciplines + the Behavioral-rules table.
- `.claude/rules/`: write-discipline · chokepoint-discipline · data-flow · typescript · testing ·
  commits-and-rounds · windows-host · wild-west-mode · **source-rule (§00.A)** · **engineering-doctrine (§00.B)** ·
  **logging-doctrine** (the §00 historical-memory why — build>test>log>repeat, repo-as-teaching-tool, never-poison-the-future).
  (source-rule + engineering-doctrine folded in from the deleted memory/ during cleanup; logging-doctrine codified 2026-06-23.)

## What's DONE
- **Phase 1** (1A–1G) tagged `v0.1.0-cleanup-complete` — tacitus/brain/tmp_jscheck excised; invariants tacitus-free.
- **Phase 2 · Chunk 2A** — Knowledge drawer (K) wired (rail + bare-K + Esc); `readProducts` fixed to read the
  vault by `canonical_name` (201 real entries). Probe: `tools/render_probe_knowledge.js`.
- **Cleanup C1–C5** — folded heart/soul canon into `.claude/rules/`; pruned invariants 47→20 (board 20/20);
  deleted 11 orphaned tools + 4 trap docs (HANDOFF, dashboard SUMMARY/README/ARCHITECTURE); deleted all of
  `memory/` + conflict_detector; neutralized `eslint --fix` in dashboard lint-staged; logged §17 incident #6
  (Cowork Write NUL-padding) in `chronicle/contradictions/`.
- **File-audit cleanup A–C4** (2026-06-22/23) — invariants dead-helper sweep (−151 lines) · sunjo plan de-staled ·
  repo-wide deleted-ref fixes · `.claude/rules/` instruction surface git-tracked · orphaned artifacts removed.
- **Logging doctrine codified** — `.claude/rules/logging-doctrine.md` (3rd "why" doctrine: historical memory); wired into CLAUDE.md.
- **Phase 2 · L1 — Creator's-Log file-mirror** — `chronicle/creators-log.jsonl` + `tools/creators_log.py {append,verify,list}`
  make round-close step 5 CLI-fireable; the §00 audit trail now lives in the repo. Invariant `creators_log_well_formed` (board 20→21).
  L2 (the in-app boot-merge into the Profile panel) is the remaining display half — see Phase-2 item 2.

## Phase 2 REMAINING (recommended order)
1. **Journey drawer (J)** — ASSESSED 2026-06-22: NOT built-but-unmounted like Knowledge. Both
   `views/journey.ts` AND `state/journey.ts` are throwing SCAFFOLDS (types only; every fn throws
   "pending Round 5"). `#drawer-journey-mount` + the J rail button are in `dashboard.html`; `journeyView`
   is imported + sits in `_refs`. Visual contract: `dashboard/components/drawer-journey-v3-PROPOSAL.html`
   (60KB; 4 tabs: Timeline / Goals / Check-ins / Milestones). DECISION (Luneth, 2026-06-22): NO fake seed
   data — the timeline AUTO-DERIVES from real activity (existing chokepoints log real scan/regimen/coverage
   events); a fresh dashboard shows a clean empty-state; check-ins are PRIVATE (never exported — ties to the
   Phase-3 export design). PLAN (~4 chunks): **J1 state engine** (implement logEvent/listEvents/logCheckin/
   listCheckins via `core/storage.getValidated`/`setValidated` + 2 new Zod LS keys + §31 chokepoint writers
   emitting a new `journey:changed` event in `core/events.ts` + the +/-7-day cross-ref walker) -> **J2 view**
   (4-tab render per the v3 mockup + the LOG EVENT footer form; zero inline literal >10 elems) -> **J3 wiring**
   (generalize `mountKnowledgeDrawer`/`toggleKnowledgeDrawer`/`wireKnowledgeKeys` into ONE shared K+J drawer
   helper; mount + J rail toggle + Esc + bare-J) -> **J4** `tools/render_probe_journey.js`. Template to mirror:
   the implemented `views/knowledge.ts` (336 lines) + its 2A wiring in `main.ts` (lines ~108-212, 287-288).
2. **Profile panel + Creator's-Log boot-merge (L2)** — the CLI half is DONE (L1: `chronicle/creators-log.jsonl` +
   `tools/creators_log.py`; step 5 CLI-fireable). REMAINING: build the Profile panel (`showProfilePanel`) + the
   app-side boot-merge. The offline file:// app CAN'T fetch() local files at runtime, so embed the jsonl at BUILD
   time (mirror the existing HTML-embedded-JSON / esbuild-JSON-import pattern) and merge it with the LS
   `wallachCreatorsLog_v1` entries in `state/log.ts` (dedup by id) so Profile shows BOTH CLI- and in-app-fired entries.
3. **Command palette (⌘K)** — `views/palette.ts` exists; universal nav.
Per-surface: data flows `eden/* → schemas/* → core/* → state/* → views/*`; no literal >10-elem array in
views/state; visual-match the v3 mockups in `dashboard/components/` (VISUALS ONLY — that demo code is slop).

## Flags (Luneth's call — not auto-actioned)
- **Old-agent CLI reasoning tools — DELETED** (cleanup C6: label_scorer, trace_verify, symptom_lookup,
  lab_interpreter, stack_coverage, catalog_index). `tools/` is now 10 purposeful files: build_*/extract_*
  (data pipeline) + corpus_search (pipeline dep) + invariants.py + safe_write.py.
- **`genesis/02-clarifications-and-plan.md`** is the captured plan but has stale file refs (HANDOFF.md, a
  "do not delete memory/" clause now superseded). It's now git-TRACKED. Refresh or leave as captured history.
- **`dashboard.html` still embeds old versions/saga/lessons/decisions content** (shown in Profile/Journey).
  That's live-app content for the Phase-2 Journey/Profile rebuild — intentionally untouched by the purge.
- **`invariants.py` retains a few dead helper fns** from the removed checks (parse-clean, never called) — an
  optional deep-tidy.

## Discipline / gotchas (carry every session)
- **ALL repo writes via `python tools/safe_write.py {replace|append|rewrite|check}`** (Edit/Write hook-blocked).
  Stage payloads in `C:/Users/Light/AppData/Local/Temp` via the Write tool; `replace` payloads must be **LF**.
  For multi-edit mechanical changes, a Temp python script computing new content + `safe_write.safe_rewrite`
  (transactional: validate-all-then-write) worked cleanly across this whole cleanup.
- **Never run a `safe_write` and `python tools/invariants.py` in the SAME Bash command** — invariants emits
  `OK   [warning] …` lines that the `post_write_verify` hook misreads as safe_write paths (false-positive block).
- **CWD trap:** never bare `cd subdir`; use `(cd dir && …)` subshells; recover via PowerShell `Set-Location`.
- **`PYTHONUTF8=1`** prefix for python (Windows cp1252). **NEVER `eslint --fix`** (corruption surface; lint-staged
  is now read-only). **Numbers placeholder-faithful** (migrate verbatim).
- **Round-close:** build OK + invariants 25/25 + render probe + build-log line + **Creator's Log entry** + commit + push.
  Creator's Log is now CLI-fireable (L1): `PYTHONUTF8=1 python tools/creators_log.py append --surface <s> --kind
  <round-close|milestone|incident|design-decision|…> --summary <≤280> [--detail …]` (validated by `creators_log_well_formed`).
  Co-author trailer: `Claude Opus 4.8`.
- **GitHub:** `origin` = https://github.com/sunjolol/the-wallach-codex (PRIVATE, `master`). Push after each chunk.
  ~95MB copyrighted Wallach PDFs under `knowledge/wallach-books/` — no public repo without the Phase-4 scrub.

## Working commands (verified, Windows host)
- Build `node tools/build.mjs` · Invariants `PYTHONUTF8=1 python tools/invariants.py`
- Lint one file `(cd dashboard && node_modules/.bin/eslint assets/js/src/views/X.ts)` — SUBSHELL, never bare cd
- size-limit `(cd dashboard && node_modules/.bin/size-limit)` · madge `(cd dashboard && node_modules/.bin/madge --circular --extensions ts assets/js/src)`
- Probes: `render_probe.js` · `_seeded` · `_scan` · `_scanner` · `_ocr` · `_adopt` · `_knowledge`
