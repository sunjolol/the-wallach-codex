# Next chunk — FULL OVERHAUL IN PROGRESS. Active plan = `chronicle/OVERHAUL-BLUEPRINT.md` (locked 2026-07-05, 8 sections). This handoff points there.

## WHERE WE ARE (2026-07-05)
The project pivoted from whack-a-mole bug-fixing to a full structural overhaul after Luneth caught that book citations were hand-typed ~200× (drifting from the sealed registry — the year said 1999 while the registry says 2011). We designed the ENTIRE system on paper first (the blueprint) before implementing. **Core model:** TWO hand-edited sealed sources — Wallach Corpus (`eden/corpus/`, live) + Youngevity Products (`eden/products/`, Phase F) — plus a shared Catalog (`eden/catalog/`: conditions · symptoms · nutrients; the 90/91 essentials stay in `eden/corpus/essentials-canon.json`, referenced by `nutrients.canon_slug`). **Everything else is GENERATED + freshness-gated** so drift can't ship. The rotten middle layer (`knowledge/essentials-targets.json` → embeds → Coverage) collapses into the clean `eden → derive → views` system (Phase C/D). Read the blueprint for the Charter (R1–R9), the data model, the pipeline, the enforcement table, per-surface plans, file disposition, and the 9-phase migration.

## DONE — Phases A + B (all committed + pushed; board 40/40 green)
**Phase A — governance reset (2026-07-05):** full legacy-dashboard sever; CLAUDE.md + the 12 `.claude/rules/` rewritten to the pillar model + Charter; memory audit 75→68; Charter promoted to its permanent home `.claude/rules/charter.md`; `no_operating_doc_contradiction` gate added; noise purge (`wallach-refresh/`, `corpus-changelog.md`, `genesis/01`, stray `dashboard/.git/`).

**Phase B — Catalog pillar built + SEALED (blueprint §2.2):**
- **B1** (`dcee79c`) — `conditions.json` (512) + `symptoms.json` (164) registries. Folded in `condition-taxonomy.json` + `condition-synonyms.json` + `corpus_derive` DISPLAY_OVERRIDES (all RETIRED) → the single `eden/tools/catalog.py` loader. Wired the `references_resolve` gate (a claim cond/symptom slug must be catalogued — closes the phantom-slug hole). Byte-identical renders, no corpus re-seal.
- **B2** (`c22b527`) — `nutrients.json` (499 = 91 canonical → `canon_slug` into essentials-canon + 408 non-essential substances → null). `references_resolve` extended to `other_substances`; check #12 gated in the seal PRE-gate (the #8-parallel, R9). Discovered + fixed 4 space-in-slug corpus defects (Immortality: bismuth-subsalicylate, cruciferous-vegetables, hydrogenated-vegetable-oil, trans-fatty-acids) → corpus re-seal **kv 302→303** (Luneth-authorized). Field named `canon_slug` (not the blueprint's tentative `is_essential`); nutrient `class` taxonomy DEFERRED to Phase F.
- **B3** (this commit) — `eden/tools/catalog_verify.py` + `catalog_seal.py` (USER-ONLY) + the `catalog_integrity` invariant. DELETED the dead `legacy_css_contained` invariant + its check fn (its `.css` was severed in Phase A). `eden/catalog/README.md` + the essentials reconcile (90/91 stay in essentials-canon, referenced by `canon_slug`, not duplicated — R3). **SEALED** eden/catalog (3 `*.golden.sha256`, Luneth-authorized).
- **Deferred (Luneth):** `eden-catalog.json` absorption → Phase F (it is a 201-product Youngevity DB draft = Pillar-2 material, not catalog vocabulary); nutrient `class` taxonomy → Phase F.

## PHASE C COMPLETE — derivation pipeline + poison purge (blueprint §3 / §7)
- **C1 DONE** (`811b418`) — pipeline spine: `eden/derived/MANIFEST.json` + `eden/tools/build_embeds.py` orchestrator + `derived_artifacts_fresh` gate (absorbed `corpus_embed_synced`). No visible change.
- **C2 DONE** (`d87b1d6`) — THE POISON PURGE. `eden/tools/targets_derive.py` derives Coverage targets from Wallach BOOK dose claims (Youngevity amounts gone); `amounts_wallach_only` gate (35 numeric targets); D1 migrated `coverage.ts` to esbuild import; retired the 3 `wallach_stance_*` gates. **"WALLACH SAYS" stance layer DROPPED** (poison: lecture cite + Youngevity stances + hand-typed cites) — re-authored MANUALLY per essential post-mining, styling kept (memory wallach-says-boxes-dropped-readd-post-mining). Rule: target = a Wallach general/maintenance DAILY dose (base-line table OR any book), **ranked NEWEST-BOOK-FIRST** (memory favor-newest-wallach-number: Potassium=Immortality 5000, not LPD 5500). Luneth signed off vs the LPD Fig 8-1 image. Board 40→38.
- **Books-only policy codified** (Luneth 2026-07-05, memory books-only-no-lectures) — `wallach-lecture` token retired from source-rule/data-flow/CLAUDE.md §00.A/domain-glossary. Corpus already 100% book-sourced.

**REMAINING in Phase C:**
- **The sweep (dose/target-first, Luneth):** (1) ✓ **DONE** (2026-07-05, commit 9b09aa1) — Fig 8-1 dose-table quotes now carry a column legend (Nutrient · RDA · True Supplement Need · 30-Day Pharmacologic, Wallach's own header) + line-broken rows in the Knowledge drawer. Pure VIEW fix: `isFig81Row` detects via `dose.for_condition`, verbatim byte-faithful, NO re-seal; Luneth signed off. (2) ★ **FULL corpus audit MANDATORY before Phase G** (memory full-corpus-audit-before-phase-g) — spot-checks found mis-labels + off-source cites + a flawed daily-amount rule; audit ALL claims before mining. **STILL OWED.**
- **C3 DONE** (`d8d7e51`) — product-vault embed (`regimen-label-lookup`) folded into the pipeline: NEW `eden/tools/products_embed.py` derives it deterministically (no timestamp) from `eden-catalog.json` → `dashboard/assets/data/regimen-label-lookup.json` (manifest artifact #3, content-gated by `derived_artifacts_fresh`); D1-migrated both consumers (views/regimen.ts readVault + views/knowledge.ts readProducts) to esbuild import; deleted the inline `regimen-label-lookup` (302 KB) + DEAD `goal-recommendations-data` (116 KB) blocks from dashboard.html (−419 KB); RETIRED `eden_embeds_match_canonical` (version-stamp → content-gate). Data byte-identical (201 products). Board 38→37.
**PHASE D COMPLETE — collapsed the rotten `knowledge/` layer (blueprint §6).** ✓ **D-a DONE** (`020fa46`, 2026-07-05) — safe dead-code sweep: removed 5 dead inline dashboard.html data blocks (1.95→1.72 MB), git rm'd 3 dead files (old `regimen-label-lookup` + the `ingredients-quickref` chain), relocated `knowledge/fringe-knowledge` → `eden/fringe-knowledge/` + `knowledge/design-wisdom` → `dashboard/design-wisdom/`, reconciled live path refs. No behavior change; board 37/37.
✓ **D-b DONE** (`d0b4656`, 2026-07-05) — re-homed the `coverage_kind` classification (the one live field) from the poison hand-file onto the sealed `essentials-canon` pillar (NORMALIZED — no Youngevity `hbsp` flavor), re-sealed corpus **kv 303→304** (Luneth-authorized), repointed `targets_derive`, and DELETED `knowledge/essentials-targets.json` (its dead `wallach_stance` poison purged with it). Coverage output proven BYTE-IDENTICAL. `knowledge/` now holds ONLY `products-db.json` (Phase F).
✓ **D-c DONE** (prep `638e8bb` + gates this commit, 2026-07-05) — (prep) DELETED the too-basic `eden/catalog/nutrients.json` (Luneth-authorized sealed-file delete): its 91 canonical entries only re-copied `essentials-canon` names + its 408 substance names were byte-identical to `humanize(slug)`, so corpus-embed re-derived BYTE-IDENTICAL (no re-seal); the nutrient/ingredient registry rebuilds richer with the Product DB in **Phase F**, so `references_resolve`'s substance half is DORMANT until then. (gates) Added the three Phase-D Charter gates LIVE + negative-tested (teeth proven) on the CLEAN surface (corpus claims + canon + catalog + corpus-derived targets/coverage-layout): `citations_reference_registry` (book refs = `book_id`, no hand-typed cite — the overhaul-trigger anti-drift gate), `prose_contained` (no prose in a fact field), `no_hand_duplicated_canonical` (no canon name re-stored outside `essentials-canon`). **Option-1 altitude (Luneth):** real teeth on the clean surface NOW; the legacy embeds (`essentials-benefits-data` still carries "(Wallach Dead Doctors Don't Lie)", etc.) + views (`knowledge.ts` DOCTRINES) + the full prose-store R4 = labeled WISH (Phase E/F/G). Charter R2/R3/R4 + source-rule/data-flow reconciled. Board 37→40.
✓ **D-review DONE** (this commit, 2026-07-05) — stripped the DEAD creators-log subsystem from dashboard.html: git rm'd the unloaded `creators-log-handler.js` (no `<script src>`; its target DOM + its guard `dashboard_integrity.py`/`check_script_blocks`, itself deleted in Phase A, were already gone) + the 5 orphaned `cl-data-*` markdown blocks (saga/lessons/decisions/changelog/notebook, ~1.67 MB) + the dead inline `versions-data` block + orphaned comments. dashboard.html is now a TRUE pure shell (**1.72 MB / 8112 lines → 6 KB / 128 lines**); content preserved in git + chronicle/creators-log + assets/data/creators-log. App boots identically (render_probe coverage + knowledge both 0 errors). Also cleared a pre-existing `no_external_style_resources` false-positive (CSP-comment font URLs). Board 40/40. **★ PHASE D COMPLETE.**
Then (after D): **E** per-surface finalize → **F** Pillar 2 (Youngevity, Luneth hand-mines) → **G** resume book mining (AFTER the full audit) → **H** Search (retrieval-first) → **I** Journey (last) + portable-offline browser.

**★ OWED BEFORE PHASE G (Luneth, do-NOT-forget):** consolidate the dense corpus-mining-MECHANICS memory cluster (`reading-and-correcting-scanned-pdfs`, `editing-sealed-corpus-claims`, `batch-mining-workflow`, `cross-book-uniform-correction`, `quote-sync-mechanism`, …) — they redundantly repeat the seal-order trap; merge preserving every recovery recipe, right before the first Phase G batch. Tracked in memory `consolidate-mining-mechanics-before-phase-g`.

**OLD BACKLOG (subsumed, not lost):** menaquinone→phylloquinone book correction + K1/K2 alert-box → Phase E/G · target-number-shift → Phase C · Immortality/DDDL re-mine → Phase G. The blueprint is authoritative.

**SET UP FOR NEXT SESSION:** start fresh + run `genesis`. **Phases A–D COMPLETE; board 40/40.** The overhaul's cleanup + gating half is DONE: the rotten `knowledge/` layer is fully collapsed (it's gone except `products-db.json` → Phase F), the three pillars + the derive pipeline + the freshness/Charter gates are live, and dashboard.html is a TRUE pure shell (128 lines). **NEXT = Phase E — per-surface finalize (blueprint §5):** update the views to the new derived data + REBUILD the legacy surfaces the D-c gates left as WISH — the `essentials-benefits-data` / `essentials-best-supplements` / `goal-recommendations` / `ingredients-*` legacy embeds (still hand-maintained prose + a hand-typed cite) + `knowledge.ts` DOCTRINES + `regimen.ts` placeholders — EXTENDING `citations_reference_registry` / `prose_contained` to each as it's cleaned; per-surface render probes + Luneth visual sign-off (this is visual/UX work — gold-standard-page-workflow: one surface to 100% at a time). Then **F** Youngevity Product DB (Luneth hand-mines; the nutrient/ingredient registry rebuild + the old-D3 vestigial-tool retirement — `eden_build.py` / `build_regimen_label_lookup.py` / `products-db.json` / `eden-catalog.json` — all live here; `references_resolve`'s substance half re-lights) → **G** resume book mining (AFTER the full corpus audit) → **H** Search (retrieval-first) → **I** Journey + portable-offline browser. LEFT-noted (Phase-E cosmetics, no gate impact): dashboard.html's header references a non-existent `ARCHITECTURE.md` + carries stale "Round 2-5" mount-slot comments; the SEALED `design-system.css` header still references `knowledge/design-wisdom` (fix at the next design-system re-seal / project end). ★ Still owed before Phase G: the FULL corpus audit (MANDATORY, memory full-corpus-audit-before-phase-g) + the mining-mechanics memory consolidation (memory consolidate-mining-mechanics-before-phase-g).
