# Next chunk — TASK (a): A2 DONE → Omega-9 canon correction + per-omega alerts NEXT

**★ CURRENT STATE (2026-07-08).** Phase F (Youngevity Product DB) COMPLETE + SEALED (215 products, registry-backed). Task (a) — the Coverage/Knowledge deep-dive on real product data — has landed three chunks:

- **A1 (`761495be`)** — product vault swapped to the sealed `eden/products/products.json`; the ENTIRE old scraped-catalog product subsystem DELETED as poison. [[old-product-system-full-delete]]
- **Products detail-panel surface (`84030f6b`)** — Knowledge▸Products lists ALL 215 products, each clickable to a full YGY detail panel. [[product-detail-panel-vision]]
- **A2 (committed this round — run `genesis` for the hash)** — `state/coverage.ts` no longer hand-string-matches nutrient names; it resolves **label name → canon slug → target** through the ONE registry resolver (`core/nutrient-resolver.ts::resolveSlug`, a faithful TS port of `eden/tools/nutrient_resolve.py`). NEW derived `dashboard/assets/data/nutrient-resolver-data.json` (MANIFEST-registered; projects the resolver's identity tables) + NEW critical invariant `nutrient_resolver_parity` (two-layer: committed fixture ≡ live `resolve()` over all 1360 distinct pillar names + an artifact-driven resolver ≡ `resolve()`) + vitest (`core/nutrient-resolver.test.ts` + `state/coverage.test.ts`; `npm test` glob fixed). `targets_derive.py` now emits each essential's `slug` (the join key). Resolver gained a parenthetical-strip (`"Vitamin B1 (Thiamine)"`) + explicit-Omega-N-wins (also fixed 2 dropped pillar omega-3 rows). Default-stack coverage BYTE-IDENTICAL to the old matcher (0 diffs). UNITS stayed IU-native — canonical-unit unification (`toMg` vs `to_canonical`) is a labeled WISH for later. Board 50/50.

Active plan = `chronicle/OVERHAUL-BLUEPRINT.md`.

## ★ RESUME HERE

1. **Omega-9 canon correction + per-omega clarity alerts (NEXT — plan-mode it; SEALED-pillar edit).** Luneth 2026-07-08: the 90-nutrients graphic (our canonical authority, [[essentials-authority-graphic]]) MISLABELS Omega-9 as "Arachidonic" — biochemically WRONG. Arachidonic is Omega-**6**; Omega-9 is **Oleic Acid (OA)**. The graphic's ONE known error.
   - **Correct labels (LOCKED format = "Omega-N (Primary Acid / ABBR)"):** Omega-3 → `Omega-3 (Alpha-Linolenic Acid / ALA)`, Omega-6 → `Omega-6 (Linoleic Acid / LA)`, Omega-9 → `Omega-9 (Oleic Acid / OA)`. Additional forms move OUT of the main label into the alert box.
   - **Blast radius (layout_key IS the coverage↔snapshot JOIN KEY — change in LOCKSTEP):** `eden/corpus/essentials-canon.json` `layout_key` (3 omegas — SEALED → user sign-off + re-seal via `eden/tools/corpus_seal.py`) + the 3 hand-authored files that reference the key (`dashboard/assets/data/{coverage-layout-skeleton,scanner-corpus-data,regimen-base-data}.json`) + regenerate the derived (targets / coverage-layout / corpus-embed). Wallach's CLAIM TEXT + book sources that mention "arachidonic" stay VERBATIM (his words, untouched). The resolver's `arachidonic → omega-6` keyword STAYS (correct). Update `nutrient_resolve.py`'s comment + self-test that reference the old omega-9 label string.
   - **Alert box (LOCKED = per-omega on each deep-dive, BOTH Coverage + Knowledge>Essentials):** a tier-1 CLARITY alert on each omega essential's detail panel — Omega-3 shows ALA + EPA + DHA, Omega-6 shows LA + GLA, Omega-9 shows OA — each with a short description. Explicitly NOT a Wallach claim (general reference, clearly marked, §00.A-safe — the source rule permits clearly-marked non-Wallach reference). Prose lives in a NEW segregated store (MANIFEST-accounted, R4/`prose_contained`). Draft from Luneth's supplied descriptions (shorten as needed; visual sign-off before close).
   - **Ledger:** correct memory [[essentials-authority-graphic]] to note the omega-9 error (so a future session never "restores" arachidonic to match the graphic); add a `chronicle/contradictions/` adjudication doc (source-rule-adjacent canon correction, mirrors `2026-06-24-source-rule-90-essentials-omega9.md`); record the durable omega definitions.
2. **A3 — cost-per-nutrient recommender.** Promote `product-composition.json` → a consumed surface; composition + breadth + banded-cost ranking (cost from `prices.json`). HONEST LIMIT: saturating-adequacy needs Wallach targets = BLOCKED on (b); ship the structure, adequacy lights up post-mining. [[cost-per-nutrient-match-score]].
3. **Conditions → product suggestions** — PARKED until book mining (needs the cure→essential→product link). [[product-detail-panel-vision]].
4. **(b) — the STILL-OWED full corpus audit → Phase G book mining** (+ build the source-anchored substance triage buffer, [[substance-registry-and-triage-buffer]]; gated by `corpus_audit_gate`). [[full-corpus-audit-before-phase-g]].

**★ KEY CONTEXT:** all 91 Wallach targets are honest gaps today (no numeric amount) → the "% toward target" coverage VERDICT awaits corpus dose-mining (b). The registry already powers composition aggregation + cost-per-nutrient NOW. Global styling touch-ups are deferred to ONE end pass (Luneth 2026-07-08) — don't nitpick CSS mid-build.

## The overhaul in one paragraph (context)
Full structural overhaul after book citations were caught hand-typed ~200×. **Model:** TWO hand-edited sealed sources — the Wallach Corpus (`eden/corpus/`) + the Youngevity Product DB (`eden/products/`, COMPLETE + SEALED) — plus the shared Catalog (`eden/catalog/`). Everything else is GENERATED + freshness-gated. Phases A–F DONE. Task (a) = finish the Coverage/Knowledge deep-dive on real product data (A1 + Products surface + A2 done; **the omega correction + A3 remain**). Then (b) = the owed corpus audit → Phase G mining. The Charter (R1–R9) + its gates are the enforceable spine; `.claude/rules/` carries the per-domain HOW.
