# Next chunk — ★ AUTHORITATIVE HANDOFF (updated 2026-07-22, Condition-DETAIL view close)
# ★★★★★ 2026-07-22 — CONDITION DETAIL view landed (Osteoporosis exemplar, data-driven for all 502) + breadcrumbs added drawer-wide · board 77/77 · committed+pushed
# ▶ NEXT: the remaining demo-to-live surfaces — Products tab · Products detail · Ask-Wallach WORDING. Mining still PAUSED.

## TASK CONTEXT (carries across the whole demo-to-live effort — memory demo-elements-still-to-do + demo-layout-yes-demo-style-no)
- THE demo = `file:///…/temporary/knowledge-drawer-prototype.html` (NOT dashboard/components/*.html). Surfaces still not-yet-live:
  1. Products tab · 2. Products detail · 3. Conditions tab ✅ · 4. Conditions detail ✅ (this session) · 5. Ask-Wallach popup/side-menu WORDING (NOT a rebuild — the drawer works; only copy).
- HARD RULE: take the demo's LAYOUT/STRUCTURE, RESTYLE to CURRENT standards, then IMPROVE past it (drop dated demo styling). Fonts: `--ds-font-display`=Unbounded, `--ds-font-sans`/`--ds-font-serif`=Space Grotesk, `--ds-font-display-interface`=Chakra Petch (labels), `--ds-font-display-artifact`=Bruno Ace (numbers), `--ds-font-mono`=JetBrains. NO serif except crown-jewel Wallach pull-quotes. Visuals identical in spirit, CODE PROPER. Reference current-best surfaces: omega-3/6 detail, Absorption tab, plant-derived-mineral detail. VISUAL SIGN-OFF each chunk (Luneth is the tester).

## ✅ LANDED THIS SESSION — CONDITION DETAIL view (`renderConditionPage`, the demo-to-live Osteoporosis exemplar, generic over all 502)
- NEW renderer `views/entity-page.ts::renderConditionPage(slug)` — a PURE PROJECTION mirroring `renderEssentialPage` (no per-condition literal; `entity_render_is_projection` holds). Reads `getConditionPage(slug)` (entity-page-data.json conditions[]) + the sealed corpus. Wired from `knowledge.ts` renderTab 'conditions' branch (prepended above the grid, like essentials); `renderConditionsTab` slimmed to grid-only; the old `renderConditionDeep` + its ~15 kd-corpus/kd-claim helpers REMOVED from `knowledge-corpus.ts` (which now only OWNS the Conditions-tab grid + the exported condition derivations `conditionSynopsis` / `essentialsInRoles` / `familiarEssentialName`).
- SECTIONS (demo layout → live kd-ep-* system): category-tinted hero (icon tile + name + chip+counts subline + back) · synopsis lede (derived `conditionSynopsis`, NOT the broken auto `one_liner`) · Wallach's protocol (the REAL sourced `protocol_claim_ids`, NEVER composited — §00.A; first open, green accent) · NUTRIENTS: relationship-aware hybrid — "To restore" pills (glimpse) + collapsed "Caused by these deficiencies" / "Also cited alongside" lenses (Expand ▾/Collapse ▴ pills) · BEST PRODUCTS (rankProductsForCoverage over restore[], "covers N/M · $wholesale", best-value tag) · THE FULL PICTURE (renderRecord over record[] kind groups, keyword filter) · Related conditions + Keep exploring pills.
- CATEGORY colour-coding per condition (from condition-categories.json, all 502 mapped): a NEW per-category SVG `icon` field (12 body-system glyphs, added to the curation + schema + reader) rendered as a --cat-coloured hero tile; the whole condition CARD BORDER + top hairline + the drawer SCROLLBAR take --cat. ⚠ SCROLLBAR GOTCHA (memory webkit-scrollbar-root-custom-props): WebKit scrollbar pseudo-elements read ONLY root-level custom props — so `render()` publishes the colour on `<html>` as `--kd-cond-scroll` (cleared off a condition); Firefox uses `scrollbar-color`, Chrome the webkit thumb, both read that root var.
- Claim card change (record cards ONLY, `.kd-ep-claim--record`): expanded = FULL summary in the parent-kind family colour + bold (no more truncated-then-repeated title); collapsed = the scannable teaser. Search Q&A cards (`renderSearchCard`) UNTOUCHED (scoped by the --record class).
- BREADCRUMBS (new, drawer-wide — Essentials/Conditions/Products): ported the signed-off demo's BOUNDED/LOOP-FREE/cap-6 trail model to `knowledge.ts` (origin-tab anchor · dedup-truncate on revisit · CRUMB_MAX drop-oldest), restyled to our mono chrome (`.kd-crumb*`). All entity nav routes through one `openDetail()`; `goCrumb()` on click. Labels resolved from state, never scraped from DOM.
- Absorption one-off: facets lead with "What to do" (protocol) above "Basics" (`knowledge-foods.ts::facetSections`).
- Verified: build · invariants 77/77 · render_probe_entity + render_probe_knowledge (UPDATED to the new kd-ep--cond structure; it had been stale on the old deep-view since round 1) + a breadcrumb behaviour probe — ALL PASS · 0 page errors · category propagation across 6 categories · scrollbar mechanism proven via computed scrollbar-color (blue on conditions, orange off). Luneth visually signed off each round incl. the scrollbar.
- DATA GAP still open (not blocking): the auto `one_liner` for a condition falls back to the FIRST definition claim, which for osteoporosis is a *fibrous-dysplasia* definition (mismatch). We render the derived `conditionSynopsis` instead. A future `entity_page_derive.py` fix could give conditions a clean one-liner.

## ▶ NEXT — pick a remaining demo surface (ASK Luneth which; memory decisions-need-a-question)
- Products tab + Products detail: first CONFIRM what the demo ADDS beyond the live `knowledge-products.ts` (list + detail already work) before rebuilding — likely a restyle + the demo's richer detail layout, same demo-to-live discipline.
- Ask-Wallach WORDING: rail "SEARCH"→"Ask Wallach" + the popup/side-menu copy to match the demo. NOT a re-architecture of the working Search drawer.

## ⏸ PARKED — Batch-4 book mining (resume only if Luneth redirects): re-dedup each teal-new candidate vs ALL 7 sealed books first (memory dedup-across-all-books-before-authoring). Byte-verified passages in temporary/plant-derived-research-2026-07-17/.

## 🔴🔴 REVIEW PROCESS — Luneth's hard rule (EVERY corpus/content touch): before sealing ANY claim show it in EXACT final form — QUESTION → SHORT ANSWER → (full only if it adds) → QUOTE — and approve the CLAIM, never a side-question. Unreviewed = log "unreviewed", never "approved". corpus_seal is USER-ONLY.

## 🔧 KEY MECHANICS
- Preview pane CACHES the bundle — for a reliable visual use a headless puppeteer screenshot (scratchpad shot_condition.js pattern): fresh chrome loads current dist + CSS; dismiss the onboarding modal ("just browsing") first, then screenshot. NOTE: headless Chrome uses OVERLAY scrollbars (won't screenshot); prove scrollbar colour via computed `scrollbar-color` instead.
- Headless clips content beyond the `.kd-body` scroll height (overflow) — screenshot with a TALL viewport OR scroll the element into view.
- CSS is LINKED not bundled — CSS edits need NO rebuild; JS/data edits need `node tools/build.mjs`.
- Curation-layer pattern (reused for the category icons): `assets/data/*.json` (hand-authored) + `core/schemas/*.ts` (+ index export) + `state/*.ts` reader + MANIFEST `accounted` entry.
- Round-close: build → invariants → probe → build-log → `creators_log.py append` → RE-inline build → commit + push.
- Windows/UTF-8: prefix `PYTHONUTF8=1`; safe_write payloads LF; every project write via `safe_write`. NEVER bare `cd subdir` (drifts cwd → cwd-relative hooks fail; recover with PowerShell Set-Location).
