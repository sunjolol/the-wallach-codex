# ★★★ NEXT SESSION — READ THIS FIRST.

**No forced task.** 2026-08-17 was an all-day dark-theme polish run: a foreground brightening sweep + profile popup, then several rounds of point fixes on controls Luneth flagged (an 11-item list + a follow-up round). Board **92/92**. eden/ untouched. **origin/master in sync at `fc63030b` (all pushed).**

## ✅ DONE (2026-08-17)
- **Dark follow-ups, batch 2b** (`fc63030b`): (1) `.kd-ep-back:hover` (< ALL CONDITIONS / < ALL PRODUCTS / < All essentials) follows the page CATEGORY via a chained `var(--cat, var(--form, var(--ds-accent)))` — --cat on conditions/essentials, --form on products, accent on topics; dark-brightened. (2) product detail box + `.kd-ep-hero__sym--form` icon borders → the delivery-form colour `--form` (same as item-8's --cat for conditions; (1,5,0) beats the accent-deep :is()). (3,4) HOME left-stripes → **50%** (`.kd-home .sh-tile::before` essentials + `.kd-home .sh-condrow` common-conditions, which batch 2 had MISSED); explore/condition borders stay 28%. Restore-button colours left as-is per Luneth. theme.css (J11) + a base `.kd-ep-back:hover` change in drawer-knowledge.css.
- **Dark point-fixes, Luneth's 11-item list, batch 2** (`9cf9800e`): theme.css block (J10), dark + `#drawer-knowledge-mount`. (8) condition-detail box + hero-icon borders follow the CATEGORY colour `--cat` again (Cancer grey / Osteoporosis blue / Anemia red) instead of the accent-deep the sweep forced — `--cat` is set inline on the box; the fix had to beat the drawers-block accent-deep `:is()` (which is **(1,4,0)** — `:is()` takes its most-specific arg) so I used (1,5,0). (6,7,9,11) a `--dk-fam-a` knob (**28%**, his pick) darkens explore pills, home `.sh-tile::before` left-stripes, `.kd-ep-pill--nut/--explore`, and `.kd-ep-claim` protocol borders; protocol text slightly darker; legend swatches kept bright.
- **Dark point-fixes, batch 1** (`757b2b24`): theme.css (J9) + drawer-knowledge.css. `.rec__add` (+) on coverage+regimen visible + accent hover; scanner `.vd-step__badge.is-done`→accent like 2&3; `.ds-btn-primary` + `.vd-step__badge.is-active` text → **#f3ead7** (light-on-accent, the parked primary-label decision); `.fgroup__more` keeps its category hue (pulled from J4 accent routing); `.kd-ep-nutri__lbl` 0.6→**0.7rem** (all themes).
- **Recycle removed-item "Restore" hover** (`6e91f579`): `.rc-btn-ghost:hover` in dark filled solid accent under bright-accent text (invisible). Fix (J8): DEEPEN the tint, keep bright text → readable on all 8 accents. **Reverted** the earlier wrong-button `.rc-btn-restore` change. Lesson: an INJECTED proxy button false-passed — drive the REAL element ([[verification-doctrine]]).
- **Dark FILL + red-hover** (`88acd1a3`): `.pf-done` gradient → `--ds-accent-hot`; `.vd-nrow__del.ui-close:hover` border → RED `color-mix(--ds-status-err 55%, ink)` on every accent (J6/J7).
- **Dark foreground sweep** (`f8652c5d`) + **low-nits + CSS-comment gate** (`87f63219`,`db28cd68`): every non-orange/yellow FOREGROUND brightened for dark; profile popup; `css_comment_no_premature_close` gate.

## ▶ START HERE — candidates (nothing forced; ask via AskUserQuestion)
1. **ORAC + Absorption redesign** — both slated; their dark internals were EXCLUDED from the sweep (deferred to the redesign). Apply the same fg-brightening + fill-saturation + category-border methods ([[dark-theme-token-remaps]]).
2. **Primary-button-label pass** (deferred, partly done): batch 1 flipped `.ds-btn-primary` + `.vd-step__badge.is-active` to a fixed light `#f3ead7` on their accent fills. The deleted-SAVES `.rc-btn-restore` pill still carries dark `--ds-paper-light` text on a saturated accent — if he wants ALL primary/accent-fill buttons on a fixed light, do it as one coherent pass, not piecemeal.
3. **Deep-drawer internals** (essential/product deep-dives, foods figures): got the sweep CSS but weren't each screenshot-verified — spot-check if one is reported off.

## STANDING / PARKED (do NOT raise unprompted)
- **CORPUS SEAL held.** eden/ untouched; 7 unreviewed draft books in `eden/corpus/drafts/`.
- Reusable dark-verify harnesses in gitignored `temporary/` + this session's scratchpad: `probe_ghost2.js` (drives the REAL recycle popup), `probe_ground.js` / `probe_batch2.js` (drive the REAL knowledge drawer: home/explore/condition-detail, seed via rail nav + `[data-kd-tab]` + `[data-kd-condition]`, flip `--dk-fam-a` inline to compare darkening levels). The pattern for any real-control dark check.

## GOTCHAS
- **Per-file endings**: `views/welcome.ts`, `views/scanner.ts`, `state/scanner.ts`, `state/ocr.ts`, `styles/workspace-scanner.css`, `scanner-corpus-data.json`, `ocr-dict-data.json` are **CRLF**; other views + `styles/*.css` (incl. theme.css, drawer-knowledge.css) + chronicle `.md` are **LF**. `safe_write check` FIRST.
- **theme.css is the dark/appearance layer (NOT sealed).** Dark overrides via `:root[data-theme="dark"]`, AFTER the `[data-accent]` blocks. Brighten a wash/soft/accent-deep TOKEN once; per-surface only for hardcoded literals, inverting islands, **dual-use FILLs** (accent-deep-as-fill → `--ds-accent-hot`), backgroundless-button UA #f0f0f0. To beat a dark `:is()` accent-deep block your selector must clear **(1,4,0)** (`:is()` = its most-specific arg). Never a `*/` in a comment (gated). [[dark-theme-token-remaps]].
- **Verify dark headless** (pane won't display, file:// → data: snapshot): Puppeteer `--allow-file-access-from-files`, seed `wallachUserProfile_v1={name,browsing:false,chosenAt:'YYYY-MM-DD',theme:'dark',accent}` (chosenAt = ISO **day string**). getComputedStyle color-mix → `color(srgb 0..1)` floats; hover/transition → `oklab(...)`; canvas-normalise before comparing. Drive REAL elements (an injected proxy false-passes). [[in-app-preview-is-static-snapshot]].

## GENESIS
`genesis` → run `PYTHONUTF8=1 python tools/genesis.py`, report, then **ask which candidate to resume** (nothing forced) — never a flair-only boot. If a new invariant red appears, that is the only response.
