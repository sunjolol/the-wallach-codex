# Next chunk — ★ AUTHORITATIVE HANDOFF (set 2026-07-29, after Copper's header SHIPPED + the lessons were codified)

# ⚠ HOW THIS SESSION STARTS
Luneth types `genesis` HIMSELF, in a NEW session. Claude runs `tools/genesis.py` ONLY in response
to that — never as a self-initiated in-session "reboot" ([[reboot-after-boot-file-changes]]).

# ★★★★★ READ FIRST (plain language)
Board **78/78 green · corpus kv432 · 2267 sealed claims · repo clean + pushed**.
**Copper's element header is LIVE** — the first bespoke per-essential header. The task now is
**the remaining 89 essentials**, one at a time.

## ★ BEFORE YOU TOUCH ANYTHING: read `.claude/rules/element-headers.md`
That playbook is THE deliverable of the copper session. It exists because copper cost a full
session and three rejections that were all avoidable, and Luneth's explicit instruction was that
none of it gets re-explained. It carries: the 4-mockup workflow · the real-container rule · the
selenium type CEILING · measure-don't-estimate · alignment-by-construction · one-arc cohesion ·
and the complete-entity-copy-entry rule. `CLAUDE.md`'s rules table points at it.
Memory pointer: [[element-header-playbook]].

## The workflow (Luneth's, unchanged)
**4 genuinely distinct mockups** (different layout AND illustration, bespoke to that element —
never the selenium chassis reskinned) → **Luneth picks** one or a mix → build the winner into the
live entity page → **STOP for his visual sign-off**. He drives.

## What Copper established (reuse, don't re-derive)
- **Composed-header schema** (`core/schemas/mechanism-clarity.ts`): OPTIONAL self-suppressing
  blocks — `figure_labels` · `split{left,right}` (2×2 grid; evidence = a claim quote OR a
  `field{total,columns,bands[]}`) · `bridge` · `figure_pre_beats` / `figure_post_beats` ·
  `beats_layout:"row"` · `beat.turn`. Compose from these; only a genuinely new SHAPE needs a
  schema addition.
- **Figures dispatch on a GENERIC key**, never a slug. Live: `rancidity` · `cofactor_fork` ·
  `decline_rail` · `reversal_rail`.
- **ALL copy incl. every in-figure label** lives in `mechanism-clarity-data.json` (R4); numbers +
  quotes are pulled BY CLAIM ID (R3).
- **CSS**: the `kd-ep-fam__*` vocabulary in `drawer-knowledge.css`. Reuse before adding.
- **Probe**: `tools/render_probe_copper.js` (47 checks) is the template — copy it per element, and
  KEEP its regression pass on an already-shipped element.
- **Mockup shell**: `temporary/copper-header-combined.html` — copy it, don't rebuild it.

## TWO NEW GATES (2026-07-29) — the lessons are codified, not remembered
- `element_header_complete` (critical) — an element with a shipped header MUST carry BOTH `lede`
  and `why` in `entity-copy.json`. Negative test `tools/test_element_header_complete.py` (10 cases).
- `figure_type_within_standard` (critical) — figure labels are exactly 12.0px and nothing exceeds
  the 17.6px selenium glyph. Negative test `tools/test_figure_type_within_standard.py` (12 cases).

## OPEN DEFERRAL (carry forward)
**`entity-copy.json` holds 3 of 91 essentials** (calcium · selenium · copper). The other 88 have
**neither** a lede nor a why-this-number line. The gate above binds only on elements that HAVE a
header, so this remainder is a labelled WISH, not a covered gap. Luneth wants both on EVERY element
([[daily-target-provenance-always]]) — each `why` must derive from the documented transform chain in
`essentials-targets-data.json`. Rides along per element, or wants a dedicated pass — his call.

## LOAD FIRST next session
- `.claude/rules/element-headers.md` (the playbook) + `.claude/rules/visual-verification.md` (the STOP gate).
- The live gold standards in `views/entity-page.ts`: `renderMechanism` (composed, copper) ·
  `renderOmega6Experience` · `renderPdmClarity`, and their CSS in `drawer-knowledge.css`.
- Memories: [[element-header-playbook]] · [[illustration-type-scale]] ·
  [[header-mockups-in-real-container]] · [[category-color-coding]] · [[element-intro-what-is-claim]] ·
  [[daily-target-provenance-always]] · [[element-sources-at-bottom]] · [[gold-standard-page-workflow]].

## STANDING DOCTRINES (unchanged)
1. Every claim lives in ONE of three homes (essentials/conditions/Explore); search is a retrieval layer.
2. Diet not food; nutrients from the DIET (food OR supplements). 3. NEVER fabricate — verbatim ⊆ sealed book, or GAP.
4. `corpus_seal` + `catalog_seal` are USER-ONLY (per-invocation authorization). 5. Memory index: consolidate only past ~200 lines ([[memory-consolidation-threshold]]).

**Corpus kv432 · 2267 claims · board 78/78 green · repo clean + pushed. Next: pick the SECOND element, read the playbook, run the 4-mockup process.**
