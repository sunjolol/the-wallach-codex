# Next chunk — ★ PHASE G-1 + G-2 + auto-heal DONE (2026-07-09, board 52/52) → NEXT = element A-Z sweep (amounts_wallach_only chain-tightening DONE)

**★ CURRENT STATE (2026-07-09).** Phases A–F COMPLETE + SEALED. Phase-G book mining is UNLOCKED (`corpus_audit_gate` = `phase_g_unlocked`). **knowledge_version 315 · 1233 claims · board 52/52 green · 38 of 91 essentials carry a numeric Wallach target · glossary 204 plain-language terms.** `amounts_wallach_only` now recomputes the full dose-transform chain (not just provenance) — 7-class negative test, commit `95424a85`. Active plan = `chronicle/OVERHAUL-BLUEPRINT.md`.

## What THIS session landed (2026-07-09) — four closed chunks
1. **"Why this number?" box refine** (commit `75cfbcab`) — the coverage detail-view expander now shows ONLY where the newest Wallach number DISAGREES with an older book's figure (26 essentials show / 12 hide; compares each book's UPPER; ties + single-source hide), works for ANY newer book (Epigenetics, Immortality, DDDL — book names dynamic via `wtnBook`), and walks the FULL derivation (stated range → target-the-upper → IU→metric convert → per-100-lb ×1.54 → 2-sig-fig round → posted; Vitamin A = per-form chains + sum). Text sized 15px upper / 13px lower (rem). Trigger codified in `render_probe_knowledge.js` (2 assertions). Logic in `views/knowledge.ts` `whyThisNumberQualifies` + `wtnChain`.
2. **G-2 glossary** (commit `fccbb88a`) — +56 curated plain-language term glosses (dotted-underline tooltips), `dashboard/assets/data/glossary.json` 148 → 204. Source = the Epigenetics end-of-book glossary, but it's a ~1,600-entry ACADEMIC molecular-biology glossary (not Wallach's own plain defs), OCR-messy + digit-laden — wholesale import REJECTED. Curated only the genuinely-obscure terms that actually appear in our claim_text (autoimmune, congenital, goiter, ataxia, myelin, kwashiorkor, telomere…), number-free, common-word-first ([[term-gloss-standard]]). glossary.json is a hand-authored R4 prose store (allowlisted in MANIFEST); gated by `glossary_wellformed` (no digits) + `jargon_terms_glossed`.
3. **Regimen snapshot AUTO-HEAL** (commit `22188a0e`, [[auto-heal-not-user-debug]]) — `state/coverage.ts` `liveNutrients(item)` re-reads LIVE product composition from the vault (`regimen-label-lookup.json`) by exact canonical name at compute time, so a stale saved VALUE self-corrects on next render with no re-adding. Read-time only (NO write-back, no §31). `user_scanned` items + the base HBSP foundation (legacy names lack the ™) keep their own data. Codified in `state/coverage.autoheal.test.ts` (5 cases). Proven live: a stale item heals 5→100 mcg while its scanned twin stays 5.
   - **Optional hardening (deferred):** stamp `product_id` on add (rename-proofs future adds) + heal the base foundation too (its 3 items lack a vault-matching name today). Not blocking — name-match already heals existing + future vault items.
4. **R2 gate chain-tightening** (commit `95424a85`, closes the last R2-family WISH) — `amounts_wallach_only` used to only check that a numeric target POINTED at a Wallach dose claim; it now independently RE-DERIVES the number from Wallach's stated range (upper-of-range → ×IU-factor → ×1.54 weight-scale + round-2sf) and byte-compares to the posted value. TRACE anchors `provenance.original_*` to the sealed claim's dose; IU factors pinned to known physical constants (0.3/0.025/0.67); Vitamin-A summed-parts handled. Split into `_amounts_wallach_only_impl` + thin wrapper so a negative test can drive it; new `tools/test_amounts_wallach_only.py` (baseline + 7 poison classes, 8/8, exit-coded, run: `python tools/test_amounts_wallach_only.py`). Docs reconciled (charter R2 · source-rule · data-flow · targets_derive docstring). Board 52/52.

## ★ NEXT — the one remaining queued item (Luneth-OK'd, not blocking)
1. **Resume the element A-Z sweep.** Immortality is mined only to ~38% (iodine/potassium frontier, char ~299k of 794k); the **back 62% is untouched** — Lanthanum, Magnesium, Manganese, Molybdenum, Sodium, Sulfur, Tin, Titanium, Vanadium, Zinc + the rest of the rare earths. Also Rare-Earths + DDDL under-mined for doses. (Large mining effort — see workflow below + per-book policies.)

## Book-mining workflow (when resuming mining)
Favor the NEWEST book for placement but keep older ([[enrich-tier1-every-book-favor-newer]] / [[favor-newest-wallach-number]]); ≥1 solid claim per book per topic; batch = `corpus_extract finalize` (ADD) + `mine_batch apply` (EDIT) + one USER-authorized `corpus_seal` per batch ([[batch-mining-workflow]], [[editing-sealed-corpus-claims]]); per-book policies: [[iaiyh-mining-policy]] · [[epigenetics-mining-policy]] · [[immortality-mining-policy]] · [[dddl-undermined-remine]]. Keep internal Table/Fig/page pointers OUT of `claim_text` (gate `internal_refs_out_of_prose`, [[front-facing-human-first]]). Text-surgery on a sealed book = edit `.txt` via safe_write → `corpus_resnap --write` → seal (as done for the Epigenetics table).

## Substance triage buffer ([[substance-registry-and-triage-buffer]])
When mining hits a substance with no slug in `eden/catalog/nutrients.json`: leave it OUT of the claim (board stays green), then `PYTHONUTF8=1 python eden/tools/substance_triage.py park --raw-name <raw OCR> --book <book_id> --locator <loc> --context "<text>" [--claim-id <id>]`. At the book's review pass, resolve each against the source IMAGE ([[verify-against-source-images]]): promote real ones to `nutrients.json` + `mine_batch` backfill; reject OCR garbage. `substance_triage_accounted` REDs at book-completion if any entry is still pending.

## Carried follow-ups (deferred at the pre-Phase-G audit sign-off — NOT blockers; in corpus-audit-status.json `deferred_followups`)
1. **Deep linguistic/logic anomaly sweep** ([[linguistic-logic-sweep]]) — FINALIZATION-phase.
2. **Book-source purification** ([[book-source-purification-campaign]]) — 2/6 pristine (iaiyh, DDDL); Immortality → Epigenetics → LETS → Rare-Earths still raw. (NB: the Epigenetics table region was normalized but the book stays `raw` overall.)
3. **3 parked claim notes:** IAIYH-000020 (award vs verbatim) · germanium "osteoarthritis" vs "osteoporosis" wording · RARE-000048 selenium verbatim extension.

## Other still-open
Conditions → product suggestions · recommender weight-tuning · canonical-unit unification · JS size budget (code-splitting) · global styling touch-ups (ONE end pass) · legal/copyright/a11y/i18n ([[legal-copyright-pass-at-end]], ONE end pass).

## The overhaul in one paragraph (context)
Full structural overhaul after book citations were caught hand-typed ~200×. Model: TWO hand-edited sealed sources — the Wallach Corpus (`eden/corpus/`) + the Youngevity Product DB (`eden/products/`, COMPLETE + SEALED) — plus the shared Catalog (`eden/catalog/`). Everything else is GENERATED + freshness-gated. Phases A–F DONE; Phase G (book mining) UNLOCKED; Phase G-1 (Epigenetics dose table) + its detail-view refine + G-2 glossary + regimen auto-heal all DONE. The Charter (R1–R9) + its gates are the enforceable spine; `.claude/rules/` carries the per-domain HOW.
