# Next chunk — TASK (a) Coverage/Knowledge deep-dive: A1 + Products surface DONE → resume at A2

**★ CURRENT STATE (2026-07-08).** Phase F (the Youngevity Product DB pillar) is COMPLETE + SEALED (215 products, remediated, registry-backed, `products.json` + the Catalog `nutrients.json` golden-sealed). On top of it, task (a) — the deferred Coverage/Knowledge deep-dive, now unblocked by real product data — has landed its first two chunks:

- **A1 (`761495be`)** — the app's product vault swapped to the sealed `eden/products/products.json`; the ENTIRE old scraped-catalog product subsystem DELETED as poison (eden-catalog.json + draft + golden, eden_build/seal/verify.py, build_regimen_label_lookup.py, knowledge/products-db.json, 3 eden/derived orphans — 11 files). `prices.json` marketing `description` stripped (432). `eden_hash_integrity` retired → `no_product_marketing_prose` added (strict key-allowlist, negative-tested). Memory [[old-product-system-full-delete]].
- **Products detail-panel surface (`84030f6b`)** — Knowledge▸Products now lists ALL 215 products, each clickable to a full YGY detail panel (price retail+wholesale · serving · directions · supplement facts · blends · other ingredients); the essentials "FOUND IN YGY VAULT" chips are clickable to the same panel. NEW derived `product-detail-data.json` (products+prices join by ygy_id, freshness-gated) + `eden/tools/product_detail_derive.py` + `views/knowledge-products.ts` + `core/schemas/product-detail.ts`. `no_product_marketing_prose` extended (recursive marketing-key scan over the detail artifact). Memory [[product-detail-panel-vision]].

Board **49/49 green**; all committed + pushed (HEAD `84030f6b` at wrap — run `genesis` for exact). Active plan = `chronicle/OVERHAUL-BLUEPRINT.md`.

## ★ RESUME HERE — the remaining (a) chunks, then (b)

1. **A2 — unify the runtime coverage-matcher onto the registry (NEXT; plan-mode it fresh).** The flagged §00.B drift: `state/coverage.ts::matchToEssential()` + `toMg()` is a hand-ported legacy STRING matcher, independent of the rigorous registry resolver `eden/tools/nutrient_resolve.py` (which backs `product-composition.json`). Two resolution truths. Plan: derive a compact runtime resolver map from `eden/catalog/nutrients.json` (essential_aliases + canonical_forms + substance-specific IU→mass factors), inline it into the bundle (new MANIFEST-registered artifact), and rewrite the coverage matcher to consult it — ONE truth. Ship a PARITY gate (the TS runtime resolver ≡ Python `nutrient_resolve` on a shared fixture) + `state/coverage.test.ts`. **VIEW-AFFECTING** (coverage numbers shift — e.g. "Thiamin" → vitamin-b1 will finally map) → visual sign-off.
2. **A3 — cost-per-nutrient recommender.** Promote `product-composition.json` → a consumed surface; build composition + breadth + banded-cost ranking (cost from `prices.json`). HONEST LIMIT: the saturating-adequacy term needs Wallach targets = BLOCKED on (b); ship the structure, adequacy lights up post-mining. Design locked: [[cost-per-nutrient-match-score]].
3. **Conditions → product suggestions** — PARKED until book mining (needs the cure→essential→product link). [[product-detail-panel-vision]].
4. **(b) — the STILL-OWED full corpus audit → Phase G book mining** (+ build the source-anchored substance triage buffer, [[substance-registry-and-triage-buffer]]; gated by `corpus_audit_gate` which freezes the claim count until the audit signs off). [[full-corpus-audit-before-phase-g]].

**★ KEY CONTEXT:** all 91 Wallach targets are honest gaps today (no numeric amount) → the "% toward target" coverage VERDICT awaits corpus dose-mining (b). The registry already powers composition aggregation + cost-per-nutrient + potency comparison NOW. Global styling touch-ups are deferred to ONE end pass (Luneth 2026-07-08) — don't nitpick CSS mid-build.

## The overhaul in one paragraph (context)
Full structural overhaul after book citations were caught hand-typed ~200×. **Model:** TWO hand-edited sealed sources — the Wallach Corpus (`eden/corpus/`) + the Youngevity Product DB (`eden/products/`, now COMPLETE + SEALED) — plus the shared Catalog (`eden/catalog/`). Everything else is GENERATED + freshness-gated. Phases A–F DONE. Task (a) = finish the Coverage/Knowledge deep-dive on the now-real product data (A1 + Products surface done; **A2 + A3 remain**). Then (b) = the owed corpus audit → Phase G mining. The Charter (R1–R9) + its gates are the enforceable spine; `.claude/rules/` carries the per-domain HOW.
