# Next chunk — FULL OVERHAUL. Active plan = `chronicle/OVERHAUL-BLUEPRINT.md` (locked 2026-07-05, AUTHORITATIVE for the how). Board **45/45 green**; all work committed + pushed through `fba6c2a` (2026-07-06 — run `genesis` for exact HEAD). Phases A–D COMPLETE; **Phase E UNDERWAY — Coverage is now data-clean + visually polished + responsive.**

## The overhaul in one paragraph
The project pivoted from whack-a-mole bug-fixing to a full structural overhaul after Luneth caught book citations hand-typed ~200× (drifting from the sealed registry — a cite said 1999 while the registry said 2011). We designed the whole system on paper first (the blueprint), THEN built it. **Model:** TWO hand-edited sealed sources — the Wallach Corpus (`eden/corpus/`, live) + the Youngevity Product DB (`eden/products/`, Phase F) — plus the shared Catalog (`eden/catalog/`: conditions + symptoms; the 90/91 essentials live in `eden/corpus/essentials-canon.json`). **Everything else is GENERATED from the pillars + freshness-gated**, so drift can't ship. Read the blueprint for the Charter (R1–R9), data model, pipeline, enforcement table, per-surface plans, and the 9-phase migration.

## DONE — Phases A–D (the cleanup + gating half). Full per-chunk detail is in the Creator's Log + git; this is the compact map.
- **A — governance reset:** legacy dashboard fully severed; CLAUDE.md + the 12 `.claude/rules/` rewritten to the pillar model + Charter (permanent home `.claude/rules/charter.md`); `no_operating_doc_contradiction` gate; noise purge.
- **B — Catalog pillar built + SEALED:** `conditions.json` (512) + `symptoms.json` (164) + the `references_resolve` gate (a claim slug must be catalogued — closes the phantom-slug hole).
- **C — Derivation pipeline + THE POISON PURGE:** `eden/derived/MANIFEST.json` + `build_embeds.py` + `derived_artifacts_fresh`; Coverage targets now derive from Wallach BOOK dose claims (Youngevity amounts gone) + `amounts_wallach_only` gate; all embeds → esbuild import (D1); the "WALLACH SAYS" stance layer DROPPED as poison (re-authored post-mining; styling kept).
- **D — Collapsed the rotten `knowledge/` layer + landed the R3/R4 gates:** re-homed `coverage_kind` onto the sealed canon + deleted the poison `essentials-targets.json`; deleted the too-basic `catalog/nutrients.json` (→ Phase-F rebuild) + added `citations_reference_registry` / `prose_contained` / `no_hand_duplicated_canonical` LIVE-with-teeth on the clean surface; stripped the dead creators-log subsystem so dashboard.html is a TRUE pure shell. `knowledge/` is gone except `products-db.json` (Phase F). (A late D-follow-up deleted 5 orphaned dead embeds: essentials-benefits-data, essentials-best-supplements, goal-recommendations-data, ingredients-embed, ingredients-quickref-data — imported by nothing, blueprint §6 = regenerated in Phase F/G.)

## ★ THIS SESSION (2026-07-06, later) — vision audit + hardening + Coverage. All committed + pushed.
Luneth re-sent the ORIGINAL overhaul prompt and had me audit everything we built against that vision, THEN we fixed the gaps found.
1. **Four vision-audit "cracks" fixed + each negative-tested** (`9a12ddf`), board **41→45**:
   - `data_artifacts_accounted` (R1 completeness): every `dashboard/assets/data/*.json` must be in `eden/derived/MANIFEST.json` — `artifacts` (derived + freshness-gated) OR the new `accounted` block (hand-authored / externally-gated, each with disposition + reason). Closes the "is the manifest list COMPLETE?" hole that `derived_artifacts_fresh` never covered.
   - `charter_gates_present` (R7 meta-gate) + `exceptions_justified` (R9): both were WISH, now **LIVE** — the Charter can't name a gate that neither exists nor is labeled WISH; every `.claude/invariant-baseline.json` exception must be `{invariant, reason, test}` (bare/reasonless = RED).
   - `corpus_audit_gate` (R8 / crack #4): the mandatory pre-Phase-G full-corpus audit is now **STRUCTURAL** — `eden/tools/corpus-audit-status.json` freezes the corpus at 1203 claims (`phase_g_unlocked=false`); mining a new claim onto unaudited data REDs the board until sign-off. Harness `eden/tools/corpus_audit.py` → `corpus-audit-worklist.md` (first run flagged **15 dose-kind claims with no numeric dose** — the exact class the audit memory predicted).
   - Widened `prose_contained` + `citations_reference_registry` to the legacy regimen-base/scanner/ocr data embeds (+ prose-CONTAINER awareness for `antiListNotes`). charter.md R1/R3/R4/R7/R9 status updated.
2. **Coverage chunk 1 — layout DERIVED from canon** (`f0e728e`, crack #1's deferred half): the 91 tile names + 60 mineral symbols/numbers were hand-typed in `coverage-layout-data.json` (a silent canon duplication). Now the hand-authored `coverage-layout-skeleton.json` holds ONLY the arrangement/chrome + tile key refs, and `eden/tools/coverage_layout_derive.py` fills name/sym/num from `essentials-canon`. `coverage-layout-data.json` moved accounted→artifacts (4 derived total). Verified byte-for-value identical; Luneth signed off.
3. **Coverage chunk 2 — visual polish** (`fba6c2a`): fixed the `you'''re`→`you're` subtitle bug; contained the search bar (right edge now aligns with the "THE WHOLE PICTURE" box's right edge); added a responsive fallback (panel stacks <1160px, rail collapses to icons <820px, phone trims <560px) — desktop 1920×1080 byte-identical (the classic grid-blowout fixed with `minmax(0,1fr)` + `min-width:0`, a no-op at 1920). Luneth signed off. (A tiny follow-up `907846f` marked the Charter R1 coverage-layout WISH as done.)

## ★ Phase E status — per-surface finalize (blueprint §5). The VISUAL/UX half.
Discipline (non-negotiable): ONE surface to 100% before the next (memory gold-standard-page-workflow); functional gates (build · invariants · the per-surface render probe) THEN **STOP for Luneth's visual sign-off** (memory visual-verification; no-ceremony-pragmatism — do the mechanical steps, keep the substantive gates). Priority: Coverage · Knowledge · Regimen · Scanner → Search → Journey (last).
- **Coverage (⌘1)** — data-clean (targets + layout BOTH derive from the pillars + freshness-gated), visually polished, responsive. REMAINING: the per-essential **deep-dive** (mockup `dashboard/components/trace-mineral-tile-detail.html`) + the real coverage **math / best-supplements** (both **Phase-F-gated** — need the Product DB for composition). The hero title/subtitle is still inline view prose (R4 WISH, moves to a content store in E/F).
- **Knowledge (K)** — clean (the Doctrine tab landed last session). **Finishable end-to-end NOW** (corpus-only, nothing blocked) — the strongest candidate for the next surface.
- **Regimen (⌘2)** — DEFERRED to Phase F: real finalize needs the product/recs engine + new §31 slot/wishlist state; ships a LOUD "FABRICATED DEMO SCAFFOLD — Phase-F placeholder" note; the hand-typed cite was already killed (R3). Item rows / dose / remove / add-from-vault ARE real §31.
- **Scanner (⌘3)** — Eden's WALL is gated (`scanner_user_items_marked`); full surface finalize is Phase E/F.
- **Journey (J)** — quarantined, rebuilt LAST (Phase I).

## ★ NEXT (pick with Luneth)
Coverage is largely done. Options: (a) **Coverage per-essential deep-dive** vs its mockup (doable now, closes Coverage's UX); (b) pivot to **Knowledge** (finishable end-to-end today, nothing blocked); (c) **Phase F — Youngevity Product DB** (Luneth hand-mines `eden/products/`), which unblocks the Coverage math + Regimen.

## Then F → G → H → I
- **F — Youngevity Product DB** (Luneth hand-mines `eden/products/`): + `products_verify` + `products_derive`; the nutrient/ingredient registry rebuilds RICHER + product-shaped (the deleted `catalog/nutrients.json` returns) → `references_resolve`'s substance half RE-LIGHTS; retire the vestigial `eden_build.py` / `tools/build_regimen_label_lookup.py` / `knowledge/products-db.json` / `eden/eden-catalog.json` / `eden/SCHEMA.md` (all doc-labeled vestigial). Wire best-supplements + coverage composition math to real products.
- **G — resume book mining** into the clean gated pipeline — but ONLY AFTER the FULL corpus audit signs off (now enforced by `corpus_audit_gate`; see OWED below).
- **H — Search** (retrieval-first, offline helper). **I — Journey** (last) + the portable offline browser.

## ★ OWED BEFORE PHASE G (do-NOT-forget — item 1 is now STRUCTURALLY GATED)
1. **The FULL corpus audit** — audit ALL ~1203 claims (every kind) before any new mining; spot-checks found mis-labels + off-source cites + a flawed daily-amount rule. **NOW ENFORCED** by `corpus_audit_gate`: mining onto unaudited data REDs the board. To do it: run `PYTHONUTF8=1 python eden/tools/corpus_audit.py` → review `eden/tools/corpus-audit-worklist.md` with Luneth → set `phase_g_unlocked=true` + re-anchor `frozen_claim_count`. (memory full-corpus-audit-before-phase-g.)
2. **Consolidate the mining-mechanics memory cluster** (reading-and-correcting-scanned-pdfs, editing-sealed-corpus-claims, batch-mining-workflow, cross-book-uniform-correction, quote-sync-mechanism, …) — they redundantly repeat the seal-order trap; merge preserving every recovery recipe (memory consolidate-mining-mechanics-before-phase-g).

## Backlog + left-noted (not lost)
- **OLD BACKLOG:** menaquinone→phylloquinone book correction + the K1/K2 alert-box → Phase E/G; Immortality + DDDL re-mine (both under-mined) → Phase G.
- **LEFT-noted cosmetics (no gate impact):** dashboard.html's header references a non-existent `dashboard/ARCHITECTURE.md` + carries stale "Round 2-5" mount-slot comments (fix as the shell's surfaces finalize); the SEALED `design-system.css` header still references `knowledge/design-wisdom` (fix at the next design-system re-seal / project end); ultra-narrow Coverage tile-name wrapping is cosmetic.
