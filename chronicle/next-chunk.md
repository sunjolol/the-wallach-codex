# Next chunk — ★ PHASE H MIGRATION · **H2 chunk 1b (essential entity page) BUILT + committed as a WIP checkpoint — NOT signed off; OPEN ISSUES to fix FIRST** · then finish 1b → condition page

**★ CURRENT STATE (2026-07-11, end of a DEGRADED session) — board 61/61, all 8 render probes pass (0 page errors), everything committed as a WIP checkpoint (NOT visually signed off).** Chunk 1a (data boundary) landed earlier (`c0aba70c`). This session built chunk 1b — the VISIBLE essential entity page:
- `views/entity-page.ts` (NEW) — data-driven essential page (kd-ep-*): back · hero · lede · "At a glance" (Wallach target + dotted-hover why + real ep-bar coverage + ep-src best sources) · Worth-knowing facet cards · condition(orange)/works-with(green) pills · full record (kind groups + filter) · Keep-exploring(violet). Projects `getEssentialPage(slug)`; gate-bound (the 4 view gates BIND it).
- `entity-copy.json` (NEW) + `core/schemas/entity-copy.ts` + `state/entity-copy.ts` — hand-authored, USER-APPROVED per-entity lede + short "why" store (R4 prose home, clean surface). Calcium's lede (= demo text) + why DRAFTED, pending Luneth approval.
- `knowledge.ts` — `renderEssentialDeep` delegates to `renderEssentialPage`; old why-derivation (renderWhyThisNumber + WTN*) + getTargets + renderOmegaClarity removed/moved. CSS `kd-ep-*` added to `drawer-knowledge.css`. MANIFEST + invariants updated (entity-copy accounted + prose-home keys lede/why; entity-page.ts in `_CLEAN_VIEW_FILES` + `_ENTITY_VIEW_FILES`). New `tools/render_probe_entity.js`; `render_probe_knowledge.js` retargeted.

**★ THE SESSION DEGRADED — root cause: I repeatedly REUSED OLD app components / followed the plan's "reuse/port" notes instead of REPLICATING the signed-off demos. Luneth corrected the same class of mistake ~4×.** GOVERNING RULE (always): **replicate the LATEST REFINED demo EXACTLY — never reuse an old app component (kd-meter/kd-source/kd-why/renderCorpusClaim primitives) or an OLDER prototype-FILE version just because the blueprint/next-chunk says "reuse"/"port." If a plan note contradicts the demo, the DEMO wins.** [[replicate-demo-not-blueprint-notes]], [[entity-page-redesign-demo-phase]].

## ★ OPEN ISSUES — fix FIRST next session, then STOP for Luneth's visual sign-off
1. **★ THE ISSUE Luneth flagged — best sources: show 6 products, NOT 5, + the "best value" bubble.** `renderAtAGlance` in `views/entity-page.ts` sets `const TOP = 5`; the REFINED demo shows **6** before the "Show all" expander → change to 6. Verify the **"best value" bubble** matches the refined demo (current build has a small inline `kd-ep-vtag` tag; the refined treatment may be a distinct bubble). I missed this by replicating the OLDER `temporary/essential-page-prototype.html` (shows 5) — read the LATEST refined demo (`temporary/knowledge-drawer-prototype.html` best-sources) or ASK Luneth for the exact refined spec.
2. **Full-record claim cards — SUSPECTED deviation (verify w/ Luneth; he said this was NOT the flagged issue but it is unconfirmed):** record cards render OLD corpus primitives (renderDoseBlock "DOSE" block · renderFig81Legend · renderRefHeader · "CITED · <book>" cite), ported per the plan. The demo uses ONE clean claimCard everywhere (question · answer · verbatim · "— Dr. Joel Wallach, cite"). Worth-knowing cards match the demo; record cards do not (and differ from each other). Decide with Luneth: one clean card vs keep dose info in a demo-consistent style.
3. **Lede + why need Luneth approval** — Calcium's drafted in `entity-copy.json`; present, approve, then author the rest one at a time (NEVER auto-derive).
4. **Correct the misleading plan notes** — the 1b build-plan BELOW + `phase-h-migration-blueprint.md` §4 H2 still say "REUSE renderIntakeMeter/renderEssentialSources/renderWhyThisNumber" + "PORT the claim primitives" — those notes CAUSED the deviations. Rewrite to "replicate the demo's components with real data." (why-derivation reuse already undone; best-sources count + record-card reuse are the open ones.)
5. **Products tab + product detail = still H3/H5** (deliberate, confirmed keep-plan-order). Drawer still 520px (H3 widens to 950).

---
_The 1b build-plan below is the ORIGINAL plan; its "REUSE/PORT" notes are SUPERSEDED by the governing rule above (replicate the demo). Kept for reference only._

## ★ H0 — DONE (4 chunks, board 53→57, all pushed to origin)
1. **Governance** (`9b0cfd03`) — defused the OVERHAUL §5.5 generative-LLM landmine IN PLACE (D4 marked RESOLVED/shelved) + marked §5.2 superseded; froze `entity-page-redesign-blueprint.md` (top banner + §4/§11 status). CLAUDE.md "Surfaces:" reword left for **H7** per delete-manifest §3 (do it once the merged palette exists in H4).
2. **Enforcement floor** (`8979cbe3`) — 3 view gates + committed negative tests, landed BEFORE the surfaces: `views_no_inline_prose` (clean-surface-scoped, `_CLEAN_VIEW_FILES` EMPTY in H0 → append each view as migrated H2-H4), `entity_render_is_projection` (`_ENTITY_VIEW_FILES` EMPTY → H2; scans the real 947-id set), `no_stub_render_paths` (7 demo-scaffold tokens over views/*.ts + styles/*.css). Tests: `tools/test_{no_stub_render_paths,views_no_inline_prose,entity_render_is_projection}.py`.
3. **Content-store** (`3ce4842f`) — `dashboard/assets/data/view-copy.json` (hand-authored, R4 prose home: `kind_labels`[14] + `facet_labels`[13] + `ui`{} grows H2-H4) + `core/schemas/view-copy.ts` + `state/copy.ts` (`kindLabel`/`facetLabel`/`ui`, graceful fallback). `FACET_LABEL` moved OUT of `core/schemas/search.ts` → single-source; search probe confirms byte-identical render. +gate `kind_label_covers_corpus` + `tools/test_kind_label_covers_corpus.py`.
4. **Generator** (`f9695494`) — `eden/tools/entity_page_derive.py` → `dashboard/assets/data/entity-page-data.json` for ALL 597 entities (Luneth's call). LEAN: claim IDs + derived extras (co-occurrence `related[]`, `protocol_claim_ids`, derived `one_liner`, synonyms, claims by KIND) — NO claim text (resolves at render from corpus-embed + search-index). LOOSE file → 0 bundle impact until H2. Wired fake-fns #3 related / #4 synonyms / #5 counts / #6 protocol; #1 coverage% + #2 covers-N stay LIVE via `coverage.ts`/`recommender.ts`. MANIFEST 10th derived artifact. Sweep: 597/597 `record==claim_count`, 0 unresolved refs, exemplar counts match prototypes (Calcium 102/5/8, Osteoporosis 33/6).

## ★ H1 — DONE (2 chunks, board 57→61, both pushed to origin) + fan-out verified
1. **Colour-category map** (`346eb363`) — the LOCKED colour language (§6) is now enforced data: `view-copy.json` +`kind_categories` (all 14 kinds → 6 families green/teal/amber/orange/violet/red); `core/schemas/view-copy.ts` +field; `state/copy.ts` +`kindCategory()`. Gates `claim_category_mapping_total` (total+exact over sealed kinds, no default branch) + `view_category_not_hardcoded` (surface-scoped `_ENTITY_VIEW_FILES`, binds H2) + 2 negative tests. **Luneth ACCEPTED the 3 default colours: quote→orange · personal_anecdote→violet · food_source→teal.**
2. **Pill-derivation fix + prominence** (`557eabf8`) — the essentials[]-union leak killed via ONE directed relation `maps(E,C)`: a restore/help-with pill appears iff a claim links E,C where kind=protocol/dose (always) OR deficiency_sign/prognosis that is NOT a shotgun (≥3 essentials AND ≥3 conditions). `works_with`=interaction-kind (not co-occurrence); `restore`/`conditions` pills use maps(); `protocol_claim_ids` drops base-line-table rows (PROMINENCE — fluoride out of the curated "what to do" slot). `corpus_embed.py` +`base_line_table`; `corpus.ts` +field. Gates `entity_pills_justified` (independent re-derivation — every pill backed; baseline all 2568 pills justified) + `no_positional_hero` + 2 negative tests. Results: osteoporosis restore 16(leaky)→8 clean · calcium works_with 41(co-occ)→4 real · 5 D2 misfits + phosphorus-in-restore gone · fluoride out of the primary slot.
3. **Fan-out fit re-check** (workflow `wf_a7ea82b2-c73`, 17 entities, 0 errors) — VERDICT: **leaks gone corpus-wide (17/17 clean); NO rule-logic flaw.** Over-correction = 2 DATA-side classes (fix UPSTREAM in mining, NEVER loosen the rule — that would reopen leaks): (a) the flattened **Table 7-8** (`WAL-CLM-RARE-000275`) — 5 dropped ties (hypertension→Mg+B, osteoporosis→Cu+S, bone_spurs→B); (b) a mis-tag class — copper→goiter (Adriatic-island evidence) + bone_spurs→copper, tagged mechanism/interaction not deficiency_sign.

## ★ MIS-TAG RE-TYPES — ✓ EXECUTED & SEALED 2026-07-11 (kv 321->322)
Luneth APPROVED all 43 re-types + the IMMORT-000001 split (artifact https://claude.ai/code/artifact/ec860c9c-530a-4da0-91a1-56eeff196399); executed this session per the runbook (finalize split -> mine_batch re-type -> seal ONCE -> embed/build/invariants/probe). What landed:
- **43 claims re-typed** mechanism/interaction -> deficiency_sign (dddl 8 · epigenetics 4 · immortality 13 · lets-play-doctor 1 · rare-earths 17) — each nutrient now reaches its condition's restore pills.
- **IMMORT-000001 SPLIT** — the flattened original narrowed to goiter<-iodine; 4 new 1-condition claims added (IMMORT-000211 keshan_disease<-selenium · 000212 cardiomyopathy<-selenium · 000213 pica<-phosphorus · 000214 enzootic_ataxia<-copper), each sharing the full 307-char verbatim (names every disease -> verbatim_names_mapped_conditions holds).
- **Verified:** board 61/61; `entity_pills_justified` 2578->2600 (no union leak — nothing over-fired); knowledge render probe green (0 page errors); 11 spot-checks pass, each backed by the expected claims — goiter<-copper (DDDL-000025/IMMORT-000180/RARE-000149) · prostate_cancer<-selenium (IMMORT-000011) · diabetes<-chromium (IMMORT-000089) · hypertension<-calcium (IMMORT-000071) · deafness<-manganese (EPIGEN-000010) · aneurysm<-copper (IMMORT-000107/RARE-000120) · the 4 split conditions each backed by IMMORT-000211..214.
- **NOT touched (decided):** RARE-000086 KEEP (inferred pairings, §00.A); EPIGEN-000095 + IMMORT-000198 KEEP mechanism (verifier rejected). Source audit: workflow wf_b814baa8-ca4. Full 43-item detail recoverable from git (commit e74ffbbc, the deleted exec dir).
- **Precedent + reading:** [[flattened-table-claim-split-not-loosen]] · [[editing-sealed-corpus-claims]] · [[batch-mining-workflow]]. The Table 7-8 split (242a55d4) was the exact precedent.

## ★ H1 — DONE + Table 7-8 split DONE (all pushed to origin) + fan-out verified


## ★ NEXT — H2 chunk 1b: the VISIBLE essential entity page (blueprint §4 H2)
Chunk 1a (the DATA foundation) is DONE + committed. Now build the render — the FIRST real-build visual surface -> STOP for Luneth's visual sign-off. **Chunk plan (each ends in a visual STOP):** 1a foundation ✓ · **1b = essential page (Calcium exemplar)** · 2 = condition page (Osteoporosis, help-first) · 3 = H2 close (delete WALLACH-SAYS + dead deep-views, adversarial leak-check, round-close).

**1b build — a NEW file `views/entity-page.ts` (gate-bound; NOT knowledge.ts, to keep the old drawer code OUT of the clean-view gate scope):**
- Re-implement the signed-off D1 prototype `temporary/essential-page-prototype.html` in the `kd-ep-*` namespace, projecting from `getEssentialPage(slug)`. Section order: back · hero(symbol) · lede(one_liner) · "At a glance"(Wallach target + why-this-number + REAL coverage meter + best sources) · "Worth knowing"(search[] facet cards) · "Need help with a condition?"(conditions[] ORANGE pills) · "Works with"(works_with[] GREEN pills) · "The full record"(record[] kind groups + keyword filter) · "Keep exploring"(related[] VIOLET pills). DROP the prototype's PROTOTYPE-v4 footer (the `no_stub_render_paths` gate forbids the token).
- REUSE (import, do NOT re-derive): `renderIntakeMeter`+`tileOf` (views/knowledge-corpus), `renderEssentialSources` (views/knowledge-products). knowledge.ts computes `renderWhyThisNumber(td)` and PASSES the whyHTML string into renderEssentialPage (entity-page.ts must NOT import knowledge.ts -> circular).
- PORT the load-bearing claim primitives INTO entity-page.ts (own them; the knowledge-corpus copies die at H3): FIG_8_1_FOR_CONDITION/isFig81Row, fig81OwnRow, formatDose, doseContextLabel, collapseWS, glossCol, renderDoseBlock, renderFig81Legend, renderRefHeader — re-namespaced `kd-ep-claim`. (knowledge-corpus.ts:137-310 are the originals.)
- TWO card renderers: (a) search Q&A card ("?" badge; question+answer_short preview+answer+verbatim+composeCite+topics; resolve via getSearchClaim). (b) record/statement card (NEUTRAL mark; truncated claim_text summary -> full claim_text+dose+fig81-legend+ref-header+verbatim+cite; resolve via getClaim/resolveClaims).
- COLOUR: record-kind family via `kindCategory(kind)` -> `data-family="..."` attr; search-facet family via CSS `[data-facet]` selectors (matches prototype). NO bare family-word literal in TS (`view_category_not_hardcoded`). NEVER pick a hero by `claims[0]`/`record[0]` (`no_positional_hero`).
- CSS: add `kd-ep-*` rules to `drawer-knowledge.css` ADDITIVELY (do NOT delete legacy kd-essential-deep/kd-corpus/kd-claim — H6's from-empty rebuild does that). Family palette = `#drawer-knowledge-mount`-scoped custom props using the prototype's BRIGHTER `--fam-*` hues (science #2f9dba · action #5aa82c · signs #d69a24 · story/violet #8a52d6; orange=--ds-accent · red=--ds-status-err). Violet is the only genuinely-new hue; do NOT touch the sealed design-system.css.
- PROSE: the 2 sentence-y `ep-line` leads -> `view-copy.json` `ui{}` (empty today) + pull via `ui(id)` with a `{n}`-replace for counts; short labels ("At a glance" etc.) may stay inline. NO >=12-word literal in the view (`views_no_inline_prose`).
- GATES (same patch): APPEND `dashboard/assets/js/src/views/entity-page.ts` to BOTH `_ENTITY_VIEW_FILES` (invariants.py:2634) AND `_CLEAN_VIEW_FILES` (:2629) — else the 4 view-half gates pass VACUOUSLY (ships unguarded).
- WIRE: knowledge.ts `renderEssentialDeep(key,snap)` -> `renderEssentialPage(layoutKey, snap, whyHTML)`. layoutKey->slug via `getEssentialByLayoutKey(layoutKey).slug`; coverage meter + best-sources still join by layoutKey. Pills emit data-kd-condition (slug) / data-kd-essential (slug->layoutKey) — same-tab nav works via the existing handler; cross-tab pill nav is H3 (breadcrumb).
- VERIFY: build + invariants (61/61, gates now BIND, add negative-test coverage if a gate needs it) + a NEW essential render probe (`tools/render_probe_entity*.js`) -> **STOP for Luneth visual sign-off.**

**★ RESUME CONTEXT — the 6-agent understand sweep (`wf_c4511ffd-cf6`)** saved the full subsystem maps (prototype markup+CSS line refs, artifact schema, primitives, gate mechanics + line numbers, CSS/family tokens) at `AppData/Local/Temp/claude/C--Users-Light-Desktop-claude-health-expert/5ff01ed8-.../tasks/wzgx6vhqi.output` (temp — may be cleared; re-run the understand workflow if gone). Prototype already read this session: essential-page CSS = lines 9-169; render markup via `awk 'NR>=176 && length($0)<600' temporary/essential-page-prototype.html`; claimCard body = lines 183-203; op-grid = 224-236.

**Two items to surface at 1b sign-off (Luneth's visual call):** (a) Calcium's derived `one_liner` pulled a long metallic-calcium TABLE (the "tangential-definition mis-fit") -> decide better-heuristic vs authored ledes for key entities; (b) claim-card badge: neutral mark on record/statement cards vs "?" on Q&A cards.
- **Read first:** `chronicle/phase-h-migration-blueprint.md` §4 (H2) + §3 (delete-manifest) · memory [[entity-page-redesign-demo-phase]].
- **Per-phase discipline:** board-green + render probe + Luneth visual sign-off before the next; nothing old deleted until its replacement generates correctly (never a gap where the app breaks).

## THREAD 1 (✓ COMPLETE 2026-07-11 — all demos signed off; feeds the master blueprint above) — Entity-Page Redesign · D3 = the Knowledge drawer
**★ READ FIRST: `chronicle/entity-page-redesign-blueprint.md` §8.5** — the CONVERGED D3 direction (after 2 REJECTED passes). Also memory [[entity-page-redesign-demo-phase]].

**Vision:** one presentation unit — the entity page — for every subject; the faceted format generalized to essentials + conditions. **Colour language (LOCKED):** teal=science · orange=Wallach's positions (sparse) · green=what-to-do · amber=signs · violet=story/lore · red=cautions. Pills: conditions=orange · nutrients=green · explore=violet. Orange TEXT=deep, never bright; opened cards keep their own family colour.

**SIGNED OFF:** D1 (Essential/Calcium) `temporary/essential-page-prototype.html` · D2 (Condition/Osteoporosis, help-first) `temporary/condition-page-prototype.html`.

**D3 CONVERGED — NO layout change (blueprint §8.5):**
- Knowledge stays a DRAWER, widened 420→**950px**, adopting the LOVED browse-shell inner design. **`temporary/browse-shell-prototype.html` = the reference Luneth approved the FEEL of** (his mockup = its Home). 5 tabs: **Home · Essentials · Conditions · Explore · Products** (DROP Corpus + Doctrine). Home = the FULL browse-shell home (hero "Everything Wallach taught, in one place" + **live-suggest search kept** + shelves). Elegant rounded segmented tab menu; the KNOWLEDGE header is a SUBTLE eyebrow, not a big title. **NEVER "91"** — tab reads "Essentials", counts say "90" ([[essentials-authority-graphic]]).
- ONE consolidated **"Ask Wallach"** search (command-palette-v3 pattern, split results: Jump-to entities + In-the-books claims). Generative AI SHELVED (infeasible offline). Search + browse are COMPLEMENTARY, not redundant.
- Phases: **P1 = the 950px drawer** ✓ → **P2 = the unified Ask Wallach search** ✓ → **P3 = Products detail overhaul** ✓ (all signed off 2026-07-11; all open the D1/D2 entity pages). **ALL D3 phases DONE → NEXT = the unified master blueprint (D1–D5) → build Phase H.**

**★ P1 STATE — ✓ COMPLETE, ALL 5 TABS SIGNED OFF (2026-07-10).** Demo: **`temporary/knowledge-drawer-prototype.html`** @**950px**, built on the REAL shell (dashboard.css + drawer-shared/drawer-knowledge.css). All 5 tabs done + visually approved: **Home · Essentials · Conditions · Explore · Products.**
  - **✓ FIXED — the Explore-chip bug:** they now carry real base pill styling inside `#drawer-knowledge-mount` (the `.sr-pill` base is scoped to `#drawer-search-mount`, so it never reached the knowledge context). Header also cleaned: dropped `· KN·07`, tab menu centered.
  - **✓ Essentials cards** made byte-identical to `browse-shell-prototype.html` (0-diff computed-style check), then polished per Luneth: natural-case Space-Grotesk names +1px, claims count +1px matching the name colour, cards widened so NOTHING wraps, sorted by claim count to top-18 (clean 2-gap grid), + a colour-key legend under the grid.
  - **✓ Essentials tab** built to match browse-shell's Essentials view: 6 category sections (3 mineral sub-groups + Vitamins/Amino/Fatty) via a ported slugs-only `GRID`, 91 tiles, 0 broken, 0 wraps; label NEVER says "91" (uses "The essentials"); omega-9 kept as the 91st at "0 claims". Reuses the approved cards. Dropped browse-shell's demo footer; no colour legend (subheads name the categories).
  - **Conditions** — browse-shell's approved design: `ep-seclabel` "All 506 conditions" (sorted by claim count) + accent filter box w/ live count + 3-col `sh-condgrid` of orange-accent rows (`name · N claims · M nutrients`, singular/plural-correct). Filter is **sticky** — a `.sh-stick` shield pinned flush under the header (negative `top` covers the body's 16px top padding; Chromium pins sticky to the CONTENT-box top so a negative margin can't lift the box, only a negative `top`), symmetric ~12px gap above/below.
  - **Explore** — topics grouped by type (Therapies & ideas · Big concepts · Elements · Substances · People), 40 violet `ep-explore` pills.
  - **Products** — flat list of 215 sorted by nutrient coverage, 2-col `sh-prodgrid` of green-accent rows (`name · covers N/90 · price`; zero-coverage shows just price), sticky filter via a shared `wireFilter` helper. Detail panel **deferred to P3**; rows clickable-ready (`data-nav="p:id"`). Product data injected as `const PRODUCTS` from the product-detail + recommender artifacts.
  - **Spacing pass (Luneth):** first section-label per tab trimmed 15px via `.ep-seclabel--tight` (hero + between-section gaps untouched); the sticky filter given symmetric breathing room.
  - **✓ DONE 2026-07-10 — tiles/rows WIRED to the D1/D2 entity pages** (3 chunks, all headless-verified w/ 3 puppeteer probes, board 53/53, gitignored prototype): **(1)** Calcium tile→D1, mounted in a nested `#drawer-search-mount.ep-standalone` host so drawer-search.css + D1's scoped `.sr-*`/family-colour rules apply VERBATIM (no re-scoping — sidesteps the process-lesson-#2 trap); kept `.ep-seclabel`/`.ep-cloud` as the drawer's copies so browse tabs stay untouched; removed the carried-over "PROTOTYPE v4" footer. **(2)** Osteoporosis row→D2 (+ its style delta: caution=red `--ds-status-err`, reference=teal, `.ep-protocol`); `essNameToSlug`/`condNameToSlug` resolvers → a branch pill opens a BUILT page if one exists, else a labeled "lands in the real build" stub; Calcium⇄Osteoporosis branch both ways. **(3)** breadcrumb trail + history stack (`trail[]`, `crumbFor`/`sectionOf`/`goCrumb`) REPLACING the single back button — a parent crumb returns to that EXACT page (fixes Luneth's "hitting back and not going back"), root crumb exits, baked `.sr-entity__back` hidden. **★ BREADCRUMB FEEL SIGNED OFF 2026-07-11** — and refined to a BOUNDED, LOOP-FREE history (Luneth caught the crumbs growing forever on Calcium⇄Osteoporosis ping-pong): `trail[0]` is ALWAYS a "Home" anchor (unbreakable fallback); re-visiting an entity already in the trail JUMPS BACK to it (truncates) instead of appending; and the trail caps at `CRUMB_MAX=6` (Home + 5 most-recent, oldest dropped) — a one-line tunable at knowledge-drawer-prototype.html:418. Headless probe green (ping-pong stays len 2; cap holds; Home exits). **NEXT = P2 (unified Ask Wallach search) → P3 (Products detail overhaul).** Only the 2 exemplars have hand-built page data (Option A, Luneth's call); generalized data-driven per-entity rendering + `knowledge.ts`/`drawer-knowledge.css` (kd-*) = the real build (Phase H).
**★ P2 STATE — CHUNK 1 ✓ COMPLETE + SIGNED OFF (2026-07-11).** The unified **"Ask Wallach"** retrieval palette, in `temporary/knowledge-drawer-prototype.html` (gitignored). Command-palette MODAL (centered 720px scrim+modal, `ap-*` namespace, green Ask-Wallach identity), opens via the green topbar button / rail item / `/`; closes via the app's normal `.knh__close` X + Esc + click-outside. **Split results:** "Jump to" (entities via the existing `searchAll` live-suggest — family-coloured, match-highlighted) + "In the books" (the 198 mined faceted claims as **question + one-line quick-answer**, `facet · book` meta). **Zero-query = discovery taster** (Try-asking chips · Common conditions · Popular essentials). Opening a result routes through `nav()` → the SAME D1/D2 entity pages (fresh `Home › …` root). **Claim-jump:** clicking an "In the books" question lands on the subject page, opens that exact card, smooth-scrolls to it, and flashes a **~1s green wash OVER THE CARD** (matched by `.sr-claim__q` text). Blur-lag fixed (removed the opacity anim on the backdrop-filter element — it reblurs every frame; blur 4px). Data = `const CLAIMS`/`const BOOKS` injected from `search-index.json` (198/6). Verified headless (multiple puppeteer probes; zero page errors). **★ CHUNK 2 ✓ SIGNED OFF 2026-07-11 — the ranking/synonym pass** (§8.5 #4): (a) claims re-injected WITH the full `answer` text (recall) + a resolved `SEARCH_ENTITIES` synonym table (56 entities, all synonyms searchable); (b) TIERED scoring (`apTier` exact›prefix›word-start›substring; `nameTier`×20 + element-symbol=100 so "cr"→Chromium; `aliasTier`×16) in `rankEntities`/`rankClaims` replacing the crude match; (c) synonym wins — "blood sugar"→Diabetes, "quicksilver"→Mercury, "b12"→Cobalt, "climate"→Carbon — each Jump-to row shows a **"via <synonym>"** hint; (d) claim-boosting (matching an entity boosts its claims); (e) **CHARGED-CONTENT GATE** — `CHARGED={homosexuality,intersex}` NEVER surface in search unless the query names them (name exact/prefix OR exact synonym); incidental body-substring (e.g. "testosterone" pulling a homosexuality claim) is BLOCKED; Explore tab EXEMPT (full index). **NEXT = P3 (Products detail overhaul).** Deferred to the search-MINING phase (Thread 2, NOT prototype code): products in Jump-to + topic destination pages (Phase H); + the men's-health capture below.
**★ P3 STATE — ✓ DONE + APPROVED 2026-07-11 (the Products detail overhaul — LAST redesign demo).** In `temporary/knowledge-drawer-prototype.html` (gitignored): clicking any of the 215 products opens a full product ENTITY PAGE (D1/D2 skeleton) — hero (GREEN box icon + green name = product family) · factual lede (no marketing, §00.A) · "at a glance" (WHOLESALE price big + retail small · per-serving · N/90 coverage · cost/serving) · supplement-facts table per component (BOLD names + amounts; multi-component parts labelled by `role`) · **category colour bars matching the Essentials palette** — `--cat` set on `.pd-nrow[data-cat]` (the bug: `--cat` only lived on `.sh-tile`, so rows fell back to dark ink; fixed by mirroring the palette) + a per-product colour LEGEND inline-right of the "Supplement facts" heading (all-4 worst case fits one line) · **nutrient rows LINK to their essential page** (24/29 on BTT) via `resolveNutrientSlug` (name + alias map Thiamin→B1 / Folate→B9 / D3→D + L-/DL- amino strip — only ever resolves to a REAL page) · blends collapsible · directions callout · green "essentials on this label" pills. **Breadcrumb generalized:** `trail[0]` = the ORIGIN TAB (Products/Essentials/…), not always Home; `goCrumb` re-syncs the highlighted tab on back-nav (fixed it staying on the wrong tab). **Label order kept AS PRINTED (not regrouped)** — the FDA has-%DV-vs-other structure is meaningful; the colour bars give the category scan without reordering. Full record: [[entity-page-redesign-demo-phase]].
**★ WHOLESALE RULE-OVERRIDE (Luneth 2026-07-11, [[wholesale-featured-price]]) — LANDED IN THE REAL APP + committed.** Wholesale is now the FEATURED price everywhere + ALL cost calcs; retail preserved as secondary/reference. Real-app: `eden/tools/recommender_derive.py` joins `price_wholesale` (retail fallback) → regenerated `product-recommender-data.json`; `state/recommender.ts` + `core/schemas/recommender.ts` + `views/knowledge-products.ts`. Prototype: product page at-a-glance, Products-tab rows (`wsPrice`), D1/D2 best-sources (re-ranked on wholesale). Verified build + invariants 53/53 + render probes; an adversarial completeness audit (6 agents) found 0 surviving retail-primary assertions. SUPERSEDES every old "use retail/MSRP" note.
**★ PROCESS LESSONS (this session degraded — Luneth frustrated; do better):** (1) When Luneth says "X isn't styled / doesn't match," pin down the EXACT element BEFORE touching anything — ASK if ambiguous (I misread "buttons" as the tab menu 3×; he meant the purple explore chips). (2) Reused `.sr-*` classes ONLY work under their mount id — verify scoping. (3) MEASURE, don't eyeball — zoom + computed-style diff per `.claude/rules/visual-verification.md`; do a real side-by-side vs his mockup. (4) Fix ONLY what was flagged; STOP over-correcting unrelated things. (5) Smallest chunk → show → confirm.

## THREAD 2 (QUEUED) — Search G-7 + book mining
**knowledge_version 322 · 1259 sealed claims · board 61/61 green.** Search G-7 migration is COMPLETE (56 entities / 198 enriched claims; batch 9 = the charged cluster, landed + committed `e632b204`). Outstanding, in order:
1. **SEAL the 2 search source files** — `eden/corpus/search-enrichment.json` + `eden/catalog/search-entities.json` (now stable). Add index sharding if large.
2. **Resume mining search-first** from Immortality element A-Z at **Mn-Manganese**, full taxonomy per `.claude/rules/search-corpus.md`.
3. **Cross-book capture** the wrongly-skipped older homosexuality/intersex treatises (Rare Earths Ch7 + DDDL + IMMORT-000020) + MORE Quackbusters/medical-freedom material — per the CORRECTED doctrine (capture ALL books; newest-wins is CONTRADICTION-handling ONLY; run charged content past Luneth). The "newest-book-only capture" rule was PURGED 2026-07-10.
4. **★ NEW capture priority (Luneth 2026-07-11) — GENERAL-INTEREST LAY TOPICS people actually search.** Mine + tag Wallach's men's-health / strength / vitality material (minerals for muscle / athletic performance / testosterone / male vitality) as a real entity (e.g. `mens_health` / `strength`) with the LAY synonyms men type ("increase testosterone", "get stronger", "build muscle", "stamina", "virility"). Today the corpus has NO such content (only incidental mentions) so those queries return empty — the P2 ranking machinery is READY; it just needs the tagged data. Proactively tag general-interest lay topics so plain-language questions land. [[general-interest-lay-topic-tagging]], [[ask-wallach-search-vision]].
5. **★ Charged-search-gate doctrine (Luneth 2026-07-11):** homosexuality/intersex (and future charged topics) must NEVER appear in SEARCH results unless the query EXPLICITLY names them (their term/synonym — "gay", "homosexual", "lesbian", …). They belong in the Explore tab (a full categorised index) ONLY. Enforced in the P2 prototype via `CHARGED` + `chargedExplicit`; PORT to the real search build. Backfill missing synonyms (e.g. add "lesbian" → homosexuality) so the gate catches every term. [[charged-search-gate]], [[editorial-fringe-exclusion-policy]].

## ★ KEY REMINDERS
- **Round-close gotchas** ([[creators-log-append-gotchas]]): Creator's Log `--summary` HARD-capped 280; pass args via `"$(cat file)"` WITHOUT escaping inner quotes; remove any botched empty tail before commit. Log embed bakes into `dist/main.js` at BUILD time — log → rebuild → commit or Profile goes stale.
- safe_write scripting: edit ONE file multiple times → read once + apply all + write once ([[safe-write-crlf-flip]]).
- Prototypes reuse the app's REAL stylesheets + `.sr-*` classes so cohesion is structural; new bits use an `ep-*` namespace + tokens only.

## The doctrine (AUTHORITATIVE)
- Redesign: `chronicle/entity-page-redesign-blueprint.md`.
- Search: `.claude/rules/search-corpus.md` · `chronicle/search-build-blueprint.md` · `chronicle/search-corpus-plan.md`.
- Master plan: `chronicle/OVERHAUL-BLUEPRINT.md` (redesign = Phase H design spine; mining = Phase G).

## Gates + tooling (all LIVE, board 61/61)
`derived_artifacts_fresh` (covers search-index.json) · `search_index_wellformed` + negative test `tools/test_search_index_wellformed.py` · `render_probe_search.js`. The redesign demos are gitignored `temporary/` prototypes — they touch NO pillar, artifact, or app code, so the board is unaffected by them.

## The paused mining roadmap (blueprint §7)
G-4 finish Immortality (resume Mn-Manganese) · G-5 DDDL re-mine · G-6 the 3 new books · **G-7 Search build = migration COMPLETE (seal pending)** · G-8 close-out + seal. Phases H (app completion — now getting its design spine via this redesign) + I (design) after.
