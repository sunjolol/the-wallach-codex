# Next chunk — PHASE F: extraction complete (215 products) → PRE-SEAL REMEDIATION Phases 1–9.1 DONE + Phase 9.2 CHUNK 1 + CHUNK 2 DONE (chunk 1: nutrient registry `eden/catalog/nutrients.json` built, substance-half of references_resolve RE-LIT + strict, `033cc772`; chunk 2: substance-specific unit conversion + product-composition derived artifact + live invariant `product_registry_resolves`) → resume at Phase 9.2 STEP 5: the USER-SIGNED SEAL (see the ★ PRE-SEAL REMEDIATION section directly below for exactly where to pick up). Active plan = `chronicle/OVERHAUL-BLUEPRINT.md`. Board **47/47 green** (47th = `product_registry_resolves`, Phase-F chunk-2; 46th = `mining_coverage_accounted`, the book-mining coverage gate); all committed + pushed through Batch 25 / the T-range + the coverage-gate tune-up (2026-07-07 — run `genesis` for exact HEAD). Phases A–E are parked; **Phase F is the ACTIVE effort — building the Youngevity Product DB pillar by vision-extracting the product labels. ★ PHASE F EXTRACTION COMPLETE (2026-07-08): 214 products, ALL alphabetical ranges (0-9 through Z) + the GLP-1 line + the 2 reconciliation-found gaps (Sinus & Allergy Support, Zinc + Immune Support). NEXT (post-extraction Phase F): (1) SEAL products.json via eden/tools/products_seal.py (USER-signed golden.sha256 = Eden's wall); (2) wire products.json into the derive pipeline + build for the Coverage math / cost-per-nutrient recommendations (the Phase-F-gated Coverage/Knowledge deep-dive); (3) promote products_verify to a live invariant in tools/invariants.py + add products_hash_integrity / product_labels_accounted / products_derived_fresh per eden/products/README.md. Historical fills: 10 cross-range (FitShake, Super Greens, Activated Charcoal, Imortalium, Mineral STX, collagen gummies/shot, Taheebo, TrueDetox, XeraTest, CardioBeets) + 3 SOLD-OUT removed. W-range (2026-07-08, +3) = womens-fx (4-pack variant), womens-hormonal-balancer, womens-probiotic-complete (16-strain menopause probiotic; two identical label crops). V-range (2026-07-08, +3) = viaviente (puree, 2-pack + 12-pack as variants), vitalstart (VitalSTART cardio stick; servings/container not on panel -> null), vitali-c-plus (was extracted provisionally as "vitalic"; Luneth confirmed name/SKU USLF070044, ygy 9930). NAMING RULE (Luneth 2026-07-07, corrected 2026-07-07): drop the redundant 'Youngevity' prefix wherever it is redundant (Ultimate-branded AND otherwise). CardioBeets DROPS it too -> product_id 'cardiobeets', name 'CardioBeets™ (195 g)' (SUPERSEDES the earlier 'CardioBeets KEEPS it' — that was a misread: Luneth had left 'youngevity' in the filename he pointed to, NOT an instruction to keep the prefix). prices.json sidecar keeps YGY's literal listing slug/name (joined by ygy_id), so this rule touches products.json only.**

## ★ PRE-SEAL REMEDIATION — Phases 1–9.1 DONE; resume at 9.2 then SEAL (2026-07-08)
A Luneth-driven final audit cleaned + VERIFIED the 215-product pillar before sealing. Key meta-lesson: every "gap" the audit flagged turned out to be MY read-analysis error, not the data — the pillar is complete + faithful. Board 46/46; products_verify OK (215/221/0); `eden/tools/nutrient_resolve.py report` exit 0. DONE (each committed+pushed):
- **P1** SaXi extracted → completeness now exhaustive (432-manifest fully reconciled; SaXi was the sole genuine single-product gap).
- **P2** the 2 "data errors" (osteoprocare Copper 0mg/56%, ultimate-iodine Zinc 55mcg/5%) = faithful transcriptions of MANUFACTURER label misprints — NO change (coverage reads amount, never pct_dv).
- **P3** 12 nutrient-name spelling collisions normalized (Vitamin B-6→B6, etc.); SURFACED 65 more ingredient-vocab collisions → deferred to 9.2.
- **P4** Sodium/Potassium unified into nutrients[] (10 moved out of macros{}).
- **P5** non-quantified rows = faithful/complete (%DV-only labels), no change.
- **P6** servings_per_container backfilled (VitalStart 7, Integris K2 30, CoQ10 60 — user-supplied; cream N/A null).
- **P7** CFU made comparable: per-strain → amount+unit (billion/million CFU); blend-total → NEW `total_cfu` field (womens-probiotic 35B, integris-probiotics 10B, ultimate-flora-fx 4B, BTT-2.5 1B); products_verify extended + negative-tested.
- **P8/8b** name→essential reconciliation (read-only) + label-verified 7 name-only amino products (all faithful). Corrected coverage = 51/91 essentials with a usable amount (I undercounted by ignoring blend ingredients — see [[typical-values-are-usable]]).
- **(trapped actives)** captured the 5 quantified actives stuck in label text as sub_ingredient amounts (Caffeine 100mg ×2, Trans-Resveratrol 30mg, SOD 140IU ×2) + fixed the 3-0-rise green-tea/caffeine mislabel; products_verify now validates sub-amounts.
- **P9.1** `eden/tools/nutrient_resolve.py` (NEW, tested): maps every substance → essential slug or botanical, over nutrient rows + blend ingredients + sub-ingredients; 15/15 known-value checks; NO essential missed (40 untouched = rare-earth/foundational minerals, which have no per-element product numbers).

**★ DECISIONS LOCKED during remediation (do NOT re-litigate):** Vitamin K2 → the vitamin-K essential; Sulfate → Sulfur; PABA + non-canon aminos (Glycine/Cysteine/Glutamine/GABA/…) → botanicals (own registry entries, NOT essentials); "Typical" fatty-acid/amino profiles = DEFINITIVE usable amounts; EVERY quantified number (nutrient + ingredient + sub-ingredient + blend total + total_cfu) must be comparable in the lookup; units stay label-faithful in the pillar, conversion lives in the registry/derive layer (never lossy in-pillar).

**★ RESUME HERE — Phase 9.2 (finish the registry), THEN seal:**
1. **[DONE 2026-07-08, CHUNK 1]** Externalized the resolver's alias table → `eden/catalog/nutrients.json` (Catalog pillar): `essential_aliases` (36 vitamin + 3 mineral) + `canonical_forms` (62). Resolver reads it. **Creating the file RE-LIT the dormant substance-half of `references_resolve`** (the README's Phase-F intent) → registered all 408 corpus `other_substances` slugs in `nutrients` (display_name = `corpus_derive.humanize` so the derived other-substances index stays byte-identical; canon_slug = null — none resolve to an essential, confirming the corpus's clean essentials/other split). Corrected 3 now-stale "substance half is DORMANT" messages in `invariants.py` (R9). The gate is now STRICT: a claim can no longer cite a phantom/typo substance. See memory [[substance-registry-and-triage-buffer]].
2. **[DONE 2026-07-08, CHUNK 1]** Canonicalized the botanical vocabulary: the 62-entry `canonical_forms` map collapses the collisions (Latin binomials genus-Title/species-lower; L-amino capital residue `L-Alanine`; brand-faithful `Fibersol-2`; `Cordyceps Mushroom` common vs `Cordyceps sinensis` binomial) → 717 surface forms → 655 canonical, ZERO remaining collisions; `Bacillus subtillus`→`Bacillus subtilis` spelling merge (Luneth). The products pillar stays BYTE-FAITHFUL to the labels; canonicalization lives in the registry layer (never lossy in-pillar). Resolver adds `canonicalize()` / `slug_of()`; 21/21 known-value checks pass.
3. **[DONE 2026-07-08, CHUNK 2]** Unit conversion: `nutrient_resolve.to_canonical()` — substance-specific IU→mass (A 0.3mcg, D 0.025mcg, E 0.67mg) + g/mcg/mg mass; a non-scalar "<20" label bound → None (surfaced in _stats, not guessed). Writing its 5 self-checks caught + FIXED a latent `canonical_unit` bug (its mcg-vitamin set was keyed by display-name retinol/cholecalciferol/… which never matched the vitamin-* slugs → silently returned mg for A/D/K/B12/folate).
4. **[DONE 2026-07-08, CHUNK 2]** DERIVED artifact `eden/derived/product-composition.json` (NEW `eden/tools/products_composition_derive.py`; in `eden/derived/MANIFEST.json` → freshness-gated by `derived_artifacts_fresh`): per essential → the products delivering it + summed amount in the canonical unit, sorted amount-DESC; + 655 canonical botanicals → products. Promoted the resolver to the LIVE invariant `product_registry_resolves` (board 46→47). NOTE: NOT wired into the runtime coverage matcher — that unification is the Coverage deep-dive (flagged §00.B drift; this artifact is the authoritative resolution it will read). The cost-per-nutrient RECOMMENDER (saturating adequacy + breadth + banded cost) is a Phase-G feature — design locked, memory [[cost-per-nutrient-match-score]].
5. THEN seal products.json (+ reseal the catalog to cover the new `nutrients.json`) — USER-signed golden.sha256 — + add products_hash_integrity / product_labels_accounted / products_derived_fresh + promote products_verify to a live invariant. (`eden/tools/products_seal.py` does not exist yet — create it patterned on `catalog_seal.py`; add nutrients.json to `catalog_verify`/`catalog_seal` FILES at reseal.)

**★ KEY CONTEXT:** all 91 Wallach targets are honest gaps today (no numeric amount) → the "% toward target" coverage VERDICT awaits corpus dose-mining (Phase G / the owed audit). The registry already powers composition aggregation + cost-per-nutrient + potency comparison NOW.

## The overhaul in one paragraph (context)
Full structural overhaul after book citations were caught hand-typed ~200×. **Model:** TWO hand-edited sealed sources — the Wallach Corpus (`eden/corpus/`, live) + the Youngevity Product DB (`eden/products/`, **now being built**) — plus the shared Catalog (`eden/catalog/`). Everything else is GENERATED + freshness-gated. Phases A–D (governance reset, Catalog pillar, derivation pipeline + poison purge, knowledge-layer collapse) DONE. Phase E (Coverage data-clean + polished) done enough; the per-essential deep-dive + real coverage math are Phase-F-gated. **We deferred finishing Knowledge/Coverage until the product data exists (Luneth's call: don't design more moving parts before the data breadth is in).**

## ★ PHASE F — Youngevity Product DB (`eden/products/`). THE ACTIVE WORK.
**What it is:** the sealed COMPOSITION record of every Youngevity product — what's actually in each. Powers the Coverage math ("does my regimen hit my Wallach targets?"), cost-per-nutrient recommendations for ALL 91 essentials ("cheapest product for my selenium goal"), and blend/ingredient search. Replaces the transitional `eden/eden-catalog.json`.

**★ AUTHORITATIVE SCHEMA + DESIGN: `eden/products/README.md` — READ IT FIRST.** (It supersedes the blueprint §2.1 "Shape (proposed)" sketch, which was simpler than what we actually locked.) Summary:
- `products.json` — composition, keyed by `product_id`. Product = `{product_id, name, sku, ygy_id, components:[…]}`. Component = `{role?, form, serving_size, servings_per_container, directions?, extract_ratio?, macros{}, nutrients[], blends[], other_ingredients[], source_label}`. Multi-panel products (tablet+softgel) → multiple components. nutrient = `{name, form?, amount, unit, unit_detail?(RAE/NE/DFE), label_iu?, pct_dv, part?, latin?}`. blend = `{name, total{amount,unit}|null, pct_dv?, as_labeled, ingredients:[{pos, name, part?, form?, latin?, standardization?, amount?, sub_ingredients?}]}` — **array ORDER = label order = descending amount (FDA)**.
- `prices.json` — VOLATILE sidecar (retail/wholesale + YGY description), all 432, keyed by ygy_id, auto-generated from the saved .mhtml manifest. NOT sealed.
- `warnings.json` — VOLATILE sidecar, YGY label warnings, source:ygy, quarantined.
- Enforcement: `eden/tools/products_verify.py` (structure + PROSE-CONTAINMENT, negative-tested). §00.A: composition-only, NEVER a target; the ONLY long free-text token anywhere is a blend's bounded `as_labeled`; prices/desc/warnings live in the source:ygy sidecars, never a Wallach claim/target.

**★ DECISIONS LOCKED (do NOT re-litigate — Luneth signed off):** (1) prices/desc separated into volatile prices.json; (2) bounded `as_labeled` fidelity token per blend/ingredient; (3) EVERY ingredient catalogued (essentials→91 + botanicals/actives) into the rebuilt registry; (4) warnings → warnings.json sidecar; (5) commit every few batches (not per batch); (6) bundles/paks skipped (no label of their own); non-supplements (books/devices/patches) excluded; multi-panel single products → components[]. (7) **ONE RECORD PER FORMULA, not per sku** — a pack-size variant sharing an IDENTICAL formula → a `variants[]` entry (sku·ygy_id·name·form·source_label); a sku whose formula DIFFERS gets its OWN record; older/regional label images of the SAME sku → `additional_labels[]` (both fields documented in README). (8) amino profiles captured AS BLENDS (per-AA amounts where labeled). (9) a buy-together 2-product bundle that's the ONLY way to get either item (e.g. 3.0 Rise & Restore) → ONE record with TWO role-named components. (10) for same-name CLUSTERS, get Luneth's authoritative label→product map before finalizing — even careful formula-matching can mislabel WHICH product a label is (BTT/osteo/CAL/Collagen all needed his map).

**★ THE EXTRACTION WORKFLOW (the process we established — FOLLOW IT):**
- SOURCES (in `temporary/`, gitignored + PERSISTENT across sessions): `temporary/labels/` = 204 downloaded label images; `temporary/ygy-products-full/` = saved listing .mhtml (all 432 products' names/prices/descriptions).
- LOOKUPS (persisted in `temporary/phase-f-tools/`): `products_manifest.json` (432 products: name/sku/ygy_id/slug/prices/desc — the join data, keyed by Magento ygy_id) + `worklist.txt` (alphabetical base products → candidate label files). Regenerate via `parse_products.py`/`worklist.py` there if needed (fix their I/O paths off the session scratchpad first).
- PER BATCH (~6–10 products): take the next alphabetical BASE products from `worklist.txt` → **READ each candidate label image with vision to CONFIRM identity — NEVER trust the filename** (the fuzzy matcher errs: it SWAPPED the two A.C.T. products + MISSED acv_gummy; Luneth's explicit worry, so always verify by label content) → build records with a small Python builder (pattern in git-history scratchpad `build_batch*.py`: helpers `nut()/ings()/blend()`, pull name/sku/ygy from the manifest, merge into products.json via `safe_write.safe_rewrite`) → `PYTHONUTF8=1 python eden/tools/products_verify.py` (must be 0 problems) → commit every few batches (round-close: build-log + creators_log + `node tools/build.mjs` + push) → periodically show Luneth a spot-check sample.
- MULTI-PANEL (tablet+softgel) → multiple components[]. TINCTURES → `extract_ratio` + warning → warnings.json. Long compound excipients (e.g. Bio Fuel's micellized core) are OK (other_ingredients cap is 350; PROSE_RE still blocks sentences).
- CLUSTERS of near-identical products (BTT ×several, Body Balance ×several, etc.) → DEDICATED side-by-side pass reading all cluster labels together, to avoid misassignment.

**MISSING LABELS:** the matcher's "no-label" list is MOSTLY FALSE-NEGATIVES (the label exists, it just failed to match — proven: Colloidal Silver/ZRadical/TruBoost/Vitali-C/RenuIQ/Body Balance etc. all have labels). So the reliable genuine-missing list = base-unique products still un-extracted AFTER all 204 labels consumed. Tracked in `eden/products/MISSING-LABELS.md`; auto-finalize at end (products.json vs manifest, minus bundles + non-supplements). Luneth grabs the genuine missing ones at the END (he asked us to log them faithfully as we go).

**PROGRESS (2026-07-07):** **141 products done — 0-9 through S COMPLETE (Batches 1-24; then 3 SOLD-OUT/discontinued products deleted per Luneth — ASAP, Relieve, Super Cell Protector; PreWorkout Pixie was never extracted).** ★ S-range (Batches 22-24, +21): S1 Slender FX family + SMART/SOUL nootropic sticks (SOUL Stiks blend '356 g'->356 mg, impossible in a 2.7 g stick); S2 singletons (relieve [panel self-un-named -> identity from sku USBE0101101005], super-cell-protector, strawberry-kiwi-mins, super-olive-health tincture [warning->warnings.json 716], synaptiv) — ASAP was a pre-existing dup, caught by the verify gate + dropped; S3 the full Sta-Natural capsule cluster (8) read side-by-side (sta-natural-plant-shake amino profile = null-total blend). warnings.json=20. ★ Session M-R (Batches 16-21, +37 products): highlights — caught the STX-BOGO→Ultimate-Mineral-Caps mislabel + the two-muscadine split (syrup vs grape-seed); Profemme=Prostat confirmed identical Graminex; the 6-product Pollen Burst cluster; ReVERSE! 2-component comprehensive; two label misprints (Recharge FIXED via Luneth's corrected Rebalance PNG label; Refresh FX thiamin '7 mcg'@583% flagged, recorded faithfully). warnings.json=19; products_verify now accepts .png labels. Done EARLY out of later ranges: ultimate-mineral-caps (U), premium-muscadine-grape-seed (P). Per-batch detail in build-log + creators-log. ── EARLIER (B1-15): **50 products done — the 0-9 through E ranges are COMPLETE.** Batches 1–4 (14, spot-checked 100%). B5 `20c6c3f` = Beyond Tangy Tangerine/BTT line (6). B6 `a70a8b2` = rest of B (Bio Nite, Bliss, Body Balance liquid+Sticks, Bone & Tissue tincture, Bone Building). B7 `cb4fd0d` = osteo cluster (5: Beyond Osteo FX Liquid+Powder, Osteo-Mag, OsteoProCare, Ultimate Osteo FX) + verified 3.0 Rise & Restore already held both panels. B8 `292ee6f` = full C-range (15). B9 `cbffc5f` = D+E range (4: Digestwel+, ElectroFuel, Energize Wellness Tea, Eucalyptus/Honey Thyme Syrup). B10 `5708783` = the F-range (4: Female Hormonal Support tincture; FlexeoPlus + Flexi-Care Plus joint liquids — both were matcher NO-LABEL false-negatives, labels found + read; FucoidZ fucoidan capsules). warnings.json = 5 tincture entries (647/718/663/658/867). Every product verified by label CONTENT; cluster label→product maps came from Luneth. **ZRadical cluster mapping confirmed while extracting FucoidZ:** ZRadical Powder Canister = sku 3207 (label `zradical_powder_canister_suppfacts_0525.jpg`; calories/B-vitamins + a different fucoidan+fruit blend — distinct from FucoidZ capsules). Still to place in the Z-range pass: ZRadical 32floz (sku 3026, matcher NO-LABEL) + `zradical-collagen_suppfacts-0925_1_.jpg`. B14 `b66ac19` = the K-range (4: Kid's Toddy liquid multi; Kidsprinklz powder packet [matcher NO-LABEL false-neg]; Killer Biotic FX immune caps; Kidney & Bladder Support GH tincture — the last was NOT in worklist.txt, found via label hunt). Post-B13 crack: the audit found 5 BTT Whole-Foods blends lacked `as_labeled` — BACKFILLED verbatim from re-read labels + made `as_labeled` REQUIRED in products_verify (AS_LABELED_MAX 600→1200 for latin-name blends), negative-tested, audit now 0 problems. i26 FV powder (USLL005060) resolved from Luneth's label (own formula). B15 `d08726c` = the L-range complete (8 products, 9 components: Life Balance menopause caps; Life Solubles rice bran + Cookies'N Cream + Pumpkin Spice food shakes [Luneth supplied the 2 missing labels]; Liquid Gluco-Gel; Liver&Gallbladder + Lymphatic GH tinctures [warnings 681/696]; LiverPure = single-SKU KIT modeled as ONE product w/ 2 role-named components LiverPrep+LiverClean, decision #9). warnings.json now 11 entries. Convention notes: food labels' %DV-only micros recorded amount=null + %DV (check_amount allows null); polyunsaturated/monounsaturated_fat macro keys used (not whitelisted). B11 `80b9034` = the G-range (4: G.I. Cleanse parasite-cleanse tincture; Get Go N Plus energy powder; Gluco-Gel Plus joint liquid; Glucogenix blood-sugar tablets). warnings.json now 6 entries (+572). **Gluco-Gel cluster noted:** Liquid Gluco-Gel (sku 13216, `liquid-gluco-gel_suppfacts-0224.jpg`) + Ultimate Gluco-Gel 240 (sku 21252, `ult-gluco-gel-240_suppfacts-1119_2.jpg`) carry distinct-filename labels — extract in the L/U ranges (Gluco-Gel PLUS = USYG100050 already done). B12 `b1ebe9a` = the H-range (5: HGH Amino Acid Blend caps; Harmony Drops sea-mineral electrolyte drops; Heart Support + Hypothalamus Support GH tinctures; Herbal Rainforest tonic). warnings.json now 8 entries (+990,+675). **★ FLAG for Luneth:** Herbal Rainforest's Proprietary Herbal Blend total prints `16 µg` on the label — implausibly tiny for 15 botanicals (likely a misprint for mg); recorded FAITHFULLY as 16 mcg, verify against a hi-res label. **HOPE ×2 RESOLVED 2026-07-07 (commit `6b25ff3`):** SN HOPE (ygy 577) extracted as `hope-sta-natural` (Essiac-style capsule); ALSO fixed **herbal-rainforest** from a correct label Luneth supplied — Youngevity.com's own label was WRONG (correct: Vit A 13% not 45%, blend 31.3 mcg not 16 µg, 16 herbs incl. Golden Seal, no Potassium line, other-ingredients corrected). **HOPE (Youngevity, ygy 10359 / USYG7000) = a bundle** of 3 individually-sold products (Life Balance Menopause Support USYG70005, Rejuvenate/Rewind/Restore USYG70006, Women's Probiotic COMPLETE USYG70007) → SKIP per standard bundle policy; its 3 components record individually in their L/R/W ranges. B13 `eab24e9` = the I-range (9: the i26 Hyperimmune Egg family ×4 — banana-coconut USLL006012, french-vanilla chewables USLL006011, 135-capsules USLL006014, 31-day powder canister USLL005030, all read side-by-side per the clusters rule; Immu-911; Immune Support GH tincture; Integris probiotics/CoQ10+E/K2). warnings.json now 9 entries (+651). Genuine-missing flagged: i26 Hyperimmune Egg Powder French Vanilla (USLL005060, ygy 9965 — likely a flavored twin of the canister) + Imortalium (already listed). Two Integris servings-per-container are honestly null (CoQ10/K2 crops omit the count).

**★ EXTRACTION-END DETAIL (historical — current status is the ★ PRE-SEAL REMEDIATION section near the top):** PHASE F EXTRACTION COMPLETE (2026-07-08) — 214 products at extraction end (215 after SaXi added in remediation P1). Final ranges landed 2026-07-08: X = xerafem (nested Imortalium Youth Complex blend-of-sub-blends); Z = zinc-fx (+ 4-pack/6-pack variants), zradical-liquid, zradical-powder-canister, zradical-plus (with collagen); GLP-1 line = ygy-activate-glp1, ygy-elevate-glp1, ygy-regulate-glp1 (the 'YGY' prefix is KEPT — it is on the packaging name, confirmed Luneth 2026-07-08; NOT the same as the redundant 'Youngevity' prefix). Reconciliation gaps extracted: sinus-allergy-support (USGH000017/708, warning->warnings.json), zinc-immune-support (USYG100072/7777). NAMING RULE (Luneth 2026-07-07): the 'Youngevity' prefix is redundant on Ultimate-branded products — drop it (applied to Ultimate Iodine + Super Greens; apply going forward). RECONCILIATION DONE (2026-07-08): all 243 label files accounted — 229 referenced + 14 orphans (alternate crops of already-extracted products, OR sold-out ASAP / Super Cell Protector / Relieve / PreWorkout Pixie). The 432-entry manifest was cross-checked: every unmapped entry is a bundle / pack / multipack / device / book / sold-out — NO single supplements missing. The only 2 genuine gaps it surfaced (Sinus & Allergy Support, Zinc + Immune Support) are now extracted. warnings.json = 30 entries.

## ★ STILL OWED (unchanged — AFTER Phase F, before Phase G)
1. **FULL corpus audit** — `corpus_audit_gate` holds (~1203 claims frozen); audit ALL claims before resuming book mining. Run `eden/tools/corpus_audit.py` → review worklist w/ Luneth → unlock.
2. **Consolidate the mining-mechanics memory cluster** (reading-and-correcting-scanned-pdfs, editing-sealed-corpus-claims, batch-mining-workflow, cross-book-uniform-correction, quote-sync-mechanism).

## Then G → H → I
- **G** resume book mining (only after the audit). **H** Search (retrieval-first). **I** Journey (last) + the portable offline browser.

### ★ Book-mining anti-drift — the 3-leg system (tune-up landed 2026-07-07, ready for Phase G; does NOT touch the Phase-F products flow)
Book mining now has the same 3-leg safety net products has:
1. **Coverage gate `mining_coverage_accounted` (NEW, live).** Ledger: `eden/tools/mining-coverage.json`. A book flagged `mining_status:"complete"` must have every source page (screenshot books epigenetics/immortality/iaiyh — auto-derived from `locator.char_offset` → nearest `Screenshot(N)` marker) or every `sections[]` entry (chapter books dddl/rare-earths/lets) either claim-bearing OR reviewed-empty-with-a-reason, else RED. `incomplete` books (all 6 now) report a coverage snapshot only, NEVER red — so it never falsely reds the legitimately-incomplete corpus. It already surfaced the known under-mining (immortality 49/254 pages, epigenetics 66/465, iaiyh 19/34). Wholesale accounting only — sub-page per-claim completeness stays with the end-audit. (Deliberately uses `char_offset`→marker, NOT `locator.screenshot`, which is populated inconsistently per book — verified.)
2. **Manual checkpoint command (Luneth saved it).** Per chapter: re-read the book's mining-policy memory + `eden/corpus/SCHEMA.md` + the ledger, audit the last N pages/claims showing the check for each (verbatim⊆source, condition named, citation is a `book_id`, mappings ∈ catalog, dose sane, right tier), report gaps only, change nothing.
3. **End-audit = the existing `corpus_audit_gate`** (the mandatory full corpus audit — STILL OWED #1 above).
**When Phase G mines a book to done:** resolve its zero-claim pages/sections in `mining-coverage.json` (reason each), THEN flip `mining_status:"complete"` — the gate then proves no wholesale skip.

## Backlog + left-noted (not lost)
- OLD BACKLOG: menaquinone→phylloquinone book correction + K1/K2 alert-box → Phase E/G; Immortality + DDDL re-mine (under-mined) → Phase G.
- LEFT-noted cosmetics: dashboard.html header references a non-existent `dashboard/ARCHITECTURE.md` + stale mount-slot comments; SEALED `design-system.css` header references `knowledge/design-wisdom` (fix at next re-seal); ultra-narrow Coverage tile-name wrapping.
