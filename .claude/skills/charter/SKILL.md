---
name: charter
description: Read when proposing a new system, tool, or invariant, when auditing an operating file against the rules, or when deciding whether a rule may be presented as enforced. Holds the R1-R9 enforceable rules, each naming the gate that proves it and its honest status.
---

# The Charter -- R1 through R9

A small closed set of enforceable rules. **Every rule names its gate and its status**, so a rule is
never sold as safer than its enforcement.

- **LIVE** -- the gate runs in `tools/invariants.py` or a hook today.
- **PARTIAL** -- some coverage is live; the rest is a labeled WISH.
- **WISH** -- no gate yet. The rule binds by discipline and review, and says so. The label *is* the
  honesty (R7), not a waiver.

| # | Rule | Gate (the proof) | Status |
|---|---|---|---|
| R1 | Only the two sources (Wallach Corpus, Youngevity Product DB) + the Catalog are hand-editable. Every shipped artifact is generated from them. | `derived_artifacts_fresh` — regenerate every manifest artifact from the sealed pillars, byte-compare to disk; `data_artifacts_accounted` — every assets/data file is registered in the manifest (derived-and-gated OR allowlisted-with-reason), so the list can't be silently incomplete; the write-guard blocks edits to generated paths. | **PARTIAL.** LIVE: `derived_artifacts_fresh` (the manifest freshness gate over `eden/derived/MANIFEST.json`; absorbed `corpus_embed_synced`), `data_artifacts_accounted` (the completeness half: no hand-maintained data file escapes the manifest), `dashboard_dist_fresh`, and the per-pillar seal gates `corpus_integrity` (the corpus half) · `products_hash_integrity` · `catalog_integrity` · `graphics_integrity`. **A seal proves a pillar has not CHANGED — never that it is RIGHT** (the canon mineral tiers were mockup-derived, sealed, and green for three weeks). `coverage-layout-data` derives from `essentials-canon` via a hand-authored skeleton (its 91 names + 60 mineral symbols/numbers are canon-owned, no longer hand-typed). WISH: promote the remaining `accounted` files that should derive (the Products embeds) + retire the version-stamp `eden_embeds_match_canonical`. |
| R2 | Wallach-only for every recommended amount / range / dose. Youngevity = composition only, never a target. | `amounts_wallach_only` — a numeric coverage target carries a `source_claim_id` → a sealed Wallach dose claim, and recomputes from its documented transform chain; a Youngevity-sourced amount = RED. **SCOPE:** it reads ONE artifact (`essentials-targets-data.json`) and `essentials[].target.low` **and** `target.ceiling` — **36 numeric + 1 stated ceiling, of 91**; the other **54 are skipped** (non-numeric targets), and it never reads `target.range`, `target.other_claims` (31 carry it), or `target.kind`. `dose_amount_in_verbatim` — the claim→book link: the dose number must be present in the claim's OWN verbatim, unit-adjacent + row-scoped (without it, R2 anchors a target to a claim field that is ours and unverified). `collective_doses_not_fanned` — a shared Wallach budget may not post a per-member number. | **LIVE.** `amounts_wallach_only` — every numeric coverage target in `essentials-targets-data.json` carries a `source_claim_id` resolving to a sealed Wallach dose claim that maps the essential; a Youngevity-sourced/unsourced amount is RED. **Tightened (R9 refinement):** the gate no longer stops at provenance-existence — it re-runs the documented transform CHAIN (upper-of-range → IU-factor → ×1.54 weight-scale + round-2sf), anchors each `provenance.original_*` to the sealed claim's dose, requires every IU factor to equal the known physical constant (0.3 / 0.025 / 0.67), and byte-compares the recompute to the posted number; a fabricated value, planted factor, arithmetic drift, or stale artifact all go RED (proven by a 7-case negative test). The poison was purged (targets now derive from base-line dose claims, not Youngevity labels). The 3 transitional `wallach_stance_*` gates retired with the rotten inline embed. `citations_reference_registry` (the last R2/R3-family WISH) has LANDED — see R3. **Extended to the RENDER layer:** `views_no_ciphered_data` (critical) — the decorative `.ds-cipher` glyph-scrambler may wrap view-local chrome but NEVER a value imported from `state/` or `core/`. Found live: the Coverage hero ciphered `essentialCount()`, rendering Wallach's 90 as 30/80/94 four seconds in five — a number with a perfect provenance chain fabricated at render time, a failure mode every source-side gate was blind to. Proven by an 8-case negative test (fires on nested/wrapped/chained data access; spares hexSerial hashes + static placeholder chrome). R9: the first cut banned all interpolation, over-fired on 4 legitimate view-local sites, and was TIGHTENED to the state/core import boundary — not loosened, with the sparing cases pinned in the test. |
| R3 | One source per fact, referenced by ID. No canonical value hand-written twice. | `references_resolve` (every ID resolves to a catalog/registry entry) + `no_hand_duplicated_canonical` (derived copies only) + `citations_reference_registry` (book refs = `book_id`, display composed) + `no_duplicate_claims` (the same span may not answer the same question twice: same book + verbatim containment + same subject + same facet). | **PARTIAL.** LIVE: `references_resolve` (every claim condition/symptom slug ∈ the Catalog; the substance/`other_substances` half is **ACTIVE** since the nutrient rebuild — 408 substances registered, and the gate says so in its own output. *This cell went on reading "DORMANT" long after that half went active — the Charter UNDERSELLING its own enforcement, which no gate catches: `charter_gates_present` only looks for OVER-selling.*), `no_hand_duplicated_canonical` (no canonical essential name re-stored outside `essentials-canon`), `citations_reference_registry` (book refs = `book_id`, no hand-typed cite, on the clean Charter surface). `citations_reference_registry` + `prose_contained` were later extended to the legacy DATA embeds (regimen-base/scanner/ocr). `no_duplicate_claims` (the twin-card class: review found two near-identical "What is Vitamin A?" cards on one entity page and **no gate caught it**; 13 duplicates were removed. Containment ALONE is not the signature — 209 containment pairs are legitimate, one Wallach paragraph answering several questions. The 2 ruled keep-both pairs are allowlisted **in-gate with stated reasons, NOT in `.claude/invariant-baseline.json`** — that file is invariant-SCOPED, so one entry there would have tolerated every future duplicate. Negative test: `tools/tests/test_no_duplicate_claims.py`, 13 cases, replants the real vitamin-A pair). WISH: inline view prose + every pillar identity field. **Also WISH (R7):** a duplicate whose two takes each add bytes the other lacks — neither contained — escapes; none exists today, tighten with a measurement if one ships. |
| R4 | Prose is contained: summaries / descriptions / alert-boxes / glosses live in ONE compartment, ID-attached, single-copy — never inline in code, never in a fact field. | `prose_contained` — prose only in designated prose fields; the scan fails if prose-shaped text appears in code or a fact field. | **PARTIAL.** LIVE: `views_state_no_inline_data` (blocks literal arrays/objects > 10 elements in `views/` + `state/`) + `prose_contained` (no prose-shaped text in a fact field on the clean Charter surface: corpus claims + canon + catalog + corpus-derived targets/coverage-layout; later widened to the legacy regimen-base/scanner/ocr data embeds). WISH: the FULL R4 (verbatim = a claim pointer + a single-copy per-essential prose store) + extend to inline view prose. |
| R5 | The mining gate: a record cannot land unless it passes every structural check. | The `mine` pipeline — verbatim ⊆ source · citation ∈ registry · mappings ∈ catalog · prose-contained · units-sane · amount-has-Wallach-source. Fail → cannot seal → board RED. | **LIVE (corpus).** `corpus_integrity` + `mined_pages_clean` + `verbatim_names_mapped_conditions` + `book_source_clean` + `verbatim_over_soft_limit` + `umbrella_proxy_named` + the gloss gates (`claim_text_term_gloss`, `jargon_terms_glossed`, `glossary_wellformed`). **LIVE (products):** `products_verify` (215 products · 221 components · 0 problems) + `products_hash_integrity` + `product_registry_resolves` + `nutrient_resolver_parity`. *This cell went on reading "WISH: extend to products" while that gate was live and green — the Charter drifting STALE IN BOTH DIRECTIONS (R3 undersold too) is the tell that nothing re-audits it.* WISH: the R5 mine gate does not yet cover a product's SOURCE label-image transcription the way `dose_amount_in_verbatim` now covers a claim's dose. |
| R6 | Logs are sacred + append-only. Deletion is structurally blocked; the only override is the 3-part ALL-CAPS ritual. BUILD → TEST → LOG → REPEAT is sacred. | `creators_log_append_only` + `build_log_append_only` (+ any new sacred log); the round-close + `stop_round_close.py` hook enforce the cycle. | **LIVE.** `creators_log_append_only`, `build_log_append_only`, `creators_log_{well_formed,digest_synced,embed_synced,bundle_synced,archive_synced}`. |
| R7 | Codify, don't promise. Every enforceable rule ships its gate in the same patch. Unenforceable items are labeled WISH. | This Charter's own Status column; the `charter_gates_present` meta-gate fails if a rule names a gate that neither exists nor is labeled WISH. | **LIVE.** `charter_gates_present` parses this R1–R9 table and RED-flags any Gate-column gate name that is neither a live invariant / known mechanism nor labeled WISH — the Charter can no longer oversell its own enforcement. WISH (labeled): the SEMANTIC half — proving a present gate actually ENFORCES its rule — has no non-gaming machine check and rests on review. |
| R8 | No poison left behind. Everything kept is re-audited against R1–R6; violations are purged, not grandfathered. | The migration runs every preserved file through the gates; the board cannot go green with a violation present. | **WISH.** Enforced by the migration itself (each kept file re-passes the gates); no standalone invariant — it is the sum of R1–R6's gates applied to the surviving files. |
| R9 | Refinements are codified too. A misfiring gate is fixed by re-codifying with proof — tighten the check, or add an auditable, versioned exception with a reason + a test. Never a silent loosening. | `exceptions_justified` — each baseline exception carries a reason + a test; an invariant fails on an unjustified exception. The refinement ships with the misfire it fixes (R9). | **LIVE.** `exceptions_justified` — every entry in `.claude/invariant-baseline.json` must be an object `{invariant, reason, test}`; a bare-string or reason-less/test-less exception is RED. The baseline is empty today (vacuously clean), so the gate stands for the next exception. `stop_round_close.py` reads the same entries by their `invariant` name. |

## How to use
- **Proposing a new system, tool, or invariant:** for each of R1-R9 name whether the change honors,
  stresses, or violates it. A rule that CAN be a gate ships its gate in the same patch. If it cannot
  yet, label it WISH here -- never sell it as safe.
- **Closing a chunk:** R5 and R6's gates are on the round-close board; a red R6 hard-blocks.
- **Auditing an operating doc:** every operating doc must agree with this table. `charter_gates_present`
  catches this file OVER-selling its enforcement -- it does **not** catch under-selling, and this
  file has drifted stale in both directions before. Nothing re-audits it automatically.

## Above and around it
CLAUDE.md's two prime directives sit **above** the Charter; R1-R9 are their enforceable
operationalization. The per-domain how lives in the sibling skills: `wallach-source-rule` (R2),
`engineering-doctrine` (section 00.B), `round-close` (R6), `dashboard-code` (R1/R3/R4),
`write-discipline` (the safe_write primitive and the Windows-host quirks).
