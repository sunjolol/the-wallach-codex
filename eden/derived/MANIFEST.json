{
  "schema_version": 1,
  "_purpose": "The registry of every GENERATED data artifact + its ONE pure generator (Phase C / blueprint D2). The derived_artifacts_fresh invariant iterates this list: for each entry it re-runs the generator over the sealed pillars and compares to the on-disk artifact, so a hand-edit or a stale build of any listed artifact is un-shippable (R1). eden/tools/build_embeds.py iterates the same list to regenerate. GROWS as Phase C folds each embed into the pipeline (C3: the product embeds).",
  "_contract": "Each generator module exposes build_fn() -> the artifact object (pure, no write; used by the freshness gate) and write_fn() -> regenerates the on-disk file via safe_write (used by build_embeds). compare='json' means the gate compares json.loads(disk) == build_fn(); 'text' means exact-text. inputs list the pillar paths the generator reads (documentation + future input-hash tracking).",
  "artifacts": [
    {
      "artifact": "dashboard/assets/data/corpus-embed.json",
      "generator": "eden/tools/corpus_embed.py",
      "build_fn": "build_embed",
      "write_fn": "write_embed",
      "compare": "json",
      "inputs": [
        "eden/corpus/claims/",
        "eden/corpus/indices/",
        "eden/corpus/essentials-canon.json",
        "eden/corpus/books-meta.json",
        "eden/corpus/books-roadmap.json",
        "eden/corpus/knowledge-version.json",
        "eden/catalog/conditions.json"
      ],
      "note": "Slim Wallach claim graph inlined into the bundle for the Knowledge drawer (esbuild JSON import in state/corpus.ts). Freshness was previously gated by the standalone corpus_embed_synced, now folded into derived_artifacts_fresh."
    },
    {
      "artifact": "dashboard/assets/data/essentials-targets-data.json",
      "generator": "eden/tools/targets_derive.py",
      "build_fn": "build_data",
      "write_fn": "write_data",
      "compare": "json",
      "inputs": [
        "eden/corpus/essentials-canon.json",
        "eden/corpus/claims/",
        "eden/corpus/books-meta.json"
      ],
      "note": "The Coverage targets DB (state/coverage.ts esbuild JSON import). Numeric targets are Wallach-ONLY (R2), sourced from base-line supplement-program dose claims + carrying source_claim_id (gated by amounts_wallach_only). The per-essential coverage_kind (non-numeric classification: trace_pdm / dietary / dietary_with_clinical_lever / unspecified) reads from the essentials-canon pillar. Phase D-b (2026-07-05) re-homed coverage_kind onto essentials-canon + deleted the old poison hand-file knowledge/essentials-targets.json (its dead wallach_stance prose went with it; clean stances re-authored post-mining)."
    },
    {
      "artifact": "dashboard/assets/data/regimen-label-lookup.json",
      "generator": "eden/tools/products_embed.py",
      "build_fn": "build_embed",
      "write_fn": "write_embed",
      "compare": "json",
      "inputs": [
        "eden/eden-catalog.json"
      ],
      "note": "The product-vault embed (regimen-label-lookup) read by views/regimen.ts (Full-edit) + views/knowledge.ts (Products tab) via esbuild JSON import. Phase C3 migrated it off the inline dashboard.html <script> block (D1) and retired the version-stamp eden_embeds_match_canonical (this content gate replaces it). TRANSITIONAL source eden/eden-catalog.json; Phase F swaps to the Products pillar. Deterministic (NO timestamp) so build_fn byte-compares to disk."
    }
  ],
  "_accounted_purpose": "The COMPLETENESS half of R1 (crack #1 fix, 2026-07-06). derived_artifacts_fresh proves the `artifacts` above are fresh; it does NOT prove the list is complete. The data_artifacts_accounted invariant requires EVERY dashboard/assets/data/*.json to appear either in `artifacts` (derived + freshness-gated) or here in `accounted` (hand-authored or gated by a dedicated invariant), each with a disposition + reason. A data file in neither list is RED -- no silently-added hand-maintained artifact can ship. disposition = hand_authored | externally_gated.",
  "accounted": [
    {
      "file": "dashboard/assets/data/versions-data.json",
      "disposition": "hand_authored",
      "reason": "App version/changelog history, hand-maintained. Not a projection of the pillars; no generator."
    },
    {
      "file": "dashboard/assets/data/glossary.json",
      "disposition": "hand_authored",
      "reason": "Plain-language term glossary (a designated R4 prose store). Original prose content, not derived; structurally gated by glossary_wellformed."
    },
    {
      "file": "dashboard/assets/data/doctrine-data.json",
      "disposition": "hand_authored",
      "reason": "Knowledge>Doctrine cards (a designated R4 prose store, blueprint 2.4 #4). On the clean Charter surface for prose_contained + citations_reference_registry; original prose, not a pillar projection."
    },
    {
      "file": "dashboard/assets/data/coverage-layout-data.json",
      "disposition": "hand_authored",
      "reason": "TRACKED DEBT: the periodic-table section arrangement is editorial, but each tile's name/sym/num re-stores essentials-canon values by hand -- it SHOULD derive (tiles from canon by slug, arrangement as a minimal skeleton). On the clean surface for prose/citation gates but NOT yet freshness-gated. Deriving it regenerates a rendered artifact (needs visual sign-off); deferred by Luneth 2026-07-06 (crack #1 = gate now, derive later)."
    },
    {
      "file": "dashboard/assets/data/regimen-base-data.json",
      "disposition": "hand_authored",
      "reason": "Transitional HBSP base-foundation COMPOSITION (Youngevity label data, composition-only per the source rule -- never a target). Replaced by the Products pillar (eden/products/) in Phase F."
    },
    {
      "file": "dashboard/assets/data/scanner-corpus-data.json",
      "disposition": "hand_authored",
      "reason": "Scanner reference data (dietary baselines used as a subtractive coverage input, not a recommended target). Reworked with the Products pillar / scanner finalize in Phase E/F."
    },
    {
      "file": "dashboard/assets/data/ocr-dict-data.json",
      "disposition": "hand_authored",
      "reason": "OCR normalization dictionary (label word list) for the local Tesseract scanner. Reworked with the scanner finalize in Phase E/F."
    },
    {
      "file": "dashboard/assets/data/creators-log-embed.json",
      "disposition": "externally_gated",
      "reason": "Generated by tools/creators_log.py from the sacred ledger; gated by its own dedicated invariants creators_log_embed_synced + creators_log_bundle_synced (not the manifest freshness gate)."
    }
  ]
}
