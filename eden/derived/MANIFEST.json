{
  "schema_version": 1,
  "_purpose": "The registry of every GENERATED data artifact + its ONE pure generator (Phase C / blueprint D2). The derived_artifacts_fresh invariant iterates this list: for each entry it re-runs the generator over the sealed pillars and compares to the on-disk artifact, so a hand-edit or a stale build of any listed artifact is un-shippable (R1). eden/tools/build_embeds.py iterates the same list to regenerate. GROWS as Phase C folds each embed into the pipeline (C3: the product embeds).",
  "_contract": "Each generator module exposes build_fn() -> the artifact object (pure, no write; used by the freshness gate) and write_fn() -> regenerates the on-disk file via safe_write (used by build_embeds). compare='json' means the gate compares json.loads(disk) == build_fn(); 'text' means exact-text. inputs list the pillar paths the generator reads (documentation + future input-hash tracking).",
  "artifacts": [
    {
      "artifact": "dashboard/assets/data/corpus-embed.json",
      "generator": "eden/tools/corpus_embed.py",
      "build_fn": "build_embed",
      "write_fn": "write_embed",
      "compare": "json",
      "inputs": [
        "eden/corpus/claims/",
        "eden/corpus/indices/",
        "eden/corpus/essentials-canon.json",
        "eden/corpus/books-meta.json",
        "eden/corpus/books-roadmap.json",
        "eden/corpus/knowledge-version.json",
        "eden/catalog/conditions.json"
      ],
      "note": "Slim Wallach claim graph inlined into the bundle for the Knowledge drawer (esbuild JSON import in state/corpus.ts). Freshness was previously gated by the standalone corpus_embed_synced, now folded into derived_artifacts_fresh."
    },
    {
      "artifact": "dashboard/assets/data/essentials-targets-data.json",
      "generator": "eden/tools/targets_derive.py",
      "build_fn": "build_data",
      "write_fn": "write_data",
      "compare": "json",
      "inputs": [
        "eden/corpus/essentials-canon.json",
        "eden/corpus/claims/",
        "eden/corpus/books-meta.json",
        "knowledge/essentials-targets.json"
      ],
      "note": "The Coverage targets DB (state/coverage.ts esbuild JSON import). Numeric targets are Wallach-ONLY (R2), sourced from base-line supplement-program dose claims + carrying source_claim_id (gated by amounts_wallach_only). kind + wallach_stance are read TRANSITIONALLY from the hand-file knowledge/essentials-targets.json (NO amount is taken from it); Phase D moves the stance to the prose store, makes verbatim a claim pointer, and deletes that hand-file."
    },
    {
      "artifact": "dashboard/assets/data/regimen-label-lookup.json",
      "generator": "eden/tools/products_embed.py",
      "build_fn": "build_embed",
      "write_fn": "write_embed",
      "compare": "json",
      "inputs": [
        "eden/eden-catalog.json"
      ],
      "note": "The product-vault embed (regimen-label-lookup) read by views/regimen.ts (Full-edit) + views/knowledge.ts (Products tab) via esbuild JSON import. Phase C3 migrated it off the inline dashboard.html <script> block (D1) and retired the version-stamp eden_embeds_match_canonical (this content gate replaces it). TRANSITIONAL source eden/eden-catalog.json; Phase F swaps to the Products pillar. Deterministic (NO timestamp) so build_fn byte-compares to disk."
    }
  ]
}
